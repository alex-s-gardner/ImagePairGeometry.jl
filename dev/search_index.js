var documenterSearchIndex = {"docs":
[{"category":"section","location":"#ImagePairGeometry","page":"Home","text":"Geometry of a co-registered image pair on a map-projected grid.\n\nFor each point of a grid defined in projected coordinates (northing/easting), the geometry of a pair of images relates map coordinates to image coordinates in both directions:\n\nforward: grid point (x, y, z) to pixel index in each image of the pair,\ninverse: a pixel displacement between the two images to a velocity in map coordinates.\n\nDerived per grid point alongside that mapping: the expected displacement implied by a reference velocity field, the search extent in pixels implied by a search-range field, chip size converted from meters to pixels, and the scale factors relating image-space distance to ground distance.\n\nThe pixel displacement itself is estimated elsewhere, by a feature-tracking or correlation routine; this package supplies the geometry that routine searches within and the operator that converts its result to velocity.\n\nA pure-Julia reimplementation of the geogrid module of NASA JPL's autoRIFT, the geometry layer beneath the ITS_LIVE glacier velocity products. REFERENCE.md records the reference implementation, the exactness standard held against it, and every deliberate divergence.","title":"ImagePairGeometry"},{"category":"section","location":"#Status","page":"Home","text":"The projected-coordinate path is implemented and verified against the compiled reference. The radar path is not: RadarCoordinate exists so the dispatch shape is settled, and throws.","title":"Status"},{"category":"section","location":"#Walkthrough","page":"Home","text":"Two images, a grid from a DEM, and the per-point geometry of the pair:\n\njulia> using ImagePairGeometry\n\njulia> reference = ImageFootprint(origin = (300000.0, 7800000.0), spacing = (30.0, -30.0),\n                                 size = (400, 400));\n\njulia> secondary = ImageFootprint(origin = (300600.0, 7799400.0), spacing = (30.0, -30.0),\n                                  size = (400, 400));\n\ncoregister intersects the two footprints. The overlap — not either image alone — supplies the origin and size that pixel indices are relative to, so the secondary image determines them as much as the reference does:\n\njulia> pair = coregister(reference, secondary; dt = 91 * 86400.0);\n\njulia> pair.coordinate.origin, pair.coordinate.size\n((300600.0, 7.7994e6), (380, 380))\n\njulia> pair.reference_offset, pair.secondary_offset\n((20, 20), (0, 0))\n\nA grid, then the part of it the pair covers:\n\njulia> grid = MapGrid(geotransform = (295000.0, 120.0, 0.0, 7805000.0, 0.0, -120.0),\n                      size = (200, 200), crs = 32624);\n\njulia> window = grid_window(grid, footprint_bounds(IdentityTransform(), pair.coordinate))\nCartesianIndices((47:142, 47:142))\n\nThe inputs cover that window. Only the DEM is required; each other raster gates the outputs that depend on it:\n\njulia> n = size(window);\n\njulia> inputs = GeometryInputs(dem = fill(500.0, n),\n                               dhdx = fill(0.02, n), dhdy = fill(-0.01, n),\n                               vx = fill(120.0, n), vy = fill(-80.0, n));\n\njulia> result = pairgeometry(grid, pair, inputs; window);\n\njulia> size(result), nvalid(result)\n((96, 96), 9025)\n\nThe window is a conservative bound on the footprint, so its outermost points can fall outside the image. Those are nodata rather than an error:\n\njulia> result.location_x[1, 1] == -32767\ntrue\n\njulia> k = CartesianIndex(50, 50);   # comfortably inside\n\njulia> result.location_x[k], result.location_y[k]\n(195, 195)\n\njulia> result.offset_x[k], result.offset_y[k]     # expected displacement, pixels\n(1, 1)\n\njulia> result.scale_x[k], result.scale_y[k]       # exactly 1: grid and image share a CRS\n(1.0, 1.0)\n\nThe operator converts a pixel displacement to a map velocity. Here a displacement of two pixels in x and minus one in y:\n\njulia> vx = result.off2vx_dx[k] * 2.0 + result.off2vx_dy[k] * -1.0;\n\njulia> vy = result.off2vy_dx[k] * 2.0 + result.off2vy_dy[k] * -1.0;\n\njulia> round(vx, digits = 2), round(vy, digits = 2)\n(240.66, -120.33)","title":"Walkthrough"},{"category":"section","location":"#Larger-grids","page":"Home","text":"pairgeometry_blocked computes the same result block by block, optionally across threads. It is bit-identical to the unblocked run at any block size and task count, because every grid point depends only on its own inputs:\n\nusing Proj   # provides ProjTransformFactory\n\nsource = InMemoryInputs(inputs, window)\nresult = pairgeometry_blocked(grid, pair, source;\n                              transform = ProjTransformFactory(3413, 32624),\n                              blocksize = (512, 512), ntasks = 8)\n\nA ProjTransformFactory (see PROJ transforms) rather than a built transform: it is called once per task, so each task holds a PROJ transformation on a context it alone uses. A context is not safe to share between threads.\n\nWhere the grid and the imagery share a CRS, pass IdentityTransform instead — it is exact by construction and skips PROJ altogether, which is around 50 times faster per point.","title":"Larger grids"},{"category":"section","location":"#Approximating-the-projection","page":"Home","text":"Where the CRSs differ, PROJ is most of the run: three calls per grid point, at roughly 300 ns each. InterpolatedTransform tabulates the transform on a coarse lattice and interpolates between the nodes, with a selectable kernel — Bilinear, Bicubic or NearestNode.\n\ntf = InterpolatedTransform(ProjTransformFactory(3413, 32624), grid, pair;\n                           lattice = 4, mode = :hybrid, interpolation = Bilinear(),\n                           window = window)\nresult = pairgeometry_blocked(grid, pair, source; transform = tf, window = window, ntasks = 8)\n\nlattice is the node spacing as a multiple of the grid spacing. The two modes differ in which bands they leave exact:\n\n:hybrid interpolates only the two inverse calls. The pixel-location bands come from the forward transform alone, so they stay bitwise identical to the exact path. Saturates around 1.6×, since the one remaining call is the floor.\n:full interpolates both directions, for up to 6.9×. The location bands can then differ by one pixel — the positional error is far below a pixel, but it can move a point across a rounding boundary.\n\nIt is an AbstractTransformFactory, so a blocked run calls it once per task and each task owns its own lattice. The result does not depend on the block size: the lattice is built from the whole window's bounds.\n\nThe exact path is the default. docs/interpolated-transform.md records the measured cost and accuracy of each mode, spacing and band.","title":"Approximating the projection"},{"category":"section","location":"#API","page":"Home","text":"Everything exported, plus the internals the reference-fidelity notes refer to. Grouped by the file each is defined in, which follows the layering: vector and rounding primitives, then coordinates and transforms, then the grid, then the kernel, then the driver.","title":"API"},{"category":"section","location":"#PROJ-transforms","page":"Home","text":"Available when Proj is loaded.\n\nAn extension module has no name bound in any module a @autodocs block can name, so it is fetched the same way any caller would: Base.get_extension.","title":"PROJ transforms"},{"category":"section","location":"#Raster-IO","page":"Home","text":"Available when Rasters, ArchGDAL, DimensionalData and DiskArrays are loaded. Reads inputs a window at a time from disk-backed rasters, and writes the reference's nine GeoTIFFs.","title":"Raster IO"},{"category":"section","location":"#Correlator-handoff","page":"Home","text":"Available when AutoRIFT is loaded, which adds AutoRIFT.pointset(::PairGeometry) and velocity_conversion(::PairGeometry). The first converts the search grid — reindexing to one-based, and turning a nodata point into the zero search radius AutoRIFT skips on. The second returns what a PointSet has no fields for and downstream velocity conversion still needs: the displacement-to-velocity operator, the scale factors, the mask, and the chip-size aspect ratio.\n\nAutoRIFT is unregistered, so it is a weak dependency with no resolvable version and is absent from both the test and docs environments — these functions are neither exercised by CI nor documented below. Add a checkout to run them:\n\nimport Pkg\nPkg.develop(path = \"path/to/AutoRIFT.jl\")   # in test/ to run the handoff tests\n\nIt must be a revision whose pointset accepts chip_size_min_x/chip_size_max_x; the per-point chip-size bounds have nowhere else to go, and no released version has them.","title":"Correlator handoff"},{"category":"section","location":"#Index","page":"Home","text":"","title":"Index"},{"category":"module","location":"#ImagePairGeometry.ImagePairGeometry","page":"Home","text":"ImagePairGeometry\n\nGeometry of a co-registered image pair on a map-projected grid.\n\nFor each point of a grid defined in projected coordinates (northing/easting), the geometry of a pair of images relates map coordinates to image coordinates in both directions:\n\nforward: grid point (x, y, z) to pixel index in each image of the pair,\ninverse: a pixel displacement between the two images to a velocity in map coordinates.\n\nDerived per grid point alongside that mapping: the expected displacement implied by a reference velocity field, the search extent in pixels implied by a search-range field, chip size converted from meters to pixels, and the scale factors relating image-space distance to ground distance.\n\nRadar and Cartesian (e.g. optical) imagery differ in how the forward mapping is obtained — orbit state vectors and a DEM for the former, map projection information for the latter — and in the unit vectors that define the inverse operator.\n\nThe pixel displacement itself is estimated elsewhere, by a feature-tracking or correlation routine; this package supplies the geometry that routine searches within and the operator that converts its result to velocity.\n\nREFERENCE.md records the reference implementation and every deliberate divergence from it.\n\n\n\n\n\n","title":"ImagePairGeometry.ImagePairGeometry"},{"category":"type","location":"#ImagePairGeometry.AbstractCoordTransform","page":"Home","text":"AbstractCoordTransform\n\nA transform between coordinate systems, callable as t(x, y, z) -> (x′, y′, z′).\n\nSubtypes: IdentityTransform, AffineTransform. A Proj.Transformation also satisfies the interface and is what a georeferenced caller passes; it is not a subtype because it belongs to another package.\n\n\n\n\n\n","title":"ImagePairGeometry.AbstractCoordTransform"},{"category":"type","location":"#ImagePairGeometry.AbstractImageCoordinate","page":"Home","text":"AbstractImageCoordinate\n\nThe coordinate system of an image, determining how a map-projected grid point maps to a pixel index in it.\n\nSubtypes: ProjectedCoordinate, RadarCoordinate.\n\nNothing dispatches on this yet: every kernel signature names ProjectedCoordinate, because that is the only implemented path. It is a place to hang the radar path from, not a generalization the current code relies on — and whether it is the right parent is a question the radar geometry gets to answer, since a radar acquisition is not described by an origin, a spacing and a size.\n\n\n\n\n\n","title":"ImagePairGeometry.AbstractImageCoordinate"},{"category":"type","location":"#ImagePairGeometry.AbstractInputSource","page":"Home","text":"AbstractInputSource\n\nA source of the per-block input rasters.\n\nImplement readblock for a new source. InMemoryInputs views arrays already held; the Rasters extension adds a source that reads windows from disk, so a grid larger than memory never has to be materialized.\n\n\n\n\n\n","title":"ImagePairGeometry.AbstractInputSource"},{"category":"type","location":"#ImagePairGeometry.AbstractTransformFactory","page":"Home","text":"AbstractTransformFactory\n\nBuilds a fresh TransformPair each time it is called, as factory().\n\nA blocked run calls it once per task, so each task owns a transform for its lifetime. That is what Proj needs: a Proj.Transformation wraps a PJ* on a context that PROJ documents as usable from one thread at a time, and building two concurrently on the shared global context corrupts its SQLite handle. The Proj extension's ProjTransformFactory is the implementation, and InterpolatedTransform is one that returns a lattice approximation.\n\n\n\n\n\n","title":"ImagePairGeometry.AbstractTransformFactory"},{"category":"type","location":"#ImagePairGeometry.AffineTransform","page":"Home","text":"AffineTransform(; a, b, c, d, e, f, scale_z = 1.0)\n\nThe planar affine map (x, y) -> (a*x + b*y + c, d*x + e*y + f), with z scaled by scale_z.\n\nExists for testing: a rotation, scaling and translation has an exactly known inverse and exactly known axis unit vectors, so it pins the kernel's arithmetic without reference to PROJ or to the reference implementation. Not a substitute for a map projection — it cannot represent one.\n\n\n\n\n\n","title":"ImagePairGeometry.AffineTransform"},{"category":"type","location":"#ImagePairGeometry.Bicubic","page":"Home","text":"Bicubic()\n\nCubic convolution from the sixteen nodes surrounding the point, with the Catmull-Rom parameter.\n\nReproduces an affine transform exactly, as Bilinear does, and a quadratic one besides, so it holds a given accuracy at a coarser lattice — fewer nodes to build, which is where the projection calls are. The per-point arithmetic is roughly twice Bilinear's, and it needs a two-node margin around the queried region rather than one.\n\n\n\n\n\n","title":"ImagePairGeometry.Bicubic"},{"category":"type","location":"#ImagePairGeometry.Bilinear","page":"Home","text":"Bilinear()\n\nLinear interpolation in each direction from the four nodes surrounding the point.\n\nExact for a transform that is affine over a lattice cell, which is what makes the error fall as the square of the lattice spacing. What gdalwarp uses, and what the measurements in docs/interpolated-transform.md were taken with.\n\n\n\n\n\n","title":"ImagePairGeometry.Bilinear"},{"category":"type","location":"#ImagePairGeometry.CoordLattice","page":"Home","text":"CoordLattice\n\nA coordinate transform tabulated on a regular lattice and interpolated between its nodes.\n\nCallable as (x, y, z) -> (x′, y′, z′), so it stands in for a transform anywhere one is taken — including inside a TransformPair, which is how InterpolatedTransform uses it.\n\nFields\n\nx, y: the transformed coordinates at each node, (nx, ny, nlevels). The trailing axis is the elevations in zrange: two where the transform's horizontal result depends on elevation, one where it does not.\norigin: the (x, y) input coordinate of node (1, 1).\nspacing: node spacing in input coordinates, positive in both directions.\nzrange: the elevations bounding what the lattice was tabulated over.\ninterpolation: the LatticeInterpolation used between nodes.\nflat: whether a query can read one level and ignore elevation.\n\nInterpolating linearly between two elevations is exact rather than approximate: elevation enters a horizontal coordinate only through a datum shift, which is linear in it. For two CRSs on one datum — every ITSLIVE projection — there is no such shift, so [`buildlattice`](@ref) tabulates one level instead of two and a query reads it directly.\n\nA query outside the lattice throws. Clamping instead would turn a bounds error into a plausible coordinate, which is exactly the failure this package's bitwise tests exist to catch. A NaN coordinate is not a bounds error and passes through as NaN, matching the exact path: the reference reaches a NaN pixel index from a NaN elevation and reports it in bounds (see REFERENCE.md).\n\n\n\n\n\n","title":"ImagePairGeometry.CoordLattice"},{"category":"type","location":"#ImagePairGeometry.CoregisteredPair","page":"Home","text":"CoregisteredPair\n\nThe overlap of two co-registered images, and the offsets into each.\n\nFields\n\ncoordinate: the overlap as a ProjectedCoordinate. Pixel indices the geometry produces are relative to this, not to either input image.\nreference_offset, secondary_offset: the (column, row) index of the overlap's first pixel within each input image, zero-based as the reference reports them. A caller reads its image windows at these offsets.\ndt: time separation in seconds, secondary minus reference. Enters the geometry as the factor converting velocity to displacement.\n\nBuilt by coregister.\n\n\n\n\n\n","title":"ImagePairGeometry.CoregisteredPair"},{"category":"type","location":"#ImagePairGeometry.GeometryInputs","page":"Home","text":"GeometryInputs(; dem, dhdx = nothing, dhdy = nothing, vx = nothing, vy = nothing,\n                 srx = nothing, sry = nothing, csminx = nothing, csminy = nothing,\n                 csmaxx = nothing, csmaxy = nothing, ssm = nothing)\n\nThe per-grid-point input rasters, covering the window being computed.\n\nOnly dem is required. Each other quantity is nothing when not supplied, and the outputs depending on it are then left as nodata — matching the reference, which writes no file at all for those bands.\n\nDependencies among them, as the reference enforces them:\n\nSlope (dhdx, dhdy) gates the displacement-to-velocity operator, the scale factors, the expected offset and the search extent. Without it the reference writes none of them, since all four need the surface normal.\nVelocity (vx, vy) gates the expected offset; search range (srx, sry) gates the search extent. Both additionally require slope.\nChip size and the stable-surface mask are independent of slope.\n\nAll arrays must share axes with each other and with the window.\n\n\n\n\n\n","title":"ImagePairGeometry.GeometryInputs"},{"category":"type","location":"#ImagePairGeometry.GeometryParams","page":"Home","text":"GeometryParams(; chip_size_0 = 240.0, scaling = SearchRangeScaling())\n\nScalar parameters of the computation.\n\nchip_size_0: reference chip size in meters, 240.0 in production (testGeogrid.py:358).\nscaling: the search-range inflation, see SearchRangeScaling.\n\n\n\n\n\n","title":"ImagePairGeometry.GeometryParams"},{"category":"type","location":"#ImagePairGeometry.IdentityTransform","page":"Home","text":"IdentityTransform()\n\nThe transform between a coordinate system and itself.\n\nReturns its argument unchanged, which is what PROJ's +proj=noop pipeline does for two identical CRSs — bit for bit, including the vertical coordinate. Dispatching on this type rather than calling PROJ makes the same-CRS case exact by construction and removes three transform calls per grid point.\n\n\n\n\n\n","title":"ImagePairGeometry.IdentityTransform"},{"category":"type","location":"#ImagePairGeometry.ImageFootprint","page":"Home","text":"ImageFootprint(; origin, spacing, size)\n\nWhere one image sits in projected coordinates: enough to intersect two images without reading pixels.\n\norigin is the projected coordinate of the first pixel's center and spacing is the signed pixel size, matching a GDAL geotransform's (gt[1], gt[4]) and (gt[2], gt[6]). See ProjectedCoordinate, whose fields have the same meaning.\n\n\n\n\n\n","title":"ImagePairGeometry.ImageFootprint"},{"category":"type","location":"#ImagePairGeometry.InMemoryInputs","page":"Home","text":"InMemoryInputs(inputs::GeometryInputs)\n\nAn input source viewing arrays already in memory, indexed relative to the full window.\n\nBlocking such a source bounds the working set rather than total memory — the arrays are already resident — which is what makes it the right thing to compare a blocked run against.\n\n\n\n\n\n","title":"ImagePairGeometry.InMemoryInputs"},{"category":"type","location":"#ImagePairGeometry.InterpolatedTransform","page":"Home","text":"InterpolatedTransform(transform, grid, pair; lattice = 4, mode = :hybrid,\n                      interpolation = Bilinear(), window = nothing,\n                      zrange = DEFAULT_ZRANGE)\n\nA transform factory that approximates transform on a coarse lattice.\n\nPass it as pairgeometry's or pairgeometry_blocked's transform. It is an AbstractTransformFactory, so a blocked run calls it once per task and each task owns its own lattice and its own PROJ context for that task's lifetime — the same ownership the exact PROJ path needs, for the same reason.\n\ntransform is anything the exact path accepts: an AbstractCoordTransform, a TransformPair, or a factory such as ProjTransformFactory. grid and pair give the region the lattice must cover; window defaults to the whole grid intersected with the pair's footprint, and must be the same window the result is computed over.\n\nModes\n\n:hybrid — the forward transform stays exact and only the inverse is interpolated. Every integer band is bitwise identical to the exact path, because location_x/location_y come from the forward transform alone. Drops two of the three calls per point.\n:full — both directions are interpolated, so no call is made per point at all. Faster, but location_x/location_y can differ from the exact path by one pixel: the positional error is far below a pixel, and what shifts is which side of a rounding boundary a point falls on. Sound where a one-pixel difference in a search center is acceptable, which for a correlator searching tens of pixels it may be.\n\nlattice is the node spacing as a multiple of the grid spacing; 1 puts a node at every grid point. Coarser is faster to build and less accurate — docs/interpolated-transform.md tabulates the measured error against spacing. interpolation selects the kernel: Bilinear, Bicubic or NearestNode.\n\nBoth lattices are spaced by the same ground distance. The inverse lattice is tabulated over image coordinates and extended by one image pixel beyond the footprint, because the kernel queries it at the point stepped one pixel along each image axis.\n\ntf = InterpolatedTransform(ProjTransformFactory(3413, 32624), grid, pair;\n                           lattice = 4, mode = :hybrid, window = win)\nr = pairgeometry_blocked(grid, pair, source; transform = tf, window = win, ntasks = 8)\n\n\n\n\n\n","title":"ImagePairGeometry.InterpolatedTransform"},{"category":"type","location":"#ImagePairGeometry.LatticeInterpolation","page":"Home","text":"LatticeInterpolation\n\nHow a CoordLattice interpolates between its nodes.\n\nSubtypes: NearestNode, Bilinear, Bicubic. Chosen by dispatch rather than a stored flag, so the per-point arithmetic specializes with no branch.\n\n\n\n\n\n","title":"ImagePairGeometry.LatticeInterpolation"},{"category":"type","location":"#ImagePairGeometry.MapGrid","page":"Home","text":"MapGrid(; geotransform, size, crs = nothing)\n\nThe grid the geometry is computed on, defined by a DEM.\n\nFields\n\ngeotransform: GDAL's six coefficients (x₀, dx, rx, y₀, ry, dy), where (x₀, y₀) is the outer corner of the first pixel — not its center. Rotation terms rx/ry must be zero; the reference assumes a north-up grid throughout and its index arithmetic is wrong otherwise.\nsize: (ncolumns, nrows) of the DEM.\ncrs: the projection, or nothing. Carried for output metadata; the transform used in the computation is passed separately, so this is not consulted during the kernel.\n\nGrid point centers are at x₀ + (i + 0.5) * dx, matching the reference (geogridOptical.cpp:678-679).\n\n\n\n\n\n","title":"ImagePairGeometry.MapGrid"},{"category":"type","location":"#ImagePairGeometry.NearestNode","page":"Home","text":"NearestNode()\n\nTake the value at the nearest lattice node, with no interpolation.\n\nReproduces no transform beyond the nodes themselves, so it is not a candidate for production. It exists to bound an error budget from below: comparing it against Bilinear at the same spacing separates the error the lattice's coarseness carries from the error the interpolation adds.\n\nNamed for the node rather than as Nearest because AutoRIFT.Nearest is a distinct resampling method for displacement fields, and both are in scope wherever the AutoRIFT extension is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.NearestNode"},{"category":"type","location":"#ImagePairGeometry.NoDataPolicy","page":"Home","text":"NoDataPolicy(; input, output = -32767.0)\n\nHow missing values are recognized on input and written on output.\n\nFields\n\ninput: the value treated as missing in every input raster. The reference takes this from the DEM's first band and applies it to all of them, so it is one value rather than one per raster. A raster whose own sentinel differs is not fully masked; see REFERENCE.md.\noutput: written wherever an output cannot be computed. -32767.0 in production (testGeogrid.py:357), stored as Float64 and converted per output band.\n\nThe reference reads its input sentinel with GetNoDataValue(NULL), discarding the success flag, so a raster with no nodata set yields 0.0 and every genuine zero is then treated as missing. Constructing this type with input = 0.0 reproduces that; nodata_from makes the choice visible at the call site.\n\n\n\n\n\n","title":"ImagePairGeometry.NoDataPolicy"},{"category":"type","location":"#ImagePairGeometry.PairGeometry","page":"Home","text":"PairGeometry\n\nPer-grid-point geometry of an image pair, one array per output quantity.\n\nAll arrays share the axes of the grid window the result was computed over.\n\nInteger fields, sentinel -32767\n\nlocation_x, location_y: zero-based pixel index of the grid point in the image.\noffset_x, offset_y: expected displacement in pixels, from the reference velocity field.\nsearch_x, search_y: search half-extent in pixels.\nchip_min_x, chip_min_y, chip_max_x, chip_max_y: chip size bounds in pixels.\nstable_surface: stable-surface mask.\n\nFloat fields, sentinel -32767.0\n\noff2vx_dx, off2vx_dy, off2vy_dx, off2vy_dy: the operator converting a pixel displacement to a map velocity. Displacement (dx, dy) gives vx = off2vx_dx * dx + off2vx_dy * dy and likewise for vy, after scaling the displacement by the scale factors.\nscale_x, scale_y: ratio of true ground distance to nominal pixel spacing.\n\nMetadata\n\ngeotransform, crs: georeferencing of the window.\nwindow: the grid indices covered.\nnodata: the policy the sentinels come from.\n\nField names map to the reference's files as location → window_location.tif, offset → window_offset.tif, search → window_search_range.tif, chip_min/chip_max → window_chip_size_min.tif/_max.tif, stable_surface → window_stable_surface_mask.tif, off2vx/off2vy → window_rdr_off2vel_x_vec.tif/_y_vec.tif, scale → window_scale_factor.tif.\n\n\n\n\n\n","title":"ImagePairGeometry.PairGeometry"},{"category":"type","location":"#ImagePairGeometry.PointGeometry","page":"Home","text":"PointGeometry\n\nGeometry at one grid point that depends only on the grid, the image coordinate system and the projection — not on the image pair's time separation.\n\nFields\n\nimage_xy: the point in image coordinates, before conversion to a pixel index.\nxunit, yunit: unit vectors along the image's two axes, expressed in grid coordinates.\nxlen, ylen: length in grid coordinates of a one-pixel step along each image axis. The physical pixel size at this point, which differs from the nominal spacing wherever the projection distorts.\nnormal: upward surface normal from the slope rasters, or the zero vector where no slope is supplied.\n\n\n\n\n\n","title":"ImagePairGeometry.PointGeometry"},{"category":"type","location":"#ImagePairGeometry.ProjectedCoordinate","page":"Home","text":"ProjectedCoordinate(; origin, spacing, size)\n\nAn image on a map projection, indexed by an affine relation between projected coordinates and pixel indices.\n\nFields\n\norigin: projected coordinate of the first pixel's center, as (x, y). Geogrid's startingX/startingY.\nspacing: signed pixel size, as (x, y). Negative y for the usual north-up raster. Signed rather than absolute because the index calculation divides by it, so the sign carries the axis direction.\nsize: (samples, lines), i.e. (ncolumns, nrows).\n\nFor an image pair these describe the overlap of the two images, which is what coregister computes and what pixel indices are relative to. Constructing one from a single image's geotransform is correct only when that image is the overlap.\n\nThe CRS is deliberately absent: it enters through the coordinate transform passed to the kernel, so the same coordinate system can be paired with a transform from any grid CRS.\n\n\n\n\n\n","title":"ImagePairGeometry.ProjectedCoordinate"},{"category":"type","location":"#ImagePairGeometry.RadarCoordinate","page":"Home","text":"RadarCoordinate\n\nAn image in radar slant-range/azimuth coordinates.\n\nNot yet implemented: constructing one throws. The type exists so that the dispatch on AbstractImageCoordinate is fixed while only the projected path is available, and so that a caller reaching for radar gets a clear error rather than a MethodError on a type that does not exist.\n\n\n\n\n\n","title":"ImagePairGeometry.RadarCoordinate"},{"category":"type","location":"#ImagePairGeometry.SearchRangeScaling","page":"Home","text":"SearchRangeScaling(; dt_unity = 182.0, max_scale = 5.0, lower = 0.0, upper = 20000.0)\n\nParameters of the interval-dependent search-range inflation.\n\nFields\n\ndt_unity: interval in days at which the scale reaches 1, i.e. no inflation.\nmax_scale: scale approached as the interval goes to zero.\nlower, upper: bounds the scaled range is clamped to, in the same units as the input (m/yr).\n\nDefaults are the reference's (GeogridOptical.py:358-362: srs_dt_unity = 182, srs_max_scale = 5, srs_min_search = 0, srs_max_search = 20000).\n\n\n\n\n\n","title":"ImagePairGeometry.SearchRangeScaling"},{"category":"type","location":"#ImagePairGeometry.TransformPair","page":"Home","text":"TransformPair(forward, inverse)\n\nThe forward and inverse transforms between a grid CRS and an image CRS, held together.\n\nforward maps grid coordinates to image coordinates; inverse maps back. Construct from a single transform with transform_pair, which derives the inverse, or pass both explicitly when they come from a library that builds each separately.\n\n\n\n\n\n","title":"ImagePairGeometry.TransformPair"},{"category":"constant","location":"#ImagePairGeometry.DEFAULT_BLOCKSIZE","page":"Home","text":"DEFAULT_BLOCKSIZE\n\nBlock size in grid points when none is given: (512, 512).\n\n262144 points is a few megabytes of input per raster — small enough to bound memory on a continental grid, large enough that per-block overhead (a PROJ pipeline lookup, a task claim) is negligible against the per-point work.\n\n\n\n\n\n","title":"ImagePairGeometry.DEFAULT_BLOCKSIZE"},{"category":"constant","location":"#ImagePairGeometry.DEFAULT_ZRANGE","page":"Home","text":"DEFAULT_ZRANGE\n\nElevation range, in meters, bounding an image footprint where the true terrain height is unknown: (-200.0, 4000.0), the reference's default (GeogridOptical.py:115).\n\nThe footprint is computed at both extremes and the union taken, so the bounding box is conservative with respect to terrain.\n\nElevation changes a horizontal coordinate only when the transform crosses datums, since only then does the pipeline apply a 3D Helmert shift. Between two CRSs on one datum the horizontal result is identical at any height, so for imagery and grids on WGS84 — UTM, EPSG:3413, EPSG:3031, hence every ITS_LIVE projection — this range widens nothing.\n\n\n\n\n\n","title":"ImagePairGeometry.DEFAULT_ZRANGE"},{"category":"constant","location":"#ImagePairGeometry.DEG2RAD","page":"Home","text":"DEG2RAD\n\nRadians per degree, as the reference computes it: M_PI / 180.0 (geogridOptical.cpp:39).\n\n\n\n\n\n","title":"ImagePairGeometry.DEG2RAD"},{"category":"constant","location":"#ImagePairGeometry.FLOAT_BANDS","page":"Home","text":"FLOAT_BANDS\n\nNames of the Float64 fields of PairGeometry, in the reference's band order.\n\n\n\n\n\n","title":"ImagePairGeometry.FLOAT_BANDS"},{"category":"constant","location":"#ImagePairGeometry.INT_BANDS","page":"Home","text":"INT_BANDS\n\nNames of the Int32 fields of PairGeometry, in the reference's band order.\n\n\n\n\n\n","title":"ImagePairGeometry.INT_BANDS"},{"category":"constant","location":"#ImagePairGeometry.LATTICE_MODES","page":"Home","text":"LATTICE_MODES\n\nThe modes InterpolatedTransform accepts: (:hybrid, :full).\n\n\n\n\n\n","title":"ImagePairGeometry.LATTICE_MODES"},{"category":"constant","location":"#ImagePairGeometry.NODATA_INT32","page":"Home","text":"NODATA_INT32\n\nThe output sentinel as it appears in the integer bands: Int32(-32767).\n\n\n\n\n\n","title":"ImagePairGeometry.NODATA_INT32"},{"category":"constant","location":"#ImagePairGeometry.NO_NORMAL","page":"Home","text":"NO_NORMAL\n\nThe zero vector, standing for \"no slope raster supplied\".\n\nThe reference sets the normal to all zeros in that case (geogridOptical.cpp:761-764) rather than skipping the computations that use it, so the velocity closure divides by zero and the outputs that depend on it are Inf or NaN. Those outputs are not written at all without a slope raster, so the values never reach a file — but the arithmetic runs, and reproducing it keeps the code paths aligned.\n\n\n\n\n\n","title":"ImagePairGeometry.NO_NORMAL"},{"category":"constant","location":"#ImagePairGeometry.REFERENCE_FILES","page":"Home","text":"REFERENCE_FILES\n\nEach of the reference's nine output files, with the PairGeometry fields holding its bands in order.\n\nThe basis for writing GeoTIFFs that a downstream reader consuming the reference's output can read unchanged, and for comparing band by band against it.\n\n\n\n\n\n","title":"ImagePairGeometry.REFERENCE_FILES"},{"category":"constant","location":"#ImagePairGeometry.SECONDS_PER_YEAR_TERMS","page":"Home","text":"SECONDS_PER_YEAR_TERMS\n\nThe reference's year length as the three factors it divides by in sequence: (365.0, 24.0, 3600.0).\n\nTheir product, 31536000, is exact in Float64, but x / 365.0 / 24.0 / 3600.0 and x / 31_536_000.0 still differ in the last bit for most x because each division rounds. Kept as separate factors so the division order matches.\n\n\n\n\n\n","title":"ImagePairGeometry.SECONDS_PER_YEAR_TERMS"},{"category":"method","location":"#ImagePairGeometry.allocate_geometry-Tuple{CartesianIndices{2, R} where R<:Tuple{OrdinalRange{Int64, Int64}, OrdinalRange{Int64, Int64}}, NTuple{6, Float64}, Any, NoDataPolicy}","page":"Home","text":"allocate_geometry(window, geotransform, crs, nodata) -> PairGeometry\n\nA PairGeometry sized to window, with every band filled with its sentinel.\n\nPrefilling means a band the inputs do not support — the reference writes no file at all in that case — is uniformly nodata rather than uninitialized, and a point skipped for being out of bounds needs no explicit write.\n\n\n\n\n\n","title":"ImagePairGeometry.allocate_geometry"},{"category":"method","location":"#ImagePairGeometry.block_ranges-Tuple{CartesianIndices{2, R} where R<:Tuple{OrdinalRange{Int64, Int64}, OrdinalRange{Int64, Int64}}, Tuple{Int64, Int64}}","page":"Home","text":"block_ranges(window, blocksize) -> Vector{CartesianIndices{2}}\n\nwindow partitioned into blocks of at most blocksize, in column-major order.\n\nBlocks tile the window exactly: no overlap, no halo, and the union is the window. The final block in each direction is short where the window does not divide evenly.\n\n\n\n\n\n","title":"ImagePairGeometry.block_ranges"},{"category":"function","location":"#ImagePairGeometry.blocksize_from_chunks","page":"Home","text":"blocksize_from_chunks(source; floor = 256) -> NTuple{2,Int}\n\nA block size aligned to a raster source's own chunking. Defined when Rasters is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.blocksize_from_chunks"},{"category":"method","location":"#ImagePairGeometry.build_lattice-Tuple{Any, Extent, Tuple{Real, Real}, LatticeInterpolation}","page":"Home","text":"build_lattice(t, bounds::Extent, spacing, interpolation;\n              zrange = DEFAULT_ZRANGE) -> CoordLattice\n\nTabulate the transform t on a lattice of the given spacing covering bounds.\n\nt is called as t(x, y, z) -> (x′, y′, z′). bounds is the region the lattice must be able to answer queries in, as an Extents.Extent{(:X, :Y)} in t's input coordinates; the lattice extends a kernel-dependent margin beyond it so the interpolation stencil is complete at the boundary. spacing is a positive (dx, dy) in input coordinates.\n\nThis is the only place the underlying transform is called. A non-finite node means the transform failed over the region and is thrown rather than interpolated, since it would otherwise spread NaN across every cell touching that node.\n\nOne elevation level is tabulated where the transform's horizontal result does not depend on elevation, two where it does, so the cost is nx * ny calls rather than 2 * nx * ny for a same-datum pair. Which case holds is established by probing the two zrange extremes at the lattice's own corners and center: elevation enters a horizontal coordinate only through a datum shift, and a pipeline either applies one everywhere over a region this size or nowhere.\n\n\n\n\n\n","title":"ImagePairGeometry.build_lattice"},{"category":"method","location":"#ImagePairGeometry.chip_pixels-Tuple{Float64, Float64, Float64, Int64, Int64}","page":"Home","text":"chip_pixels(csx, csy, chip_size_0, pix_x, pix_y) -> NTuple{2,Float64}\n\nA chip size in meters converted to pixels along each axis.\n\nMatches geogridOptical.cpp:874-875: scaled by the ratio of the reference chip size to its pixel equivalent. The caller converts with ctrunc32 — the reference truncates here rather than rounding, unlike every other integer output.\n\npix_x/pix_y come from chip_size_pixels.\n\n\n\n\n\n","title":"ImagePairGeometry.chip_pixels"},{"category":"method","location":"#ImagePairGeometry.chip_size_pixels-Tuple{Real, Real}","page":"Home","text":"chip_size_pixels(chip_size_0, res) -> Int\n\nThe reference chip size in pixels for a pixel size of res, rounded up to a multiple of four.\n\nMatches geogridOptical.cpp:513-514: ceil(chip_size_0 / res / 4) * 4. The multiple of four is what the correlator's chip pyramid requires.\n\njulia> using ImagePairGeometry: chip_size_pixels\n\njulia> chip_size_pixels(240.0, 30.0)\n8\n\njulia> chip_size_pixels(240.0, 10.0)\n24\n\njulia> chip_size_pixels(240.0, 15.0)\n16\n\n\n\n\n\n","title":"ImagePairGeometry.chip_size_pixels"},{"category":"method","location":"#ImagePairGeometry.close_slope_parallel-Tuple{Float64, Float64, StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"close_slope_parallel(vx, vy, normal) -> SVector{3,Float64}\n\nA horizontal velocity completed to three components by requiring flow parallel to the surface.\n\nThe vertical component follows from v · n = 0:\n\nvz = -(vx * nx + vy * ny) / nz\n\nmatching geogridOptical.cpp:766-773. Applied to both the reference velocity and the search range, since a search extent is a velocity bound and must lie in the same plane.\n\nNo guard on nz: with a slope raster the normal's z is positive by construction, and without one the normal is the zero vector and the result is non-finite — which is the reference's behavior, and those outputs are not written in that configuration.\n\n\n\n\n\n","title":"ImagePairGeometry.close_slope_parallel"},{"category":"method","location":"#ImagePairGeometry.coregister-Tuple{ImageFootprint, ImageFootprint}","page":"Home","text":"coregister(reference::ImageFootprint, secondary::ImageFootprint; dt) -> CoregisteredPair\n\nIntersect two image footprints, giving the overlap's coordinate system and each image's offset into it.\n\nReproduces GeogridOptical.coregister (GeogridOptical.py:277-345) including its four failure conditions, which are errors rather than clamps: the reference refuses a pair it cannot align instead of silently processing a partial overlap.\n\nThe overlap is W = max(x₁, x₂), N = min(y₁, y₂), E = min(east₁, east₂), S = max(south₁, south₂), where each image's far edge is its last pixel center (origin + (size - 1) * spacing) rather than its outer boundary. Index arithmetic uses nround — half to even — because the reference computes it in NumPy, not in the C++ kernel where std::round applies.\n\ndt is seconds, secondary minus reference. The reference derives it from calendar dates only (testGeogrid.py:351-354), so it is always a whole number of days there; nothing here requires that.\n\nThrows ArgumentError if the two spacings differ, if the overlap is empty, or if any of the reference's four conditions fail.\n\nExample\n\njulia> using ImagePairGeometry\n\njulia> a = ImageFootprint(origin = (100.0, 900.0), spacing = (10.0, -10.0), size = (50, 50));\n\njulia> b = ImageFootprint(origin = (150.0, 950.0), spacing = (10.0, -10.0), size = (50, 50));\n\njulia> p = coregister(a, b; dt = 86400.0);\n\njulia> p.coordinate.origin, p.coordinate.size\n((150.0, 900.0), (45, 45))\n\njulia> p.reference_offset, p.secondary_offset\n((5, 0), (0, 5))\n\n\n\n\n\n","title":"ImagePairGeometry.coregister"},{"category":"method","location":"#ImagePairGeometry.cross3-Tuple{StaticArraysCore.SVector{3, Float64}, StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"cross3(u, v) -> SVector{3,Float64}\n\nCross product, matching cross_C (geogridOptical.cpp:1014-1018).\n\n\n\n\n\n","title":"ImagePairGeometry.cross3"},{"category":"method","location":"#ImagePairGeometry.cross_check-Tuple{ImagePairGeometry.PointGeometry}","page":"Home","text":"cross_check(g::PointGeometry) -> Float64\n\nDegrees by which the surface normal deviates from perpendicular to the plane of the image axes.\n\nMatches geogridOptical.cpp:812-814: the angle between the normal and the normalized cross product of the axis unit vectors, less 90°, in absolute value. The reference computes the displacement-to-velocity operator only where this exceeds 1°, since below that the two-by-two system is near singular and its inverse is dominated by rounding.\n\nFor realistic terrain this sits at 82–90°, far above the threshold; it approaches it only for near-vertical surfaces.\n\n\n\n\n\n","title":"ImagePairGeometry.cross_check"},{"category":"method","location":"#ImagePairGeometry.cround-Tuple{Float64}","page":"Home","text":"cround(x::Float64) -> Float64\n\nRound half away from zero, as C's std::round does: cround(0.5) == 1.0, cround(-0.5) == -1.0.\n\nJulia's round rounds half to even, so it returns 0.0 for both. Use this wherever the reference calls std::round, and nround wherever it calls np.round.\n\n\n\n\n\n","title":"ImagePairGeometry.cround"},{"category":"method","location":"#ImagePairGeometry.cround32-Tuple{Float64}","page":"Home","text":"cround32(x::Float64) -> Int32\n\nstd::round followed by C's implicit conversion to GInt32, as in\n\nraster11[jj] = std::round(...);   // GInt32 = double\n\nBecause cround has already produced an integral value the subsequent truncation is the identity, so this is a rounding conversion — unlike ctrunc32.\n\nThrows InexactError if the result is not representable as Int32. The reference instead relies on undefined behavior, which clang implements on arm64 as saturation; failing loudly is the deliberate divergence, recorded in REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.cround32"},{"category":"method","location":"#ImagePairGeometry.ctrunc32-Tuple{Float64}","page":"Home","text":"ctrunc32(x::Float64) -> Int32\n\nC's implicit double to GInt32 conversion with no rounding applied first: truncation toward zero, so ctrunc32(22.99) == 22.\n\nThe reference uses this — not rounding — for chip size and the stable-surface mask (geogridOptical.cpp:874-875, 884-885, 893), while applying std::round first for the offset and search-range outputs (:807-808, 849-863). Substituting a rounding conversion here shifts chip sizes by one pixel.\n\nThrows InexactError outside the Int32 range, as cround32 does and for the same reason.\n\n\n\n\n\n","title":"ImagePairGeometry.ctrunc32"},{"category":"method","location":"#ImagePairGeometry.dot3-Tuple{StaticArraysCore.SVector{3, Float64}, StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"dot3(v, w) -> Float64\n\nDot product summed left to right, matching dot_C (geogridOptical.cpp:1008-1012).\n\nNot LinearAlgebra.dot, whose reduction order is unspecified and which may contract to an fma.\n\n\n\n\n\n","title":"ImagePairGeometry.dot3"},{"category":"method","location":"#ImagePairGeometry.footprint_bounds-Tuple{Any, ProjectedCoordinate}","page":"Home","text":"footprint_bounds(transform, c::ProjectedCoordinate; zrange = DEFAULT_ZRANGE) -> Extent\n\nBounding box, in grid coordinates, of the image described by c.\n\ntransform maps image coordinates to grid coordinates and is called as transform(x, y, z) -> (x′, y′, z′). The image's four corners are transformed at both elevations in zrange — eight points — and the box is their min/max, reproducing GeogridOptical.determineBbox (GeogridOptical.py:115-155).\n\nCorners are pixel centers of the first and last pixel (origin + (size - 1) * spacing), not the outer boundary, matching the reference.\n\nReturns an Extents.Extent{(:X, :Y)}. The elevation range is not part of the result: it only widens the horizontal box.\n\nGiven a TransformPair this takes the correct direction itself. Note that it is the pair's inverse — the kernel's forward direction is grid to image, and this needs image to grid — so passing tf.forward by hand computes a bounding box in the wrong space, which surfaces as a window far outside the grid.\n\n\n\n\n\n","title":"ImagePairGeometry.footprint_bounds"},{"category":"method","location":"#ImagePairGeometry.grid_window-Tuple{MapGrid, Extent}","page":"Home","text":"grid_window(g::MapGrid, bounds::Extent) -> CartesianIndices{2}\n\nThe window of grid points overlapping bounds, as one-based CartesianIndices.\n\nReproduces geogridOptical.cpp:239-244:\n\nlOff   = std::max(std::floor((ymax - geoTrans[3]) / geoTrans[5]), 0.);\nlCount = std::min(std::ceil((ymin - geoTrans[3]) / geoTrans[5]), demYSize - 1.) - lOff;\n\nincluding the demXSize - 1. / demYSize - 1. clamp, which excludes the grid's last column and row from ever being processed. That is reproduced rather than corrected — the reference's outputs are one pixel short of the DEM extent and matching it is the point.\n\nThe reference computes these as double and truncates to int, and can produce a negative count for a pair that does not overlap the grid, which it then hands to GDAL as a raster size. This throws instead; see REFERENCE.md.\n\nCartesianIndices rather than an offset/count pair so the result composes with array views, DiskArrays chunk iteration, and blocking without conversion. The reference's zero-based pOff/lOff are recoverable as first(window).I .- 1.\n\n\n\n\n\n","title":"ImagePairGeometry.grid_window"},{"category":"method","location":"#ImagePairGeometry.gridorigin-Tuple{MapGrid}","page":"Home","text":"gridorigin(g::MapGrid)\n\nOuter corner (x₀, y₀) of the grid's first pixel. Point centers are half a pixel inside this.\n\n\n\n\n\n","title":"ImagePairGeometry.gridorigin"},{"category":"method","location":"#ImagePairGeometry.gridpoint_center-Tuple{MapGrid, Integer, Integer}","page":"Home","text":"gridpoint_center(g::MapGrid, i::Integer, j::Integer) -> NTuple{2}\n\nProjected coordinate of the center of grid point (i, j), one-based.\n\nThe half-pixel offset matches the reference (geogridOptical.cpp:678-679): geometry is evaluated at pixel centers, not corners.\n\n\n\n\n\n","title":"ImagePairGeometry.gridpoint_center"},{"category":"method","location":"#ImagePairGeometry.gridspacing-Tuple{MapGrid}","page":"Home","text":"gridspacing(g::MapGrid)\n\nSigned grid spacing (dx, dy). Geogrid reports dx as gridSpacingX in its scalar output, where downstream code uses it to relate chip size to grid spacing.\n\n\n\n\n\n","title":"ImagePairGeometry.gridspacing"},{"category":"function","location":"#ImagePairGeometry.image_footprint","page":"Home","text":"image_footprint(image) -> ImageFootprint\n\nAn ImageFootprint describing where a raster sits. Defined when Rasters is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.image_footprint"},{"category":"method","location":"#ImagePairGeometry.inbounds-Tuple{Float64, Float64, ProjectedCoordinate}","page":"Home","text":"inbounds(xind, yind, c::ProjectedCoordinate) -> Bool\n\nWhether a zero-based pixel index falls inside the image.\n\nMatches geogridOptical.cpp:775, which tests xind > nPixels - 1 | xind < 0 on the pre-conversion Float64. A NaN index — reachable when the transform is fed a NaN elevation — compares false in every direction and so is reported in bounds, as the reference reports it. See REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.inbounds"},{"category":"method","location":"#ImagePairGeometry.inverse-Tuple{IdentityTransform}","page":"Home","text":"inverse(t::AbstractCoordTransform)\n\nThe transform in the opposite direction.\n\nThe kernel needs both directions — forward to find a pixel index, inverse to find where a one-pixel step in the image lands on the grid — and takes them as one object so a caller cannot supply a mismatched pair.\n\n\n\n\n\n","title":"ImagePairGeometry.inverse"},{"category":"method","location":"#ImagePairGeometry.isidentity-Tuple{IdentityTransform}","page":"Home","text":"isidentity(t) -> Bool\n\nWhether t is the identity.\n\nA predicate for inspecting a transform, not the mechanism behind the fast path: that is chosen by dispatch on IdentityTransform, so nothing in the kernel branches on this at runtime. A Proj.Transformation between two identical CRSs is a no-op pipeline but reports false, since only the type is examined.\n\n\n\n\n\n","title":"ImagePairGeometry.isidentity"},{"category":"method","location":"#ImagePairGeometry.ismissingval-Tuple{NoDataPolicy, Real}","page":"Home","text":"ismissingval(p::NoDataPolicy, x) -> Bool\n\nWhether x is missing: equal to the sentinel, or NaN.\n\nEquality is what the reference tests, and NaN is equal to nothing — but the ITS_LIVE velocity rasters store NaN while declaring a nodata of -32767, so on real data the reference's test never fires there and NaN reaches its arithmetic. std::round(NaN) converted to GInt32 is undefined behavior, which arm64 clang resolves to 0 — the same value the reference writes from its own missing-velocity branch (geogridOptical.cpp:800-805). Treating NaN as missing therefore agrees with the reference bit for bit on the outputs while reaching it by a defined route.\n\n\n\n\n\n","title":"ImagePairGeometry.ismissingval"},{"category":"method","location":"#ImagePairGeometry.latticesize-Tuple{CoordLattice}","page":"Home","text":"latticesize(L::CoordLattice) -> NTuple{2,Int}\n\nNumber of nodes in each direction.\n\n\n\n\n\n","title":"ImagePairGeometry.latticesize"},{"category":"function","location":"#ImagePairGeometry.mapgrid","page":"Home","text":"mapgrid(dem) -> MapGrid\n\nA MapGrid describing a raster's grid. Defined when Rasters is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.mapgrid"},{"category":"method","location":"#ImagePairGeometry.nodata_from-Tuple{Real}","page":"Home","text":"nodata_from(missingval; output = -32767.0) -> NoDataPolicy\n\nA NoDataPolicy from a raster's declared missing value, falling back to 0.0 when there is none.\n\nThe fallback reproduces the reference: it reads the DEM's nodata without checking whether one is set, so an absent sentinel arrives as 0.0 and every zero-valued input pixel is read as missing. Passing nothing here makes that consequence explicit rather than incidental.\n\n\n\n\n\n","title":"ImagePairGeometry.nodata_from"},{"category":"method","location":"#ImagePairGeometry.norm3-Tuple{StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"norm3(v) -> Float64\n\nEuclidean norm as sqrt of the left-to-right sum of squares, matching norm_C (geogridOptical.cpp:1020-1024).\n\nUnlike LinearAlgebra.norm this does no scaling to avoid overflow. The reference does none either, and the inputs here are map coordinates and unit-scaled differences, orders of magnitude away from overflowing.\n\n\n\n\n\n","title":"ImagePairGeometry.norm3"},{"category":"method","location":"#ImagePairGeometry.npoints-Tuple{PairGeometry}","page":"Home","text":"npoints(r::PairGeometry) -> Int\n\nNumber of grid points in the result, valid or not.\n\n\n\n\n\n","title":"ImagePairGeometry.npoints"},{"category":"method","location":"#ImagePairGeometry.nround-Tuple{Float64}","page":"Home","text":"nround(x::Float64) -> Float64\n\nRound half to even, as NumPy's np.round does. An alias for Julia's round, spelled out so that call sites reproducing Python arithmetic say which reference they follow.\n\nThe reference's coregister uses np.round; its C++ kernel uses std::round. See cround.\n\n\n\n\n\n","title":"ImagePairGeometry.nround"},{"category":"method","location":"#ImagePairGeometry.nsamples-Tuple{ProjectedCoordinate}","page":"Home","text":"nsamples(c::ProjectedCoordinate)\nnlines(c::ProjectedCoordinate)\n\nImage width and height in pixels.\n\n\n\n\n\n","title":"ImagePairGeometry.nsamples"},{"category":"method","location":"#ImagePairGeometry.nvalid-Tuple{PairGeometry}","page":"Home","text":"nvalid(r::PairGeometry) -> Int\n\nNumber of grid points that mapped inside the image, i.e. whose location is not the sentinel.\n\n\n\n\n\n","title":"ImagePairGeometry.nvalid"},{"category":"method","location":"#ImagePairGeometry.offset_to_velocity-Tuple{ImagePairGeometry.PointGeometry, ProjectedCoordinate, Float64}","page":"Home","text":"offset_to_velocity(g::PointGeometry, c::ProjectedCoordinate, dt)\n    -> NTuple{4,Float64}\n\nThe two-by-two operator converting a pixel displacement to a map velocity, as (vx_from_dx, vx_from_dy, vy_from_dx, vy_from_dy).\n\nGiven a displacement (dx, dy) in pixels, the map velocity is\n\nvx = vx_from_dx * dx + vx_from_dy * dy\nvy = vy_from_dx * dx + vy_from_dy * dy\n\nwhich is how the downstream correlator reconstructs velocity, after scaling the displacement by scale_factors.\n\nMatches geogridOptical.cpp:817-832. The operator inverts the two-by-two system relating map displacement to image displacement, projected into the plane perpendicular to the surface normal — so it embeds both the projection's local geometry and the slope-parallel-flow constraint. The shared determinant is evaluated once rather than the reference's four times, which is bit-identical.\n\nValid only where cross_check exceeds one degree; the caller checks, as the reference does, and writes nodata otherwise.\n\n\n\n\n\n","title":"ImagePairGeometry.offset_to_velocity"},{"category":"method","location":"#ImagePairGeometry.pairgeometry-Tuple{MapGrid, CoregisteredPair, GeometryInputs}","page":"Home","text":"pairgeometry(grid, pair, inputs; transform = IdentityTransform(), window = nothing,\n             params = GeometryParams(), nodata = nodata_from(nothing)) -> PairGeometry\n\nGeometry of pair at every point of grid covered by window.\n\ngrid is the target MapGrid, pair a CoregisteredPair, and inputs a GeometryInputs whose arrays cover window. transform maps grid coordinates to image coordinates — an IdentityTransform, a TransformPair, a Proj.Transformation (whose inverse is derived when only one direction is given), or a factory returning one, such as ProjTransformFactory or InterpolatedTransform, which is resolved once here.\n\nwindow defaults to the whole grid intersected with the pair's footprint, via footprint_bounds and grid_window.\n\nEvery point is independent, so computing a window in pieces gives the same answer as computing it whole, so blocking and threading cost nothing in fidelity.\n\nExample\n\njulia> using ImagePairGeometry\n\njulia> grid = MapGrid(geotransform = (0.0, 10.0, 0.0, 100.0, 0.0, -10.0), size = (10, 10));\n\njulia> a = ImageFootprint(origin = (5.0, 95.0), spacing = (5.0, -5.0), size = (16, 16));\n\njulia> pair = coregister(a, a; dt = 86400.0);\n\njulia> win = grid_window(grid, footprint_bounds(IdentityTransform(), pair.coordinate))\nCartesianIndices((1:8, 1:8))\n\njulia> r = pairgeometry(grid, pair, GeometryInputs(dem = zeros(size(win))); window = win);\n\njulia> size(r), nvalid(r)\n((8, 8), 64)\n\n\n\n\n\n","title":"ImagePairGeometry.pairgeometry"},{"category":"method","location":"#ImagePairGeometry.pairgeometry_blocked-Tuple{MapGrid, CoregisteredPair, AbstractInputSource}","page":"Home","text":"pairgeometry_blocked(grid, pair, source; transform, window = nothing,\n                     blocksize = DEFAULT_BLOCKSIZE, ntasks = nothing,\n                     params = GeometryParams(), nodata = nodata_from(nothing))\n    -> PairGeometry\n\npairgeometry computed block by block, optionally across threads.\n\nsource is an AbstractInputSource — InMemoryInputs, or the Rasters extension's disk-backed source. blocksize is in grid points. ntasks defaults to min(nblocks, Threads.nthreads()); pass 1 to run serially.\n\nThe result is bit-identical to the unblocked computation at any block size and any task count, because each point depends only on its own inputs.\n\ntransform may be a TransformPair, an AbstractCoordTransform, or — for a threaded run over a PROJ transform — a zero-argument function returning a fresh TransformPair. That last form exists because Proj.Transformation wraps a PJ* on a context that is not thread-safe: each task calls it once and owns the result for its lifetime. See ProjTransformFactory, defined when Proj is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.pairgeometry_blocked"},{"category":"method","location":"#ImagePairGeometry.per_year-Tuple{Float64}","page":"Home","text":"per_year(x) -> Float64\n\nx divided by one year in seconds, in the reference's division order: x / 365.0 / 24.0 / 3600.0.\n\n\n\n\n\n","title":"ImagePairGeometry.per_year"},{"category":"method","location":"#ImagePairGeometry.pixel_index-Tuple{ImagePairGeometry.PointGeometry, ProjectedCoordinate}","page":"Home","text":"pixel_index(g::PointGeometry, c::ProjectedCoordinate) -> NTuple{2,Float64}\n\nZero-based pixel index of the point in the image, rounded to the nearest whole pixel.\n\nMatches geogridOptical.cpp:723-724. Uses cround — halves away from zero — because the reference rounds here in C++, not in NumPy. The result is Float64 so the bounds test that follows sees exactly the value the reference tests, before any integer conversion.\n\n\n\n\n\n","title":"ImagePairGeometry.pixel_index"},{"category":"method","location":"#ImagePairGeometry.pixel_offset-Tuple{StaticArraysCore.SVector{3, Float64}, ImagePairGeometry.PointGeometry, Float64}","page":"Home","text":"pixel_offset(vel, g::PointGeometry, dt) -> NTuple{2,Float64}\n\nDisplacement in pixels, along each image axis, that vel produces over dt.\n\nMatches geogridOptical.cpp:807-808: the velocity projected onto the axis unit vector, times the interval, divided by the physical length of one pixel along that axis. Rounded with cround to a whole pixel.\n\nvel is in meters per year and three-component — use close_slope_parallel to build it — and the axis lengths come from g, so projection distortion is accounted for.\n\n\n\n\n\n","title":"ImagePairGeometry.pixel_offset"},{"category":"method","location":"#ImagePairGeometry.pointgeometry-Tuple{TransformPair, Real, Real, Real, ProjectedCoordinate, StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"pointgeometry(tf::TransformPair, gx, gy, gz, c::ProjectedCoordinate, normal) -> PointGeometry\n\nGeometry at the grid point (gx, gy, gz) for an image with coordinate system c.\n\ngx, gy are the point's center in grid coordinates and gz its elevation. normal comes from surface_normal, or is NO_NORMAL.\n\nReproduces geogridOptical.cpp:717-752. The axis unit vectors are obtained by stepping one pixel along each image axis, transforming back to grid coordinates, and normalizing the difference — so they carry the projection's local rotation and scale, which is what makes the velocity conversion valid off the projection's center.\n\nUnder IdentityTransform a specialized method computes the same values in closed form.\n\n\n\n\n\n","title":"ImagePairGeometry.pointgeometry"},{"category":"function","location":"#ImagePairGeometry.proj_transform","page":"Home","text":"proj_transform(grid_crs, image_crs) -> TransformPair\n\nA TransformPair between two CRSs, built with PROJ.\n\nDefined when Proj is loaded. A threaded run wants ProjTransformFactory from that extension instead, so each task builds a transform on its own PROJ context.\n\n\n\n\n\n","title":"ImagePairGeometry.proj_transform"},{"category":"method","location":"#ImagePairGeometry.readblock-Tuple{InMemoryInputs, CartesianIndices{2, R} where R<:Tuple{OrdinalRange{Int64, Int64}, OrdinalRange{Int64, Int64}}}","page":"Home","text":"readblock(src::AbstractInputSource, block::CartesianIndices{2}) -> GeometryInputs\n\nThe inputs covering block.\n\nFor InMemoryInputs this is a view, so no copy is made and a blocked run over resident arrays allocates nothing per block.\n\n\n\n\n\n","title":"ImagePairGeometry.readblock"},{"category":"method","location":"#ImagePairGeometry.scale_factors-Tuple{ImagePairGeometry.PointGeometry, ProjectedCoordinate}","page":"Home","text":"scale_factors(g::PointGeometry, c::ProjectedCoordinate) -> NTuple{2,Float64}\n\nRatio of true ground distance to nominal pixel spacing, per axis.\n\nMatches geogridOptical.cpp:841-842: the physical length of a one-pixel step in grid coordinates divided by the nominal spacing. Exactly 1 where the grid and image share a CRS; away from that, the map projection's local scale distortion — around 0.9996 at a UTM zone's central meridian.\n\nThe correlator multiplies its measured displacement by these before applying offset_to_velocity, which is why that operator uses nominal spacing.\n\n\n\n\n\n","title":"ImagePairGeometry.scale_factors"},{"category":"method","location":"#ImagePairGeometry.scaled_searchrange-Tuple{SearchRangeScaling, Real, Real}","page":"Home","text":"scaled_searchrange(p::SearchRangeScaling, sr, scale) -> Float64\n\nOne axis of a search range, inflated by scale then clamped to p.lower/p.upper.\n\nMatches geogridOptical.cpp:701-711, where the clamp is min(max(sr, lower), upper).\n\nThe clamp is the reason the reference's nodata handling for search range is partly ineffective. Applied before the nodata comparison, it maps a negative sentinel such as the DEM's -32767 to lower — normally 0.0, which the downstream == 0 test does catch — but maps the search-range rasters' own +32767 to upper, an apparently valid 20000 m/yr range. Reproduced; see REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.scaled_searchrange"},{"category":"method","location":"#ImagePairGeometry.search_pixels-Tuple{StaticArraysCore.SVector{3, Float64}, StaticArraysCore.SVector{3, Float64}, ImagePairGeometry.PointGeometry, ProjectedCoordinate, Float64}","page":"Home","text":"search_pixels(sr1, sr2, g::PointGeometry, c::ProjectedCoordinate, dt) -> NTuple{2,Float64}\n\nSearch half-extent in pixels along each image axis, from a search range already scaled and slope-closed.\n\nsr1 and sr2 are the two search vectors the reference forms: sr2 is sr1 with the x component negated (geogridOptical.cpp:713-714). Both are projected onto each axis and the larger magnitude taken, so the extent covers motion in either direction, then floored at one pixel — a zero extent would mean no search at all.\n\nMatches geogridOptical.cpp:849-863.\n\n\n\n\n\n","title":"ImagePairGeometry.search_pixels"},{"category":"method","location":"#ImagePairGeometry.searchrange_scale-Tuple{SearchRangeScaling, Real}","page":"Home","text":"searchrange_scale(p::SearchRangeScaling, dt) -> Float64\n\nInflation factor applied to a search range for a pair separated by dt seconds.\n\nReproduces geogridOptical.cpp:701-704 including its division order:\n\nstd::max(max_factor * ((dt_unity - 1) * max_factor + (max_factor - 1)\n         - (max_factor - 1) * dt / 24.0 / 3600.0) / ((dt_unity - 1) * max_factor), 1.0)\n\nFalls linearly from max_scale toward 1 as dt grows, floored at 1 — so a pair longer than dt_unity days is not inflated, and never shrunk.\n\njulia> using ImagePairGeometry: SearchRangeScaling, searchrange_scale\n\njulia> p = SearchRangeScaling();\n\njulia> round(searchrange_scale(p, 1 * 86400.0), digits = 4)\n5.0\n\njulia> round(searchrange_scale(p, 91 * 86400.0), digits = 4)\n3.011\n\njulia> searchrange_scale(p, 182 * 86400.0)\n1.0\n\njulia> searchrange_scale(p, 365 * 86400.0)\n1.0\n\n\n\n\n\n","title":"ImagePairGeometry.searchrange_scale"},{"category":"method","location":"#ImagePairGeometry.surface_normal-Tuple{Real, Real}","page":"Home","text":"surface_normal(dhdx, dhdy) -> SVector{3,Float64}\n\nUpward unit normal to a surface with the given slopes.\n\nMatches geogridOptical.cpp:756-764: normalize (dhdx, dhdy, -1) then negate, giving a vector with positive z. The velocity closure divides by that z component, so the sign convention is load-bearing.\n\n\n\n\n\n","title":"ImagePairGeometry.surface_normal"},{"category":"method","location":"#ImagePairGeometry.transform_pair-Tuple{ImagePairGeometry.AbstractCoordTransform}","page":"Home","text":"transform_pair(t) -> TransformPair\n\nA TransformPair from t and its inverse.\n\n\n\n\n\n","title":"ImagePairGeometry.transform_pair"},{"category":"method","location":"#ImagePairGeometry.unitvec3-Tuple{StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"unitvec3(v) -> SVector{3,Float64}\n\nv scaled to unit length by dividing each component by norm3, matching unitvec_C (geogridOptical.cpp:1026-1032).\n\nNot LinearAlgebra.normalize, which multiplies by the reciprocal norm — a different result in the last bit.\n\nA zero vector yields NaN components rather than an error, as the reference does. The zero case is reachable: the surface normal is set to all-zeros when no slope raster is supplied (geogridOptical.cpp:761-764).\n\n\n\n\n\n","title":"ImagePairGeometry.unitvec3"},{"category":"method","location":"#ImagePairGeometry.window_geotransform-Tuple{MapGrid, CartesianIndices{2, R} where R<:Tuple{OrdinalRange{Int64, Int64}, OrdinalRange{Int64, Int64}}}","page":"Home","text":"window_geotransform(g::MapGrid, window::CartesianIndices{2}) -> NTuple{6}\n\nGeotransform of the sub-grid window selects, for writing outputs that carry their own georeferencing.\n\nMatches the reference's output geotransform: the origin shifts by the window offset and the spacing is unchanged (geogridOptical.cpp:246-252).\n\n\n\n\n\n","title":"ImagePairGeometry.window_geotransform"},{"category":"function","location":"#ImagePairGeometry.write_geotiffs","page":"Home","text":"write_geotiffs(dir, g::PairGeometry) -> Vector{String}\n\nWrite a result as the reference's nine GeoTIFFs. Defined when Rasters is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.write_geotiffs"},{"category":"method","location":"#ImagePairGeometry.xsize-Tuple{ProjectedCoordinate}","page":"Home","text":"xsize(c::ProjectedCoordinate)\nysize(c::ProjectedCoordinate)\n\nAbsolute pixel size along each axis — geogrid's X_res/Y_res, reported in its scalar output and used downstream to convert a chip size in meters to pixels.\n\n\n\n\n\n","title":"ImagePairGeometry.xsize"},{"category":"type","location":"#ImagePairGeometryProjExt.ProjTransformFactory","page":"Home","text":"ProjTransformFactory(grid_crs, image_crs)\n\nA factory building a TransformPair between two CRSs, one PROJ context per call.\n\nPass it as pairgeometry_blocked's transform: it is called once per task, so each task gets a transformation on a context it alone uses. Calling it is not cheap — about 1.7 ms, dominated by a cold proj.db cache — which is why it is per task rather than per block.\n\ngrid_crs and image_crs are anything Proj.Transformation accepts: \"EPSG:3413\", an integer EPSG code, a WKT string, or a GeoFormatTypes object.\n\nThe pair's forward direction is grid to image, matching the kernel; footprint_bounds takes the inverse from the pair itself.\n\nfactory = ProjTransformFactory(3413, 32624)\nr = pairgeometry_blocked(grid, pair, source; transform = factory, ntasks = 8)\n\nFor a serial run factory() gives a pair directly, and a same-CRS run should use IdentityTransform() instead — it is exact and skips PROJ entirely.\n\n\n\n\n\n","title":"ImagePairGeometryProjExt.ProjTransformFactory"},{"category":"method","location":"#ImagePairGeometry.proj_transform-Tuple{Any, Any}","page":"Home","text":"ImagePairGeometry.proj_transform(grid_crs, image_crs) -> TransformPair\n\nA TransformPair between two CRSs on a fresh PROJ context.\n\nFor a serial run. A threaded run wants ProjTransformFactory so each task builds its own, since a context is not safe to use from two threads at once.\n\nalways_xy = false reproduces the reference, which builds its transforms with osr.CoordinateTransformation on SRSs from ImportFromEPSG — authority axis order. For projected-to-projected pairs the two agree, so this is fidelity to the reference rather than a behavioral choice.\n\n\n\n\n\n","title":"ImagePairGeometry.proj_transform"},{"category":"type","location":"#ImagePairGeometryRastersExt.RasterInputs","page":"Home","text":"RasterInputs(; dem, dhdx = nothing, ..., ssm = nothing)\n\nAn input source reading each block's window from rasters.\n\nAccepts the same eleven optional rasters as GeometryInputs, and each may be lazy: a disk-backed Raster is read one window at a time, so a grid larger than memory is never materialized. All must share the grid the geometry is computed on.\n\nOpen each with missingval = nothing, so its stored values arrive unmasked:\n\nparam(name) = Raster(name; lazy = true, missingval = nothing)\nsrc = RasterInputs(dem = param(\"h.tif\"), vx = param(\"vx.tif\"), ...)\n\nA raster whose element type admits missing is refused. Which value counts as missing is decided by NoDataPolicy from the DEM's sentinel — the reference applies that one sentinel to every raster — so a raster's own declared nodata is not it. The ITS_LIVE parameter rasters declare nodata 0 for chip size and stable surface and 32767 for search range, all of which are ordinary data here.\n\nWindows are indexed in grid coordinates, so the rasters must cover the whole grid, not just the window — which is the usual case, since the DEM is what defines the grid.\n\n\n\n\n\n","title":"ImagePairGeometryRastersExt.RasterInputs"},{"category":"method","location":"#ImagePairGeometry.blocksize_from_chunks-Tuple{ImagePairGeometryRastersExt.RasterInputs}","page":"Home","text":"ImagePairGeometry.blocksize_from_chunks(src::RasterInputs; floor = 256) -> NTuple{2,Int}\n\nA block size aligned to the inputs' own chunking.\n\nReading a window that straddles chunk boundaries reads whole chunks and discards the rest, so a block that is a whole number of chunks reads each byte once. floor guards the pathological case: a striped GeoTIFF — what Rasters.write produces by default — reports chunks one row tall, and a block one row tall would issue a read per row.\n\nReturns ImagePairGeometry.DEFAULT_BLOCKSIZE when no input is disk-backed, since then there is no chunking to align to.\n\n\n\n\n\n","title":"ImagePairGeometry.blocksize_from_chunks"},{"category":"method","location":"#ImagePairGeometry.image_footprint-Tuple{AbstractRaster}","page":"Home","text":"ImagePairGeometry.image_footprint(image::AbstractRaster) -> ImageFootprint\n\nAn ImageFootprint describing where image sits, for coregister.\n\nOrigin is the first pixel's center, which is what ImageFootprint documents and what the reference's startingX/startingY are — so the pixel edge the geotransform names is shifted half a pixel inward.\n\nOnly the geometry is read, never the pixels, so this is cheap on a disk-backed scene and needs no data at all.\n\n\n\n\n\n","title":"ImagePairGeometry.image_footprint"},{"category":"method","location":"#ImagePairGeometry.mapgrid-Tuple{AbstractRaster}","page":"Home","text":"ImagePairGeometry.mapgrid(dem::AbstractRaster) -> MapGrid\n\nA MapGrid describing dem's grid.\n\nThe grid's origin is the outer edge of the first pixel, the convention MapGrid and GDAL share. For a raster backed by a file that is GDAL's geotransform, read from the file. For one built in memory the convention is inferred from the lookup's sampling, and a Center or point lookup is adjusted by half a pixel, since taking its coordinates for edges would offset every output by that much.\n\nA raster whose lookups are not regularly spaced is refused: the index arithmetic this package reproduces assumes a constant step.\n\n\n\n\n\n","title":"ImagePairGeometry.mapgrid"},{"category":"method","location":"#ImagePairGeometry.write_geotiffs-Tuple{AbstractString, PairGeometry}","page":"Home","text":"ImagePairGeometry.write_geotiffs(dir, g::PairGeometry) -> Vector{String}\n\nWrite g as the reference's nine GeoTIFFs in dir, returning the paths written.\n\nNames, band order, data types and nodata values match the reference, so a consumer built against its output reads these unchanged. Int32 bands are written as Int32 and Float64 as Float64, as the reference writes GDT_Int32 and GDT_Float64.\n\nA file whose bands are entirely nodata is skipped, matching the reference: it writes no file at all for an output its inputs did not support, and a consumer distinguishes \"unsupported\" from \"computed and empty\" by the file's absence.\n\n\n\n\n\n","title":"ImagePairGeometry.write_geotiffs"},{"category":"method","location":"#ImagePairGeometryRastersExt._file_geotransform-Tuple{Any}","page":"Home","text":"_file_geotransform(r) -> NTuple{6,Float64} or nothing\n\nr's geotransform as GDAL reports it, or nothing for a raster not backed by a file.\n\nPreferred over deriving one from the lookups because it is the file's own answer. Rasters sets a lookup's sampling from the AREA_OR_POINT tag — Points for Point, Intervals{Start} for Area — while reporting the same coordinates either way, namely the geotransform's. So a raster tagged AREA_OR_POINT=Point, which both the ITS_LIVE parameter rasters and the Landsat scenes are, gets Points sampling with coordinates that are already pixel edges. Inferring the convention from the sampling would take those edges for centers and displace the grid half a pixel.\n\n\n\n\n\n","title":"ImagePairGeometryRastersExt._file_geotransform"}]
}
