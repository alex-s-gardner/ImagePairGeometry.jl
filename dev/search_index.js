var documenterSearchIndex = {"docs":
[{"category":"section","location":"radar/#Radar-geometry","page":"Radar geometry","text":"Where a projected image reaches a pixel index through a coordinate transform and an affine calculation, a radar image reaches one through orbit state vectors and a range–Doppler solve. This page documents that machinery: the reference ellipsoid, orbit interpolation, and the two solves that convert between ground and radar coordinates.","title":"Radar geometry"},{"category":"section","location":"radar/#Status","page":"Radar geometry","text":"Complete and verified against the compiled reference. pairgeometry takes a radar pair and produces all nine outputs, checked over eight fixture cases covering both look sides, a grid larger than the swath, scattered input nodata, and each combination of inputs that gates an output band.\n\nThe numerics beneath it are verified against isce3 — the same objects NASA JPL's geogridRadar.cpp links against.\n\nTwo float bands agree to 1.07e-4 relative rather than bitwise: bands 2 and 3 of the off2vel files, which divide by the along-track step. REFERENCE.md records the nine hypotheses eliminated in attributing that to the compiled kernel rather than to this port — including that two independent reproductions of the reference's algorithm agree with each other to 4e-6 lines while both sit 0.0013 from the kernel.","title":"Status"},{"category":"section","location":"radar/#Why-this-is-transcribed-rather-than-delegated","page":"Radar geometry","text":"The reference hardcodes a = 6378137.0 and e2 = 0.0066943799901 — the latter a truncation of WGS84's 6.69437999014e-3 at eight significant digits — and converts ECEF to geodetic by Vermeille's 2002 closed form. A geodesy library uses the full-precision datum and a different formulation, so it agrees to about 1e-9 and not to the last bit. Since the radar path's integer outputs are std::round of quantities computed through these conversions, those last bits decide a range index sitting near a rounding boundary.\n\nREFERENCE.md records the measured agreement, band by band, and the two mechanisms that prevent it being bitwise throughout: atan2 differs by one ULP between openlibm and the platform libm, and floating-point contraction reaches the Hermite velocity through a cancellation. The bound that carries the weight is stated in meters — 1.9e-9 m of ground position, against a range sample of about 2.3 m.","title":"Why this is transcribed rather than delegated"},{"category":"section","location":"radar/#Building-a-coordinate","page":"Radar geometry","text":"The incidence angle is computed first and passed in, because the reference computes it first (testGeogrid.py:487-488) and the ground range spacing depends on it:\n\nia = incidence_angle(; orbit, starting_range = 8.0e5, dr = 2.3295621147,\n                     sensing_start = 300.0, prf = 486.4863103,\n                     nsamples = 10000, nlines = 8000,\n                     look_side = LookRight, wavelength = 0.05546576)\n\ncoord = RadarCoordinate(; orbit, incidence_angle = ia, starting_range = 8.0e5,\n                        dr = 2.3295621147, sensing_start = 300.0, prf = 486.4863103,\n                        nsamples = 10000, nlines = 8000,\n                        look_side = LookRight, wavelength = 0.05546576)\n\npair = CoregisteredPair(coord; dt = 6 * 86400.0)\n\nThere is no radar coregister: testGeogrid.py:427-470 takes every radar parameter from the reference acquisition and the secondary only for the repeat interval, so the radar grid is image 1's outright.","title":"Building a coordinate"},{"category":"section","location":"radar/#Two-time-scales","page":"Radar geometry","text":"sensing_start is seconds since midnight of the acquisition day, and the orbit is on its own epoch. orbit_epoch_offset is the constant between them. Both are carried because the reference carries both: it measures the azimuth index against the first and interpolates the orbit against the second, so neither needs a conversion at the point of use.","title":"Two time scales"},{"category":"section","location":"radar/#The-two-iteration-counts-assume-SAR-geometry","page":"Radar geometry","text":"Both solves run a fixed iteration count with no convergence test, and both use fewer iterations than the reference: GEO2RDR_ITERATIONS is 16 against its 51, and RANGE_DOPPLER_ITERATIONS is 6 against its 10. Together they are worth about 2× of the per-point radar cost, and they are the two assumptions in this package that depend on a property of the acquisition rather than of the code.\n\nThe two are governed by different parameters, so a geometry that is easy for one is not necessarily easy for the other:\n\n governed by worst case requirement count\nGEO2RDR_ITERATIONS orbital altitude high orbits 12 16\nRANGE_DOPPLER_ITERATIONS target latitude mid-latitudes, 30–45° 4 6\n\nThe range-Doppler count is the less exposed of the two: its rate vanishes at the equator and at the pole, so polar ITS_LIVE grids sit well inside the margin, and it reaches its arithmetic floor at 6 — further iterations cannot refine the answer, only land on different rounding noise. Altitude and terrain height each move its requirement by less than one iteration.\n\nThe zero-Doppler count is the one to check against an unusual acquisition.\n\nThe solve converges linearly at a rate geo2rdr_rate gives in closed form, and that rate is governed by orbital altitude. 16 covers every altitude from 350 to 800 km with four iterations of margin, which spans every operating SAR mission — Sentinel-1 and NISAR, the two this package is principally for, need 12. Terrain height, incidence angle, look side, grid spacing and repeat interval are all second order.\n\nAbove roughly 1000 km the requirement climbs past 16 and the constant must rise with it. If you are processing an acquisition from an unusually high orbit, or if radar geometry disagrees with an independent reference in a way that looks like a small azimuth-time error, check this first:\n\nusing ImagePairGeometry\nusing ImagePairGeometry: geo2rdr_iterations_needed, GEO2RDR_ITERATIONS\n\n# satpos at scene center, target at the far swath edge, half the scene length as the guess error.\nneeded = geo2rdr_iterations_needed(satpos, target; guess_error = 0.5 * nlines / prf)\nneeded <= GEO2RDR_ITERATIONS || @warn \"orbit needs more iterations than the fixed count\" needed\n\nREFERENCE.md records the mission-by-mission measurements behind the choice, what the fixture does and does not constrain, and the two things the study did not cover.","title":"The two iteration counts assume SAR geometry"},{"category":"section","location":"radar/#Trading-blocking-invariance-for-speed","page":"Radar geometry","text":"WarmStart starts each zero-Doppler solve from the previous grid point's answer instead of the scene midpoint. Adjacent points are a few hundredths of an azimuth line apart, so 8 iterations suffice where a cold start needs 16 — about 1.25× on the radar path:\n\nparams = GeometryParams(zero_doppler_start = WarmStart())\nr = pairgeometry(grid, pair, inputs; transform = tf, window = win, params)\n\nThe cost is that results become order-dependent, so a blocked or threaded run no longer matches an unblocked one bit for bit. Integer bands are unaffected — no rounded index moves — but the float bands differ by up to 5e-7 relative, well inside the 2e-4 they are held to against isce3. If you need blocked and unblocked runs to agree exactly, keep the default SceneCenterStart().\n\nGrid spacing does not change what the warm start needs: measured from 120 m to 10 km the divergence is flat and no index moves, so a sparse pass for later interpolation onto a finer grid is as safe as a dense one.","title":"Trading blocking invariance for speed"},{"category":"section","location":"radar/#API","page":"Radar geometry","text":"","title":"API"},{"category":"type","location":"radar/#ImagePairGeometry.Ellipsoid","page":"Radar geometry","text":"Ellipsoid(a, e2)\nEllipsoid()\n\nA biaxial ellipsoid, stored as semi-major axis and eccentricity squared with every other quantity derived.\n\nThe no-argument form is WGS84: WGS84_A and WGS84_E2.\n\nMirrors isce3::core::Ellipsoid (Ellipsoid.h).\n\n\n\n\n\n","title":"ImagePairGeometry.Ellipsoid"},{"category":"type","location":"radar/#ImagePairGeometry.TCNBasis","page":"Radar geometry","text":"TCNBasis\n\nAn orthonormal frame at a satellite position: along-track, cross-track and nadir-pointing.\n\nFields\n\nthat: along-track, c × n normalized.\nchat: cross-track, n × v normalized.\nnhat: geocentric nadir, -x normalized — pointing down, toward the Earth's center.\n\nThe range-Doppler solve expresses the satellite-to-target vector in this frame, so the sign of nhat and the handedness of the pair are load-bearing: beta's sign carries the look side against chat, and flipping either would place the target on the wrong side of the track.\n\n\n\n\n\n","title":"ImagePairGeometry.TCNBasis"},{"category":"constant","location":"radar/#ImagePairGeometry.WGS84_A","page":"Radar geometry","text":"WGS84_A\n\nSemi-major axis in meters: 6378137.0.\n\n\n\n\n\n","title":"ImagePairGeometry.WGS84_A"},{"category":"constant","location":"radar/#ImagePairGeometry.WGS84_E2","page":"Radar geometry","text":"WGS84_E2\n\nEccentricity squared: 6.69437999014e-3, the WGS84 value at full precision.\n\nisce3 truncates this to 0.0066943799901 at eight significant digits. The difference is 6e-12 relative and reaches a position as 1.5e-7 m, well inside the tolerance every downstream comparison uses.\n\n\n\n\n\n","title":"ImagePairGeometry.WGS84_E2"},{"category":"method","location":"radar/#ImagePairGeometry.geodetic_tcn-Tuple{StaticArraysCore.SVector{3, Float64}, StaticArraysCore.SVector{3, Float64}}","page":"Radar geometry","text":"geodetic_tcn(pos, vel) -> TCNBasis\n\nThe geocentric TCN frame at satellite position pos with velocity vel, both ECEF.\n\nDespite the name — inherited from isce3::core::Basis's constructor — this frame is geocentric: nhat is antiparallel to the position vector, not to the ellipsoid normal. The two differ by up to 0.19° at mid-latitudes. The range-Doppler solve compensates by working against a local sphere of radius radius, computed per point in geo2rdr's caller.\n\ngeogridRadar.cpp:1027-1034 and Basis.h:50-58 build this identically, the former inline with unitvec_C2 and the latter with Eigen's normalized(). Both divide by the norm rather than scaling by its reciprocal, so unitvec3 serves both.\n\n\n\n\n\n","title":"ImagePairGeometry.geodetic_tcn"},{"category":"method","location":"radar/#ImagePairGeometry.lonlat_to_xyz-Tuple{Ellipsoid, StaticArraysCore.SVector{3, Float64}}","page":"Radar geometry","text":"lonlat_to_xyz(el::Ellipsoid, llh) -> SVector{3,Float64}\n\nECEF position in meters from llh = (lon, lat, height) with the angles in radians.\n\nEvaluated by FastGeoProjections.LonLatToGeocentric, which takes degrees; the conversion is here so that the radians convention the radar solves use stops at this boundary.\n\n\n\n\n\n","title":"ImagePairGeometry.lonlat_to_xyz"},{"category":"method","location":"radar/#ImagePairGeometry.nadir_sphere-Tuple{Ellipsoid, StaticArraysCore.SVector{3, Float64}}","page":"Radar geometry","text":"nadir_sphere(el::Ellipsoid, pos) -> NTuple{4,Float64}\n\n(radius, height, eta, sat_dist) for the sphere osculating the ellipsoid beneath the satellite at ECEF position pos.\n\nradius is the local radius directly below the satellite and height the satellite's height above it; eta is the scale factor relating the two to the geocentric distance sat_dist. The range-Doppler solve iterates on a target height measured from this sphere rather than from the ellipsoid, which is why it needs all of them — sat_dist is returned rather than left to the caller to recompute, since it is norm3(pos) and already in hand here.\n\ngeogridRadar.cpp:1016-1024 and Rdr2Geo.icc:69-79 agree term for term.\n\n\n\n\n\n","title":"ImagePairGeometry.nadir_sphere"},{"category":"method","location":"radar/#ImagePairGeometry.r_east-Tuple{Ellipsoid, Float64}","page":"Radar geometry","text":"r_east(el::Ellipsoid, lat) -> Float64\n\nPrime vertical radius of curvature at geodetic latitude lat in radians (Ellipsoid.h:150-153).\n\n\n\n\n\n","title":"ImagePairGeometry.r_east"},{"category":"method","location":"radar/#ImagePairGeometry.r_east_sin-Tuple{Ellipsoid, Float64}","page":"Radar geometry","text":"r_east_sin(el::Ellipsoid, slat) -> Float64\n\nr_east from an already-computed sin(lat), for a caller that needs the sine anyway.\n\n\n\n\n\n","title":"ImagePairGeometry.r_east_sin"},{"category":"method","location":"radar/#ImagePairGeometry.semiminor-Tuple{Ellipsoid}","page":"Radar geometry","text":"semiminor(el::Ellipsoid) -> Float64\n\nSemi-minor axis, a * sqrt(1 - e2) (Ellipsoid.h:41).\n\n\n\n\n\n","title":"ImagePairGeometry.semiminor"},{"category":"method","location":"radar/#ImagePairGeometry.xyz_to_lonlat-Tuple{Ellipsoid, StaticArraysCore.SVector{3, Float64}}","page":"Radar geometry","text":"xyz_to_lonlat(el::Ellipsoid, xyz) -> SVector{3,Float64}\n\n(lon, lat, height) with the angles in radians, from an ECEF position in meters.\n\nVermeille's 2002 closed form, following Ellipsoid.h:210-238. Agrees with PROJ's EPSG:4978 to EPSG:4979 pipeline to 9e-7 m over latitudes, longitudes and heights.\n\nThe cube root is cbrt, not the x^(1/3) isce3 spells (std::pow(x, 1./3.)): the two agree to 1.4e-9 in the result and cbrt is the faster of them, since pow has no way to know the exponent is a cube root.\n\nAngles come back within one ULP of a platform's own atan2 rather than bitwise equal to it. Julia's atan is openlibm's, and openlibm is kept deliberately: both it and any system libm are faithful and neither is correctly rounded, measured against a 256-bit evaluation, so matching a particular machine would trade openlibm's cross-platform reproducibility for agreement with whichever libm happens to be running. One ULP of angle here is 5e-10 m of position, against a range sample of about 2.3 m. See REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.xyz_to_lonlat"},{"category":"type","location":"radar/#ImagePairGeometry.Orbit","page":"Radar geometry","text":"Orbit(; time, position, velocity)\nOrbit(t0, spacing, position, velocity)\n\nSatellite state vectors on a uniformly spaced time axis.\n\nposition and velocity are ECEF, in meters and meters per second. Times are seconds from whatever epoch the caller uses; the epoch itself is never stored, so every time passed to interpolate must be on the same scale.\n\nThe keyword form takes an explicit time vector and derives the spacing from it, rejecting a non-uniform axis. The positional form takes the first time and the spacing directly.\n\nUniform spacing is not a simplification: isce3 stores the axis as a Linspace and its interpolator locates the bracketing state vectors by arithmetic on the spacing rather than by search (Linspace.icc:77-89). A non-uniform axis has no representation there, and getOrbitTime (BuildOrbit.cpp:12-46) rejects one.\n\nAt least four state vectors are required — the Hermite interpolant is built from four (InterpolateOrbit.icc:22).\n\nExample\n\njulia> using ImagePairGeometry: Orbit, starttime, stoptime\n\njulia> t = collect(0.0:10.0:40.0);\n\njulia> pos = [[7.0e6, 0.0, 1.0e5 * i] for i in eachindex(t)];\n\njulia> vel = [[0.0, 7.5e3, 1.0e4] for _ in eachindex(t)];\n\njulia> orbit = Orbit(; time = t, position = pos, velocity = vel);\n\njulia> length(orbit), starttime(orbit), stoptime(orbit)\n(5, 0.0, 40.0)\n\n\n\n\n\n","title":"ImagePairGeometry.Orbit"},{"category":"type","location":"radar/#ImagePairGeometry.OrbitDomainError","page":"Radar geometry","text":"OrbitDomainError\n\nThrown when an interpolation time falls outside the span of an Orbit's state vectors.\n\nThe reference interpolates with OrbitInterpBorderMode::Error (geogridRadar.cpp:434) and exits on failure, so extrapolation is not a behavior to reproduce. A time outside the domain means the orbit does not cover the acquisition — the state vectors are wrong, or the time is.\n\n\n\n\n\n","title":"ImagePairGeometry.OrbitDomainError"},{"category":"method","location":"radar/#ImagePairGeometry.interpolate-Tuple{Orbit, Real}","page":"Radar geometry","text":"interpolate(o::Orbit, t) -> NTuple{2,SVector{3,Float64}}\n\n(position, velocity) at time t, by cubic Hermite interpolation over the four state vectors bracketing it.\n\nThrows OrbitDomainError for a time outside the orbit's span, matching the reference's OrbitInterpBorderMode::Error (geogridRadar.cpp:434).\n\nTranscribed from interpolateOrbitHermite (InterpolateOrbit.icc:11-108). isce3's Legendre alternative is not ported: OrbitInterpMethod::Hermite is the default (Orbit.h:198) and the reference never changes it.\n\nHermite over four points, rather than the two a cubic through positions alone would need, because the interpolant matches position and velocity at all four.\n\nPosition agrees with isce3 bitwise except within half a spacing of the domain edges, where it is within 1 ULP. Velocity agrees to about 4e-14 relative — 3e-10 m/s on an orbital speed — because the velocity weights carry a cancellation the position weights do not: g0 is 2 * (f0 * hdot - s * h), a difference of two products each larger than their difference, so a contracted evaluation and a strictly-rounded one diverge there. isce3 is compiled with contraction enabled and Julia rounds each operation. Confirmed to be that and not a transcription error by an independent implementation of the same formula, which reproduces this result rather than isce3's. See REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.interpolate"},{"category":"method","location":"radar/#ImagePairGeometry.starttime-Tuple{Orbit}","page":"Radar geometry","text":"starttime(o::Orbit) -> Float64\nstoptime(o::Orbit) -> Float64\n\nFirst and last state vector times, in seconds on the caller's scale.\n\n\n\n\n\n","title":"ImagePairGeometry.starttime"},{"category":"method","location":"radar/#ImagePairGeometry.statetime-Tuple{Orbit, Integer}","page":"Radar geometry","text":"statetime(o::Orbit, i) -> Float64\n\nTime of the ith state vector, one-based, computed as t0 + (i - 1) * spacing.\n\nComputed rather than stored, matching Linspace::operator[] (Linspace.h:56) — so a time midway through a long orbit carries the rounding of one multiply-add, not of an accumulated sum.\n\n\n\n\n\n","title":"ImagePairGeometry.statetime"},{"category":"type","location":"radar/#ImagePairGeometry.RadarPoint","page":"Radar geometry","text":"RadarPoint\n\nWhere a ground point falls in radar coordinates, and the geometry at that point.\n\nFields\n\naztime: azimuth time on the image time scale — the one the azimuth index is measured against.\norbittime: the same instant on the orbit's time scale. See geo2rdr on why both.\nrange: slant range in meters, lagging aztime by one Newton iteration. See below.\nlook: satellite-to-target vector in ECEF meters, unnormalized, whose length is range.\nposition, velocity: satellite state at orbittime after the final iteration.\n\nrange and look are the reference's rngpix and drpos, and both are one iteration stale: they are computed at the top of the loop body from the previous iteration's satellite position, and the loop then updates the time and re-interpolates without recomputing them (geogridRadar.cpp:951-969). So the range that produces the range index does not correspond to the time that produces the azimuth index.\n\nReproducing the staleness is deliberate. The effect is sub-ULP in practice, since the final iterations barely move the solution — but look also feeds the line-of-sight axis vector and the scale factors downstream, so a \"corrected\" range would propagate a difference into bands that are asserted bitwise.\n\n\n\n\n\n","title":"ImagePairGeometry.RadarPoint"},{"category":"constant","location":"radar/#ImagePairGeometry.GEO2RDR_ITERATIONS","page":"Radar geometry","text":"GEO2RDR_ITERATIONS\n\nFixed iteration count for the zero-Doppler solve: 16. The reference uses 51 (geogridRadar.cpp:949); this is the one place the radar path spends fewer iterations than it, and the divergence is the largest single speedup on the path — about 1.6× of the whole per-point cost.\n\nThere is no convergence test and no residual threshold, matching the reference: the loop runs 16 times regardless. A threshold is the wrong instrument here because the step never reaches zero — past convergence the iteration oscillates at the ULP of aztime rather than settling — so a threshold below that floor would run to a cap on every point anyway. The floor scales with the epoch: 1e-9 azimuth lines at a sensing_start of 300 s, 1e-7 lines on a seconds-since-epoch clock at 1e7 s. Both are far below a rounding boundary.\n\nWhy 16, and when it would not be enough\n\nThe count must exceed the iterations the worst grid point of the worst scene needs. That number is scene dependent, and predictable: see geo2rdr_rate for the closed form and geo2rdr_iterations_needed to evaluate it for an acquisition in hand.\n\nRequirement measured across synthetic acquisitions spanning real mission geometry, taking the far end of the scene as the worst point — the reference starts every solve from scene center, so that point is half a scene length from the initial guess:\n\ngeometry rate iterations to 1e-9 s\n350 km (below any SAR orbit) 0.053 9\nTerraSAR-X, ICEYE, Capella (510–570 km) 0.076–0.083 10\nALOS-2 (628 km) 0.091 11\nSentinel-1 (693 km) 0.100 11–12\nNISAR (747 km) 0.107 12\nRADARSAT-2 (798 km) 0.113 12\n1400 km (above any SAR orbit) 0.186 15\n\nWorst over the realistic space is 12, so 16 carries four iterations of margin — a factor of 1e-3 in error at the worst realistic rate. Terrain height (sea level to 8000 m), incidence angle (15° to 50°), look side, grid spacing and repeat interval all move the requirement by less than one iteration. Orbit eccentricity lowers it.\n\nThe bound this rests on is orbital altitude, and it is the thing to check first if real data ever disagrees: above roughly 1000 km the requirement climbs past 16 and this constant must rise with it. geo2rdr_iterations_needed answers that for a given orbit without re-deriving anything.\n\n\n\n\n\n","title":"ImagePairGeometry.GEO2RDR_ITERATIONS"},{"category":"method","location":"radar/#ImagePairGeometry.azimuth_index-Tuple{ImagePairGeometry.RadarPoint, Float64, Float64}","page":"Radar geometry","text":"azimuth_index(p::RadarPoint, sensing_start, prf) -> Float64\n\nZero-based azimuth line index of a radar point, rounded to a whole line.\n\nMatches geogridRadar.cpp:972: std::round((tline - sensingStart) * prf), where tline is RadarPoint's aztime and sensing_start is on the same scale — seconds since midnight of the acquisition day, as GeogridRadar.py:329-332 computes it.\n\n\n\n\n\n","title":"ImagePairGeometry.azimuth_index"},{"category":"function","location":"radar/#ImagePairGeometry.geo2rdr","page":"Radar geometry","text":"geo2rdr(orbit, target, aztime0, orbittime0, pos0, vel0) -> RadarPoint\n\nAzimuth time and slant range at which orbit images the ECEF point target.\n\naztime0 and orbittime0 are the same starting instant expressed on two time scales, and pos0, vel0 the satellite state there. Both times receive the same Newton increment each iteration; only orbittime0 is fed to the interpolator, and only aztime0's descendant reaches the azimuth index.\n\nTheir difference is preserved to rounding rather than to the bit — each subtraction rounds to its own exponent, so two scales separated by a large epoch offset drift by an ULP of the larger. At 1e5 s of separation that is 1e-11 s, or 5e-9 of an azimuth line.\n\nCarrying two scales rather than one is the reference's structure (tline and tlined, geogridRadar.cpp:906-911), and it is not redundant because the two clocks have different origins. sensingStart is seconds since midnight of the acquisition day, so tline — and the azimuth index derived from it (:972) — is on that clock. tlined comes from an absolute UTC timestamp reduced to seconds since the orbit's reference epoch (:432), which is the clock the interpolator needs. The caller supplies both and this function preserves their difference.\n\nConvergence is not checked, per GEO2RDR_ITERATIONS, which is also the default niter. A caller that starts from a better guess than the scene midpoint may pass a smaller count — see WarmStart, which is the only thing in this package that does.\n\nA target the orbit cannot image — off the end of the state vectors — surfaces as an OrbitDomainError from the interpolator rather than as a silent non-convergence.\n\n\n\n\n\n","title":"ImagePairGeometry.geo2rdr"},{"category":"method","location":"radar/#ImagePairGeometry.geo2rdr_iterations_needed-Tuple{StaticArraysCore.SVector{3, Float64}, StaticArraysCore.SVector{3, Float64}}","page":"Radar geometry","text":"geo2rdr_iterations_needed(satpos, target; guess_error = 60.0, tol = 1e-9) -> Int\n\nIterations geo2rdr needs at one geometry to bring an initial guess guess_error seconds from the answer within tol seconds of it.\n\nlog(tol / guess_error) / log(r) at the rate geo2rdr_rate gives, rounded up. Use it to check that GEO2RDR_ITERATIONS covers an acquisition whose orbit sits outside the altitudes that constant was chosen against — pass the satellite position at scene center, a target at the far edge of the swath, and half the scene length as guess_error.\n\nReturns a requirement, not a verdict: compare it against GEO2RDR_ITERATIONS yourself.\n\n\n\n\n\n","title":"ImagePairGeometry.geo2rdr_iterations_needed"},{"category":"method","location":"radar/#ImagePairGeometry.geo2rdr_rate-Tuple{StaticArraysCore.SVector{3, Float64}, StaticArraysCore.SVector{3, Float64}}","page":"Radar geometry","text":"geo2rdr_rate(satpos, target) -> Float64\n\nThe per-iteration convergence rate of geo2rdr at one geometry: the factor by which each iteration multiplies the remaining error.\n\nBecause fnprime drops the acceleration term, the iteration is a fixed point with rate r = 1 - f'(t) / D, which for a near-circular orbit reduces to\n\nr = 1 - (r_t / R) cos(ψ)\n\nwith R the satellite's geocentric radius, r_t the target's, and ψ the geocentric angle between them. Since r_t cos(ψ) is (satpos · target) / R, this evaluates as 1 - (satpos · target) / R² without recovering either angle. Reproduces the measured rate to four significant digits over altitudes from 350 to 1400 km and incidence angles from 15° to 50°.\n\nR dominates: r runs from 0.053 at 350 km to 0.186 at 1400 km, and the requirement on GEO2RDR_ITERATIONS rises with it. The closed form assumes a = -ω²x, exact only for a circular orbit; a measured eccentricity of 0.05 lowers the true rate to 0.058 against a predicted 0.10, so the form is conservative rather than wrong for an eccentric orbit.\n\n\n\n\n\n","title":"ImagePairGeometry.geo2rdr_rate"},{"category":"method","location":"radar/#ImagePairGeometry.range_index-Tuple{ImagePairGeometry.RadarPoint, Float64, Float64}","page":"Radar geometry","text":"range_index(p::RadarPoint, starting_range, dr) -> Float64\n\nZero-based range sample index of a radar point, rounded to a whole sample.\n\nMatches geogridRadar.cpp:971: std::round((rngpix - startingRange) / dr). Uses cround — ties away from zero — because the reference rounds here in C++.\n\nReturned as Float64 so the bounds test sees the value before any integer conversion, as on the projected path. Note that the reference's bounds test at :1108 compares the converted int, unlike the projected path's test on the pre-conversion float; the difference is immaterial here because the value is already integral.\n\n\n\n\n\n","title":"ImagePairGeometry.range_index"},{"category":"type","location":"radar/#ImagePairGeometry.LookSide","page":"Radar geometry","text":"LookSide\n\nWhich side of the ground track the radar looks at: LookLeft or LookRight.\n\nThe reference carries this as an integer lookSide multiplying a square root (geogridRadar.cpp:1048), where -1 is right-looking. looksign converts.\n\n\n\n\n\n","title":"ImagePairGeometry.LookSide"},{"category":"constant","location":"radar/#ImagePairGeometry.LookLeft","page":"Radar geometry","text":"LookLeft\n\nLeft-looking radar geometry, the +1 branch of the reference's lookSide. See LookSide.\n\n\n\n\n\n","title":"ImagePairGeometry.LookLeft"},{"category":"constant","location":"radar/#ImagePairGeometry.LookRight","page":"Radar geometry","text":"LookRight\n\nRight-looking radar geometry, the -1 branch of the reference's lookSide — Sentinel-1's configuration. See LookSide.\n\n\n\n\n\n","title":"ImagePairGeometry.LookRight"},{"category":"constant","location":"radar/#ImagePairGeometry.RDR2GEO_THRESHOLD","page":"Radar geometry","text":"RDR2GEO_THRESHOLD\nRDR2GEO_MAXITER\nRDR2GEO_EXTRAITER\n\nisce3's Rdr2GeoParams defaults: a range residual threshold of 1e-8 m, 25 primary iterations and 15 extra ones (Rdr2Geo.h:14-22).\n\nThe reference's callers do not override them (GeogridRadar.py:169-179 passes positionally up to the DEM and ellipsoid, leaving the tail defaulted), so these are the values in production.\n\n\n\n\n\n","title":"ImagePairGeometry.RDR2GEO_THRESHOLD"},{"category":"method","location":"radar/#ImagePairGeometry.looksign-Tuple{LookSide}","page":"Radar geometry","text":"looksign(side::LookSide) -> Float64\n\nThe reference's lookSide integer as a float: -1.0 for LookRight, +1.0 for LookLeft.\n\nThe sign convention is inverted relative to how it reads: geogridRadar.cpp:1048 computes beta = -lookSide * sqrt(...), so a right-looking lookSide of -1 yields a positive beta along chat. isce3 spells the same choice out as (side == LookSide::Right) ? beta : -beta (Rdr2Geo.icc:97-101), which is why both forms appear below and agree.\n\n\n\n\n\n","title":"ImagePairGeometry.looksign"},{"category":"method","location":"radar/#ImagePairGeometry.rdr2geo-Tuple{Orbit, Ellipsoid, Real, Real}","page":"Radar geometry","text":"rdr2geo(orbit, el, aztime, range; height, doppler = 0.0, wavelength,\n        side, threshold = RDR2GEO_THRESHOLD, maxiter = RDR2GEO_MAXITER,\n        extraiter = RDR2GEO_EXTRAITER) -> SVector{3,Float64}\n\nGround point (lon, lat, height) in radians and meters, imaged at azimuth time aztime and slant range.\n\nheight is the terrain height above the ellipsoid, constant — which is all the reference's callers supply, via DEMInterpolator(zz) at a scalar zz (GeogridRadar.py:176, :289). A raster-backed DEM would interpolate per candidate location; the fixed-point structure is unchanged by that, but nothing in this package needs it.\n\nPorted from Rdr2Geo.icc:44-168. Returns the converged location; a failure to converge within maxiter + extraiter is not an error, matching isce3 — its NaN-on-failure branch is commented out in the source with a note that enabling it breaks tests (Rdr2Geo.icc:153-162), and the reference depends on that leniency. Use rdr2geo_converged when the distinction matters.\n\n\n\n\n\n","title":"ImagePairGeometry.rdr2geo"},{"category":"method","location":"radar/#ImagePairGeometry.rdr2geo_converged-Tuple{Orbit, Ellipsoid, Real, Real}","page":"Radar geometry","text":"rdr2geo_converged(orbit, el, aztime, range; kw...) -> Tuple{SVector{3,Float64},Bool}\n\nrdr2geo with the convergence flag alongside the location.\n\nfalse means the range residual never fell below threshold. The location is still isce3's answer in that case — see rdr2geo — so this is for a caller that wants to know, not a caller that wants a different result.\n\n\n\n\n\n","title":"ImagePairGeometry.rdr2geo_converged"},{"category":"type","location":"radar/#ImagePairGeometry.RadarCoordinate","page":"Radar geometry","text":"RadarCoordinate(; starting_range, dr, sensing_start, prf, nsamples, nlines,\n                  look_side, wavelength, orbit, incidence_angle, orbit_epoch_offset = 0.0)\n\nAn image in radar slant-range/azimuth coordinates.\n\nFields\n\nstarting_range: slant range to the first range sample, in meters.\ndr: range sample spacing in meters — the reference's rangePixelSize.\nsensing_start: azimuth time of the first line, in seconds since midnight of the acquisition day. That is the scale GeogridRadar.py:328-330 computes and the one the azimuth index is measured against; see orbit_epoch_offset for the other clock.\nprf: pulse repetition frequency in Hz, so 1/prf is the line spacing in time.\nnsamples, nlines: image width and height in pixels.\nlook_side: LookLeft or LookRight.\nwavelength: radar wavelength in meters. Used only through the Doppler term, which is zero for the zero-Doppler geometry the reference uses, so it does not affect the result — carried because rdr2geo takes it and a nonzero Doppler would.\norbit: the Orbit covering the acquisition.\nincidence_angle: scene-center incidence angle in radians, from incidence_angle. One scalar for the whole scene, as the reference computes it.\norbit_epoch_offset: seconds to add to a sensing_start-scale time to reach the orbit's scale.\n\nUnlike ProjectedCoordinate this describes the reference image alone, not an overlap: testGeogrid.py:427-470 takes every radar parameter from image 1 and the secondary only for the repeat interval. There is no radar analogue of coregister.\n\nThe two time scales\n\nsensing_start and the orbit are on different clocks, and orbit_epoch_offset is the constant between them. The reference carries the same split: it measures the azimuth index against seconds-since-midnight (geogridRadar.cpp:972) while interpolating the orbit against seconds-since-reference-epoch (:432). Keeping both means neither the index nor the interpolation needs a conversion at the point of use. See geo2rdr and REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.RadarCoordinate"},{"category":"constant","location":"radar/#ImagePairGeometry.FOOTPRINT_AZIMUTH_SAMPLES","page":"Radar geometry","text":"FOOTPRINT_AZIMUTH_SAMPLES\n\nNumber of azimuth fractions the footprint's intermediate lines are taken at: 21, of which the first and last are dropped by [1:-1] (GeogridRadar.py:153), leaving 19.\n\n\n\n\n\n","title":"ImagePairGeometry.FOOTPRINT_AZIMUTH_SAMPLES"},{"category":"constant","location":"radar/#ImagePairGeometry.FOOTPRINT_RANGE_SAMPLES","page":"Radar geometry","text":"FOOTPRINT_RANGE_SAMPLES\n\nNumber of range positions the footprint is evaluated at: 21, from np.linspace(0, numberOfSamples - 1, num=21) (GeogridRadar.py:152).\n\n\n\n\n\n","title":"ImagePairGeometry.FOOTPRINT_RANGE_SAMPLES"},{"category":"method","location":"radar/#ImagePairGeometry.footprint_bounds-Tuple{Any, RadarCoordinate}","page":"Radar geometry","text":"footprint_bounds(transform, c::RadarCoordinate; zrange = DEFAULT_ZRANGE) -> Extent\n\nBounding box, in grid coordinates, of the swath described by c.\n\ntransform maps geodetic degrees to grid coordinates, called as transform(lon, lat, h). Unlike the projected path — where the image's four corners transform directly — a radar footprint has to be solved for: each sample point is a rdr2geo call at one azimuth time and slant range.\n\nReproduces GeogridRadar.determineBbox (GeogridRadar.py:140-251), including its sampling exactly: 21 range positions on the first line, the same 21 on the last, and the two range edges of the 19 intermediate azimuth fractions — 80 positions, each at both elevations in zrange, so 160 rdr2geo solves. A coarser sampling would move the bounding box and shift the grid window, so the count is transcribed rather than chosen.\n\nzrange widens nothing for a grid on WGS84, for the reason DEFAULT_ZRANGE records, but is evaluated anyway because the reference evaluates it.\n\n\n\n\n\n","title":"ImagePairGeometry.footprint_bounds"},{"category":"method","location":"radar/#ImagePairGeometry.incidence_angle-Tuple{Orbit, Ellipsoid, Real, Real}","page":"Radar geometry","text":"incidence_angle(orbit, el, aztime, range; wavelength, side,\n                zrange = DEFAULT_ZRANGE) -> Float64\n\nIncidence angle in radians: the angle at the target between the line of sight to the satellite and the local ellipsoid normal.\n\nReproduces GeogridRadar.getIncidenceAngle (GeogridRadar.py:253-297): solve for the ground point at each elevation in zrange, take the angle there, and return the mean of the two. Averaging two elevations rather than evaluating at the terrain is the reference's choice, and since one scalar then serves every grid point through xsize, it is what the ground range spacing rests on.\n\nThe normal is the geodetic one, (cos φ cos λ, cos φ sin λ, sin φ) — not the geocentric direction the TCN basis uses.\n\n\n\n\n\n","title":"ImagePairGeometry.incidence_angle"},{"category":"method","location":"radar/#ImagePairGeometry.incidence_angle-Tuple{}","page":"Radar geometry","text":"incidence_angle(; orbit, starting_range, dr, sensing_start, prf, nsamples, nlines,\n                  look_side, wavelength, orbit_epoch_offset = 0.0,\n                  zrange = DEFAULT_ZRANGE) -> Float64\n\nScene-center incidence angle, for the parameters that will build a RadarCoordinate.\n\nSeparate from the type because the type stores the result: the reference calls getIncidenceAngle() before geogridRadar() (testGeogrid.py:487-488), so the angle is an input to the geometry rather than something derived on demand.\n\nScene center is floor(nsamples / 2) - 1 in range and floor(nlines / 2) - 1 in azimuth (GeogridRadar.py:265-266) — one sample short of the true midpoint at both, reproduced as written.\n\n\n\n\n\n","title":"ImagePairGeometry.incidence_angle"},{"category":"method","location":"radar/#ImagePairGeometry.midtime-Tuple{RadarCoordinate}","page":"Radar geometry","text":"midtime(c::RadarCoordinate) -> Float64\n\nAzimuth time of the scene's middle line, on the sensing_start clock.\n\nsensing_start + 0.5 * nlines / prf, matching geogridRadar.cpp:328, which is the initial guess every per-point solve starts from. Note this is not the expression GeogridRadar.py:346-347 uses to build the orbit-clock midpoint — see orbit_midtime.\n\n\n\n\n\n","title":"ImagePairGeometry.midtime"},{"category":"method","location":"radar/#ImagePairGeometry.nsamples-Tuple{RadarCoordinate}","page":"Radar geometry","text":"nsamples(c::RadarCoordinate)\nnlines(c::RadarCoordinate)\n\nImage width and height in pixels — range samples and azimuth lines.\n\n\n\n\n\n","title":"ImagePairGeometry.nsamples"},{"category":"method","location":"radar/#ImagePairGeometry.orbit_midtime-Tuple{RadarCoordinate}","page":"Radar geometry","text":"orbit_midtime(c::RadarCoordinate) -> Float64\n\nScene midpoint on the orbit's clock, as GeogridRadar.py:346-347 computes it: sensing_start + (floor(nlines / 2) - 1) / prf, shifted by orbit_epoch_offset.\n\nDeliberately a different expression from midtime, and the difference reaches the output. Both are offsets from sensing_start on their own clocks, so the epoch cancels from midtime - orbit_midtime, leaving 0.5 * nlines / prf against (floor(nlines / 2) - 1) / prf — one pulse interval apart for an even line count.\n\ngeo2rdr preserves that difference, so the converged azimuth time corresponds to a satellite position one pulse from where the orbit was interpolated, and the azimuth index is one line off the geometrically consistent value. Reproduced rather than reconciled: the reference's products contain that offset. See REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.orbit_midtime"},{"category":"method","location":"radar/#ImagePairGeometry.sensing_stop-Tuple{RadarCoordinate}","page":"Radar geometry","text":"sensing_stop(c::RadarCoordinate) -> Float64\n\nAzimuth time of the last line: sensing_start + (nlines - 1) / prf (GeogridRadar.py:193).\n\n\n\n\n\n","title":"ImagePairGeometry.sensing_stop"},{"category":"method","location":"radar/#ImagePairGeometry.xsize-Tuple{RadarCoordinate}","page":"Radar geometry","text":"xsize(c::RadarCoordinate)\nysize(c::RadarCoordinate)\n\nGround pixel size along each axis, in meters: dr / sin(incidence) across track and |v| / prf along it.\n\nMatches geogridRadar.cpp:684-686, which reports these as grd_res/azm_res where the projected path reports X_res/Y_res. So the chip-size conversion consumes them identically on both paths — these are the radar equivalents of a geotransform's pixel size, recovered from geometry because there is no geotransform to read them off.\n\nThe azimuth size uses the platform speed at scene center, so it is a single value for the scene rather than a per-point quantity. Specifically it uses satvmid (geogridRadar.cpp:686), which is interpolated at tmidd (:435) — the orbit-clock midpoint from orbit_midtime, not the sensing_start-clock one from midtime. The two differ by a pulse interval, so the choice changes azm_res in the last bits.\n\n\n\n\n\n","title":"ImagePairGeometry.xsize"},{"category":"method","location":"radar/#ImagePairGeometry.y_displacement_sign-Tuple{RadarCoordinate}","page":"Radar geometry","text":"y_displacement_sign(c::RadarCoordinate)\n\n-1.0: azimuth increases along the track, while a north-up raster's +y points down, so the two conventions oppose.\n\n\n\n\n\n","title":"ImagePairGeometry.y_displacement_sign"},{"category":"type","location":"radar/#ImagePairGeometry.RadarSpacing","page":"Radar geometry","text":"RadarSpacing\n\nThe nominal spacings the radar outputs divide by, which are not one pair.\n\nFields\n\noperator: (dr, |da|), for offset_to_velocity and the azimuth scale_factors denominator. da is the along-track step in ECEF meters (geogridRadar.cpp:1159).\nsearch: (dr, |alt|), for search_pixels. alt is the same step in grid coordinates.\n\nalt and da are one displacement expressed in two frames, so their norms differ — by the local map scale, around 0.04% in UTM. The reference divides by norm(da) at :1166-1176 and :1191 and by norm(alt) at :1207-1216, and that difference is reproduced rather than reconciled: it changes the last bits of the operator relative to the search extent.\n\nThe projected path has no such split — one spacing serves every output there — which is why the kernel takes spacings as values rather than reading them off a coordinate.\n\n\n\n\n\n","title":"ImagePairGeometry.RadarSpacing"},{"category":"type","location":"radar/#ImagePairGeometry.SceneCenterStart","page":"Radar geometry","text":"SceneCenterStart()\nSceneCenterStart(c::RadarCoordinate)\n\nStart every zero-Doppler solve from the scene midpoint, as the reference does (geogridRadar.cpp:906-911). See ZeroDopplerStart.\n\nThe one-argument form precomputes the satellite state at that midpoint. It is a scene constant, so interpolating it per grid point — as a literal transcription does — repeats one orbit interpolation across every point of the window for a value that cannot change. The per-point loop builds this form once; the no-argument form interpolates on demand, so a direct pointgeometry call needs no setup. Both give the same bits.\n\n\n\n\n\n","title":"ImagePairGeometry.SceneCenterStart"},{"category":"type","location":"radar/#ImagePairGeometry.WarmStart","page":"Radar geometry","text":"WarmStart(; iterations = 8)\n\nStart each zero-Doppler solve from the previous grid point's answer, running iterations iterations instead of GEO2RDR_ITERATIONS.\n\nAdjacent grid points at ITS_LIVE spacings are a few hundredths of an azimuth line apart, so the previous point's solution is a far better initial guess than the scene midpoint and the solve needs roughly half as many iterations. Worth about 1.2× of the per-point radar cost.\n\nThis trades away bit-identical blocking and threading\n\nThe result becomes a function of the order points are visited in, so a window computed in one piece and the same window computed as blocks no longer agree bitwise — the first point of each block has no predecessor and cold-starts, and every point after it inherits a different starting guess. That property is what pairgeometry_blocked rests on and what test/blocks.jl asserts across block sizes from (1, 1) upward, which is why this is opt-in and SceneCenterStart remains the default.\n\nPer-block seeding does not rescue it: a (1, 1) block is a cold start by construction, so no seeding rule makes a blocked run agree with an unblocked one.\n\nHow far the answer moves\n\nMeasured over a 64×64 block walked in the loop's own order, against the cold GEO2RDR_ITERATIONS result, on the fixture's acquisition:\n\niterations indices moved float bands vs isce3 fixture gate (bound 2e-4)\n5 0 7.3e-4 fails\n6 0 1.6e-4 passes, little room\n8 0 1.07e-4 passes — the cold-start value\n10 0 1.07e-4 passes\n\nNo integer band moves at any of these counts, on any of the eight fixture cases. What sets the floor is the off2vel float bands: they divide by the along-track step, so they inherit the azimuth time's error without a rounding step to absorb it. At 8 they recover the cold-start agreement exactly, which is why that is both the default and the minimum — see WARM_START_MIN_ITERATIONS.\n\nGrid spacing barely matters\n\nThe count needed depends on how far apart successive points are, which is the grid spacing — but the dependence is far weaker than it looks, and 8 covers the whole practical range. At 10 km spacing the gap between successive points is 645 azimuth lines, or 1.3 s of azimuth time, against the roughly 60 s a cold start begins from: still more than an order of magnitude closer, which at the measured rate is about four iterations' worth. Measured float-band divergence from the cold result, which is flat:\n\ngrid spacing iterations = 8 iterations = 10\n120 m (ITS_LIVE) 2.8e-7 1.2e-8\n500 m 4.9e-7 9.3e-9\n1 km 5.3e-7 9.0e-9\n10 km (a sparse pass) 4.5e-7 8.6e-9\n\nNo integer band moves at any spacing at any of these counts. So a sparse pass for later interpolation onto a finer grid — where points are deliberately far apart — is as safe as a dense one.\n\n\n\n\n\n","title":"ImagePairGeometry.WarmStart"},{"category":"type","location":"radar/#ImagePairGeometry.ZeroDopplerSeed","page":"Radar geometry","text":"ZeroDopplerSeed\n\nThe running state a WarmStart carries from one grid point to the next: the previous point's azimuth time on both clocks and the satellite state there, or nothing before the first point.\n\nMutable because the per-point loop updates it in place; one instance per loop, so a threaded run's tasks never share one. See WarmStart on why the sequence matters.\n\n\n\n\n\n","title":"ImagePairGeometry.ZeroDopplerSeed"},{"category":"type","location":"radar/#ImagePairGeometry.ZeroDopplerStart","page":"Radar geometry","text":"ZeroDopplerStart\n\nWhere pointgeometry starts the zero-Doppler solve, and how many iterations it runs.\n\nSceneCenterStart() is the reference's behavior and the default: every point starts from the scene midpoint, GEO2RDR_ITERATIONS iterations. WarmStart starts each point from the previous point's answer instead, which converges in fewer iterations because adjacent grid points are a small fraction of an azimuth line apart.\n\n\n\n\n\n","title":"ImagePairGeometry.ZeroDopplerStart"},{"category":"constant","location":"radar/#ImagePairGeometry.RANGE_DOPPLER_ITERATIONS","page":"Radar geometry","text":"RANGE_DOPPLER_ITERATIONS\n\nFixed iteration count for the range-Doppler solve: 6. The reference uses 10 (geogridRadar.cpp:1038).\n\nAs with GEO2RDR_ITERATIONS there is no convergence test, and here the residual rdiff is computed (:1070) and never read. Contrast isce3's rdr2geo, which breaks on dr < 1e-8 — this is a separate transcription for that reason, not a call into rdr2geo.\n\nWhy 6\n\nThis is a different fixed point from geo2rdr's and converges by a different mechanism. The iterate is zsch, the target's height above the sphere osculating the ellipsoid beneath the satellite. Perturbing it moves the target along the range circle, which changes its latitude, which changes the geocentric radius of the constant-geodetic-height surface. So the rate is governed by dr_t/dlat = -a f sin(2 lat) — it vanishes at the equator and at the pole and peaks near 45°, and it is small: about 6e-4, against geo2rdr's 0.09. Three iterations gain nine digits.\n\nMeasured over 1320 geometries — altitudes 400 to 1000 km, latitudes 0 to 80° on a near-polar orbit, incidence 15 to 55°, terrain height 0 and 4000 m, both look sides — the worst target displacement from the 30-iteration answer:\n\ncount worst displacement as range samples\n3 1.8e-2 m 7.6e-3\n4 7.7e-5 m 3.3e-5\n5 3.3e-7 m 1.4e-7\n6 7.8e-9 m 3.4e-9\n8 8.3e-9 m 3.6e-9\n10 8.6e-9 m 3.7e-9\n\n6 is where the iteration reaches its floor: about 1e-8 m, one ULP of norm3 at Earth radius carried through the lon/lat round trip. Counts above 6 do not refine the answer — they land on different noise, which is why 7, 8 and 10 are no closer to the fixed point than 6 is.\n\nThe requirement is 4, set by the range-sphere property test/radar_geometry.jl asserts at rtol = 1e-9: over the same 1320 geometries a count of 3 violates it on 220 of them, and 4 clears it with a worst residual of 1.9e-11. So 6 carries two iterations of margin over the requirement while sitting on the floor.\n\nLatitude is the governing parameter, not altitude as it is for GEO2RDR_ITERATIONS — and the worst-case latitudes are the mid ones, so a polar ITS_LIVE grid and the equatorial fixture are both easier than the 30–45° band. Terrain height changes the requirement by less than one iteration from the Dead Sea to Everest, and altitude by less than one over the whole 400–1000 km range.\n\n\n\n\n\n","title":"ImagePairGeometry.RANGE_DOPPLER_ITERATIONS"},{"category":"constant","location":"radar/#ImagePairGeometry.WARM_START_MIN_ITERATIONS","page":"Radar geometry","text":"WARM_START_MIN_ITERATIONS\n\nFewest iterations a WarmStart accepts: 8.\n\nSet by the float bands, not the integer ones. No rounded index moves at any count from 5 upward, so an integer-only check would permit 5 — but the off2vel bands, which divide by the along-track step and so inherit the azimuth time's error directly, miss the isce3 reference by 7.3e-4 relative at 5 and 1.6e-4 at 6, against the 2e-4 test/radar_geogrid.jl asserts. At 8 the agreement is 1.07e-4, which is the cold-start value: the warm start has stopped contributing any error of its own.\n\n\n\n\n\n","title":"ImagePairGeometry.WARM_START_MIN_ITERATIONS"},{"category":"function","location":"radar/#ImagePairGeometry.pointgeometry","page":"Radar geometry","text":"pointgeometry(tf::TransformPair, gx, gy, gz, c::RadarCoordinate, normal)\n    -> Tuple{PointGeometry,RadarSpacing,RadarPoint}\n\nGeometry at the grid point (gx, gy, gz) for the radar acquisition c.\n\ntf.forward maps grid coordinates to geodetic degrees and tf.inverse back — the opposite naming sense from the projected path's image coordinates, but the same direction of use: forward to leave the grid, inverse to return to it.\n\nReturns the PointGeometry the shared outputs consume, the RadarSpacing they divide by, and the RadarPoint the range and azimuth indices come from.\n\nReproduces geogridRadar.cpp:850-1085. Three solves per point — one geo2rdr, then two range-Doppler iterations for the along-track step — so this is roughly two orders of magnitude more expensive than the projected path's three transform calls.\n\n\n\n\n\n","title":"ImagePairGeometry.pointgeometry"},{"category":"section","location":"#ImagePairGeometry","page":"Home","text":"Geometry of a co-registered image pair on a map-projected grid.\n\nFor each point of a grid defined in projected coordinates (northing/easting), the geometry of a pair of images relates map coordinates to image coordinates in both directions:\n\nforward: grid point (x, y, z) to pixel index in each image of the pair,\ninverse: a pixel displacement between the two images to a velocity in map coordinates.\n\nDerived per grid point alongside that mapping: the expected displacement implied by a reference velocity field, the search extent in pixels implied by a search-range field, chip size converted from meters to pixels, and the scale factors relating image-space distance to ground distance.\n\nThe pixel displacement itself is estimated elsewhere, by a feature-tracking or correlation routine; this package supplies the geometry that routine searches within and the operator that converts its result to velocity.\n\nA pure-Julia reimplementation of the geogrid module of NASA JPL's autoRIFT, the geometry layer beneath the ITS_LIVE glacier velocity products. REFERENCE.md records the reference implementation, the exactness standard held against it, and every deliberate divergence.","title":"ImagePairGeometry"},{"category":"section","location":"#Status","page":"Home","text":"Both coordinate paths are implemented and verified against the compiled reference: ProjectedCoordinate for imagery on a map projection, and RadarCoordinate for slant-range/azimuth imagery. pairgeometry and pairgeometry_blocked take either, and produce the reference's nine outputs on both.\n\nThe per-point kernel and the output expressions are shared. What differs is how the forward mapping is obtained — a coordinate transform against an orbit and a range–Doppler solve — and which spacings the outputs divide by. See Radar geometry.\n\nREFERENCE.md records the exactness standard held on each path, including two radar float bands that agree to 1.07e-4 rather than bitwise, and the measurements that place the cause outside this package.","title":"Status"},{"category":"section","location":"#Walkthrough","page":"Home","text":"Two images, a grid from a DEM, and the per-point geometry of the pair:\n\njulia> using ImagePairGeometry\n\njulia> reference = ImageFootprint(origin = (300000.0, 7800000.0), spacing = (30.0, -30.0),\n                                 size = (400, 400));\n\njulia> secondary = ImageFootprint(origin = (300600.0, 7799400.0), spacing = (30.0, -30.0),\n                                  size = (400, 400));\n\ncoregister intersects the two footprints. The overlap — not either image alone — supplies the origin and size that pixel indices are relative to, so the secondary image determines them as much as the reference does:\n\njulia> pair = coregister(reference, secondary; dt = 91 * 86400.0);\n\njulia> pair.coordinate.origin, pair.coordinate.size\n((300600.0, 7.7994e6), (380, 380))\n\njulia> pair.reference_offset, pair.secondary_offset\n((20, 20), (0, 0))\n\nA grid, then the part of it the pair covers:\n\njulia> grid = MapGrid(geotransform = (295000.0, 120.0, 0.0, 7805000.0, 0.0, -120.0),\n                      size = (200, 200), crs = 32624);\n\njulia> window = grid_window(grid, footprint_bounds(IdentityTransform(), pair.coordinate))\nCartesianIndices((47:142, 47:142))\n\nThe inputs cover that window. Only the DEM is required; each other raster gates the outputs that depend on it:\n\njulia> n = size(window);\n\njulia> inputs = GeometryInputs(dem = fill(500.0, n),\n                               dhdx = fill(0.02, n), dhdy = fill(-0.01, n),\n                               vx = fill(120.0, n), vy = fill(-80.0, n));\n\njulia> result = pairgeometry(grid, pair, inputs; window);\n\njulia> size(result), nvalid(result)\n((96, 96), 9025)\n\nThe window is a conservative bound on the footprint, so its outermost points can fall outside the image. Those are nodata rather than an error:\n\njulia> result.location_x[1, 1] == -32767\ntrue\n\njulia> k = CartesianIndex(50, 50);   # comfortably inside\n\njulia> result.location_x[k], result.location_y[k]\n(195, 195)\n\njulia> result.offset_x[k], result.offset_y[k]     # expected displacement, pixels\n(1, 1)\n\njulia> result.scale_x[k], result.scale_y[k]       # exactly 1: grid and image share a CRS\n(1.0, 1.0)\n\nThe operator converts a pixel displacement to a map velocity. Here a displacement of two pixels in x and minus one in y:\n\njulia> vx = result.off2vx_dx[k] * 2.0 + result.off2vx_dy[k] * -1.0;\n\njulia> vy = result.off2vy_dx[k] * 2.0 + result.off2vy_dy[k] * -1.0;\n\njulia> round(vx, digits = 2), round(vy, digits = 2)\n(240.66, -120.33)","title":"Walkthrough"},{"category":"section","location":"#Larger-grids","page":"Home","text":"pairgeometry_blocked computes the same result block by block, optionally across threads. It is bit-identical to the unblocked run at any block size and task count, because every grid point depends only on its own inputs:\n\nsource = InMemoryInputs(inputs, window)\nresult = pairgeometry_blocked(grid, pair, source;\n                              transform = fast_transform(3413, 32624),\n                              blocksize = (512, 512), ntasks = 8)\n\nOne transform object serves every task: a fast_transform pair is immutable and holds no library state, so nothing has to be built per task.\n\nWhere the grid and the imagery share a CRS, pass IdentityTransform instead — it is exact by construction and skips the projection entirely.\n\nA radar pair transforms between the grid CRS and geodetic degrees rather than between two projected CRSs, which needs no different call — fast_transform returns longitude/easting first by default:\n\nresult = pairgeometry_blocked(grid, pair, source;\n                              transform = fast_transform(32632, 4326),\n                              blocksize = (512, 512), ntasks = 8)","title":"Larger grids"},{"category":"section","location":"#Coordinate-transforms","page":"Home","text":"fast_transform returns a TransformPair evaluated by FastGeoProjections, for the EPSG pairs it implements natively — polar stereographic, UTM, and the geographic and geocentric systems. This is the only transform the package provides: nothing in src/ links a projection library, so there is no PROJ dependency and no PROJ context to own per thread.\n\ntf = fast_transform(3413, 32624)\nr = pairgeometry_blocked(grid, pair, source; transform = tf, ntasks = 8)\n\nOn the projected path it is 178 ns/point against PROJ's 445, measured on the kernel's own call pattern — and there the transform is most of the run, so that ratio is close to the run's. On the radar path the two geometry solves dominate and the transform is around 13% of a point, so the choice is worth a few percent either way; measured on EPSG:32632 to EPSG:4326 the native forward is in fact slightly slower than PROJ's. Pick it for having no dependency, not for speed.","title":"Coordinate transforms"},{"category":"section","location":"#Axis-order","page":"Home","text":"always_xy defaults to true: a coordinate pair is (easting, northing) or (longitude, latitude), whatever the CRS's authority order says.\n\nFor a projected-to-projected pair the flag is inert — the EPSG codes this package is used with order a projected pair easting-first regardless — so it changes nothing the reference fixtures pin. Where it decides something is a geographic CRS: EPSG:4326's authority order is (latitude, longitude).\n\njulia> fast_transform(32632, 4326).forward(-242500.0, 2179000.0, 500.0)\n(1.9350326147365111, 19.567422331060865, 500.0)          # (lon, lat) -- the default\n\njulia> fast_transform(32632, 4326; always_xy = false).forward(-242500.0, 2179000.0, 500.0)\n(19.567422331060865, 1.9350326147365111, 500.0)          # (lat, lon) -- authority order\n\nThe default is not the reference's, which uses osr.CoordinateTransformation on SRSs from ImportFromEPSG — authority order. It is chosen against the failure mode instead: the radar path maps grid coordinates to geodetic degrees and reads the result as (lon, lat, h), so an authority-order transform swaps the two and produces geometry that is wrong everywhere without raising anything — the solve converges happily against a target on the wrong side of the planet. Defaulting to the order the kernel wants removes a silent failure whose only symptom is bad output.\n\nPass always_xy = false to reproduce authority order deliberately. If you build a TransformPair from PROJ yourself, note that Proj.Transformation defaults the other way, so a radar pair needs always_xy = true passed explicitly there.\n\nBoth CRSs must be given as EPSG codes, since FastGeoProjections resolves a transformation by code rather than parsing a description. For a WKT or PROJ string, or a code it does not implement, build a TransformPair from any library that does — Proj.jl among them — and pass that:\n\nusing Proj\ntf = TransformPair(Proj.Transformation(\"EPSG:3413\", \"EPSG:32624\"),\n                   Proj.Transformation(\"EPSG:32624\", \"EPSG:3413\"))\n\nA Proj.Transformation wraps a context that is not safe to share between threads, so a threaded run must pass a zero-argument factory returning a fresh pair rather than one built up front. The test suite does this — Proj.jl is a test dependency, since the reference fixtures were generated through PROJ and are asserted against it.","title":"Axis order"},{"category":"section","location":"#Approximating-the-projection","page":"Home","text":"Where the CRSs differ, the projection is most of a projected run: three calls per grid point. InterpolatedTransform tabulates the transform on a coarse lattice and interpolates between the nodes, with a selectable kernel — Bilinear, Bicubic or NearestNode.\n\ntf = InterpolatedTransform(fast_transform(3413, 32624), grid, pair;\n                           lattice = 4, mode = :hybrid, interpolation = Bilinear(),\n                           window = window)\nresult = pairgeometry_blocked(grid, pair, source; transform = tf, window = window, ntasks = 8)\n\nlattice is the node spacing as a multiple of the grid spacing. The two modes differ in which bands they leave exact:\n\n:hybrid interpolates only the two inverse calls. The pixel-location bands come from the forward transform alone, so they stay bitwise identical to the exact path. Saturates around 1.6×, since the one remaining call is the floor.\n:full interpolates both directions, for up to 6.9×. The location bands can then differ by one pixel — the positional error is far below a pixel, but it can move a point across a rounding boundary.\n\nIt is an AbstractTransformFactory, so a blocked run calls it once per task and each task owns its own lattice. The result does not depend on the block size: the lattice is built from the whole window's bounds.\n\nThe exact path is the default. docs/interpolated-transform.md records the measured cost and accuracy of each mode, spacing and band.","title":"Approximating the projection"},{"category":"section","location":"#API","page":"Home","text":"Everything exported, plus the internals the reference-fidelity notes refer to. Grouped by the file each is defined in, which follows the layering: vector and rounding primitives, then coordinates and transforms, then the grid, then the kernel, then the driver.\n\nThe radar path's own numerics — the ellipsoid, orbit interpolation, and the two geometry solves — are on the Radar geometry page.","title":"API"},{"category":"section","location":"#Raster-IO","page":"Home","text":"Available when Rasters, ArchGDAL, DimensionalData and DiskArrays are loaded. Reads inputs a window at a time from disk-backed rasters, and writes the reference's nine GeoTIFFs.","title":"Raster IO"},{"category":"section","location":"#Correlator-handoff","page":"Home","text":"velocity_conversion gathers the per-point quantities that turn a measured pixel displacement into a map velocity: the operator, the scale factors, the stable-surface mask, the chip-size aspect ratio, and y_displacement_sign — the factor a y displacement needs when the image's second axis opposes north, as a radar image's azimuth does.\n\nBuilding the correlator's search grid is the correlator's own concern, so it lives there rather than here. AutoRIFT adds a pointset(::PairGeometry) method when both packages are loaded, which reindexes the grid to one-based and turns a nodata point into the zero search radius it skips on. This package has no dependency on it in either direction.","title":"Correlator handoff"},{"category":"section","location":"#Index","page":"Home","text":"","title":"Index"},{"category":"method","location":"#ImagePairGeometry.cross3-Tuple{StaticArraysCore.SVector{3, Float64}, StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"cross3(u, v) -> SVector{3,Float64}\n\nCross product, matching cross_C (geogridOptical.cpp:1014-1018).\n\n\n\n\n\n","title":"ImagePairGeometry.cross3"},{"category":"method","location":"#ImagePairGeometry.dot3-Tuple{StaticArraysCore.SVector{3, Float64}, StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"dot3(v, w) -> Float64\n\nDot product summed left to right, matching dot_C (geogridOptical.cpp:1008-1012).\n\nNot LinearAlgebra.dot, whose reduction order is unspecified and which may contract to an fma.\n\n\n\n\n\n","title":"ImagePairGeometry.dot3"},{"category":"method","location":"#ImagePairGeometry.norm3-Tuple{StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"norm3(v) -> Float64\n\nEuclidean norm as sqrt of the left-to-right sum of squares, matching norm_C (geogridOptical.cpp:1020-1024).\n\nUnlike LinearAlgebra.norm this does no scaling to avoid overflow. The reference does none either, and the inputs here are map coordinates and unit-scaled differences, orders of magnitude away from overflowing.\n\n\n\n\n\n","title":"ImagePairGeometry.norm3"},{"category":"method","location":"#ImagePairGeometry.unitvec3-Tuple{StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"unitvec3(v) -> SVector{3,Float64}\n\nv scaled to unit length by dividing each component by norm3, matching unitvec_C (geogridOptical.cpp:1026-1032).\n\nNot LinearAlgebra.normalize, which multiplies by the reciprocal norm — a different result in the last bit.\n\nA zero vector yields NaN components rather than an error, as the reference does. The zero case is reachable: the surface normal is set to all-zeros when no slope raster is supplied (geogridOptical.cpp:761-764).\n\n\n\n\n\n","title":"ImagePairGeometry.unitvec3"},{"category":"method","location":"#ImagePairGeometry.cround-Tuple{Float64}","page":"Home","text":"cround(x::Float64) -> Float64\n\nRound half away from zero, as C's std::round does: cround(0.5) == 1.0, cround(-0.5) == -1.0.\n\nJulia's round rounds half to even, so it returns 0.0 for both. Use this wherever the reference calls std::round, and nround wherever it calls np.round.\n\n\n\n\n\n","title":"ImagePairGeometry.cround"},{"category":"method","location":"#ImagePairGeometry.cround32-Tuple{Float64}","page":"Home","text":"cround32(x::Float64) -> Int32\n\nstd::round followed by C's implicit conversion to GInt32, as in\n\nraster11[jj] = std::round(...);   // GInt32 = double\n\nBecause cround has already produced an integral value the subsequent truncation is the identity, so this is a rounding conversion — unlike ctrunc32.\n\nThrows InexactError if the result is not representable as Int32. The reference instead relies on undefined behavior, which clang implements on arm64 as saturation; failing loudly is the deliberate divergence, recorded in REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.cround32"},{"category":"method","location":"#ImagePairGeometry.ctrunc32-Tuple{Float64}","page":"Home","text":"ctrunc32(x::Float64) -> Int32\n\nC's implicit double to GInt32 conversion with no rounding applied first: truncation toward zero, so ctrunc32(22.99) == 22.\n\nThe reference uses this — not rounding — for chip size and the stable-surface mask (geogridOptical.cpp:874-875, 884-885, 893), while applying std::round first for the offset and search-range outputs (:807-808, 849-863). Substituting a rounding conversion here shifts chip sizes by one pixel.\n\nThrows InexactError outside the Int32 range, as cround32 does and for the same reason.\n\n\n\n\n\n","title":"ImagePairGeometry.ctrunc32"},{"category":"method","location":"#ImagePairGeometry.nround-Tuple{Float64}","page":"Home","text":"nround(x::Float64) -> Float64\n\nRound half to even, as NumPy's np.round does. An alias for Julia's round, spelled out so that call sites reproducing Python arithmetic say which reference they follow.\n\nThe reference's coregister uses np.round; its C++ kernel uses std::round. See cround.\n\n\n\n\n\n","title":"ImagePairGeometry.nround"},{"category":"type","location":"#ImagePairGeometry.AbstractImageCoordinate","page":"Home","text":"AbstractImageCoordinate\n\nThe coordinate system of an image, determining how a map-projected grid point maps to a pixel index in it.\n\nSubtypes: ProjectedCoordinate, RadarCoordinate.\n\nThe interface a subtype provides: nsamples and nlines for the image size, xsize and ysize for the ground pixel size along each axis, y_displacement_sign for how its second axis relates to north, and a footprint_bounds method. That is what CoregisteredPair and the chip-size conversion consume, and it is deliberately small: the two coordinate systems have almost nothing in common structurally — one is an origin, a spacing and a size, the other a range axis, a time axis and an orbit — so what they share is the handful of quantities the kernel actually asks for rather than a common representation.\n\n\n\n\n\n","title":"ImagePairGeometry.AbstractImageCoordinate"},{"category":"type","location":"#ImagePairGeometry.ProjectedCoordinate","page":"Home","text":"ProjectedCoordinate(; origin, spacing, size)\n\nAn image on a map projection, indexed by an affine relation between projected coordinates and pixel indices.\n\nFields\n\norigin: projected coordinate of the first pixel's center, as (x, y). Geogrid's startingX/startingY.\nspacing: signed pixel size, as (x, y). Negative y for the usual north-up raster. Signed rather than absolute because the index calculation divides by it, so the sign carries the axis direction.\nsize: (samples, lines), i.e. (ncolumns, nrows).\n\nFor an image pair these describe the overlap of the two images, which is what coregister computes and what pixel indices are relative to. Constructing one from a single image's geotransform is correct only when that image is the overlap.\n\nThe CRS is deliberately absent: it enters through the coordinate transform passed to the kernel, so the same coordinate system can be paired with a transform from any grid CRS.\n\n\n\n\n\n","title":"ImagePairGeometry.ProjectedCoordinate"},{"category":"method","location":"#ImagePairGeometry._as_geoformat-Tuple{Nothing}","page":"Home","text":"_as_geoformat(crs) -> Union{Nothing,GFT.GeoFormat}\n\nA CRS in the form the GeoJulia packages pass around: a GeoFormatTypes.GeoFormat, or nothing.\n\nAn Integer is read as an EPSG code, since that is how a geogrid case is usually specified and how the reference's own parameters arrive. Anything already a GeoFormat passes through, so a caller holding a WKT or a PROJ string is not forced to convert. Normalising here rather than at each consumer means GeoInterface.crs has one type to return and the Rasters extension has one to write.\n\n\n\n\n\n","title":"ImagePairGeometry._as_geoformat"},{"category":"function","location":"#ImagePairGeometry.nlines","page":"Home","text":"nsamples(c::AbstractImageCoordinate) -> Int\nnlines(c::AbstractImageCoordinate) -> Int\n\nImage width and height in pixels — columns and rows for a projected image, range samples and azimuth lines for a radar one.\n\nPart of the AbstractImageCoordinate interface, and what the bounds test on a computed pixel index compares against.\n\n\n\n\n\n","title":"ImagePairGeometry.nlines"},{"category":"function","location":"#ImagePairGeometry.nsamples","page":"Home","text":"nsamples(c::AbstractImageCoordinate) -> Int\nnlines(c::AbstractImageCoordinate) -> Int\n\nImage width and height in pixels — columns and rows for a projected image, range samples and azimuth lines for a radar one.\n\nPart of the AbstractImageCoordinate interface, and what the bounds test on a computed pixel index compares against.\n\n\n\n\n\n","title":"ImagePairGeometry.nsamples"},{"category":"method","location":"#ImagePairGeometry.nsamples-Tuple{ProjectedCoordinate}","page":"Home","text":"nsamples(c::ProjectedCoordinate)\nnlines(c::ProjectedCoordinate)\n\nThe two components of the coordinate's size.\n\n\n\n\n\n","title":"ImagePairGeometry.nsamples"},{"category":"method","location":"#ImagePairGeometry.spacing-Tuple{ProjectedCoordinate}","page":"Home","text":"spacing(c::ProjectedCoordinate) -> NTuple{2,Float64}\n\nThe nominal pixel spacing as a Float64 pair, signed — negative y for a north-up raster.\n\nWhat the output expressions divide by, so the sign matters: xsize and ysize are the absolute values and are what the chip-size conversion wants instead.\n\nNot part of the AbstractImageCoordinate interface. A radar image has no single spacing to return — its along-track step is per grid point — so the kernel takes the pair as an argument and each path supplies it its own way.\n\n\n\n\n\n","title":"ImagePairGeometry.spacing"},{"category":"function","location":"#ImagePairGeometry.xsize","page":"Home","text":"xsize(c::AbstractImageCoordinate) -> Float64\nysize(c::AbstractImageCoordinate) -> Float64\n\nGround pixel size along each image axis, in meters — geogrid's X_res/Y_res, reported in its scalar output and used downstream to convert a chip size in meters to pixels.\n\nPart of the AbstractImageCoordinate interface. A projected image reads them off its spacing; a radar image derives them from the incidence angle and the platform speed, since it has no geotransform to read. Both are absolute, so the axis direction is not carried here.\n\n\n\n\n\n","title":"ImagePairGeometry.xsize"},{"category":"method","location":"#ImagePairGeometry.xsize-Tuple{ProjectedCoordinate}","page":"Home","text":"xsize(c::ProjectedCoordinate)\nysize(c::ProjectedCoordinate)\n\nThe absolute value of the signed pixel spacing along each axis.\n\n\n\n\n\n","title":"ImagePairGeometry.xsize"},{"category":"function","location":"#ImagePairGeometry.y_displacement_sign","page":"Home","text":"y_displacement_sign(c::AbstractImageCoordinate) -> Float64\n\nThe factor relating a y displacement along c's second image axis to one pointing north: -1.0 where they oppose, +1.0 where they agree.\n\nPart of the AbstractImageCoordinate interface, and needed at both ends of a correlation — on the prior handed to the correlator and on the displacement it returns. The reference applies it at testautoRIFT.py:405-407 and :790-791, both guarded on optical_flag == 0.\n\nA property of the coordinate system rather than a choice, so it dispatches instead of taking a flag: a wrong sign describes motion in the opposite direction and stays entirely plausible, which is why this is answered here once rather than at each call site.\n\n\n\n\n\n","title":"ImagePairGeometry.y_displacement_sign"},{"category":"method","location":"#ImagePairGeometry.y_displacement_sign-Tuple{ProjectedCoordinate}","page":"Home","text":"y_displacement_sign(c::ProjectedCoordinate)\n\n+1.0. A north-up raster's +y already points down its own second axis, so a displacement in image axes needs no adjustment.\n\n\n\n\n\n","title":"ImagePairGeometry.y_displacement_sign"},{"category":"function","location":"#ImagePairGeometry.ysize","page":"Home","text":"xsize(c::AbstractImageCoordinate) -> Float64\nysize(c::AbstractImageCoordinate) -> Float64\n\nGround pixel size along each image axis, in meters — geogrid's X_res/Y_res, reported in its scalar output and used downstream to convert a chip size in meters to pixels.\n\nPart of the AbstractImageCoordinate interface. A projected image reads them off its spacing; a radar image derives them from the incidence angle and the platform speed, since it has no geotransform to read. Both are absolute, so the axis direction is not carried here.\n\n\n\n\n\n","title":"ImagePairGeometry.ysize"},{"category":"type","location":"#ImagePairGeometry.AbstractCoordTransform","page":"Home","text":"AbstractCoordTransform\n\nA transform between coordinate systems, callable as t(x, y, z) -> (x′, y′, z′).\n\nSubtypes: IdentityTransform, AffineTransform. A Proj.Transformation also satisfies the interface and is what a georeferenced caller passes; it is not a subtype because it belongs to another package.\n\n\n\n\n\n","title":"ImagePairGeometry.AbstractCoordTransform"},{"category":"type","location":"#ImagePairGeometry.AbstractTransformFactory","page":"Home","text":"AbstractTransformFactory\n\nBuilds a fresh TransformPair each time it is called, as factory().\n\nA blocked run calls it once per task, so each task owns a transform for its lifetime. That is what Proj needs: a Proj.Transformation wraps a PJ* on a context that PROJ documents as usable from one thread at a time, and building two concurrently on the shared global context corrupts its SQLite handle. A caller wrapping such a library supplies the factory, and InterpolatedTransform is one that returns a lattice approximation.\n\n\n\n\n\n","title":"ImagePairGeometry.AbstractTransformFactory"},{"category":"type","location":"#ImagePairGeometry.AffineTransform","page":"Home","text":"AffineTransform(; a, b, c, d, e, f, scale_z = 1.0)\n\nThe planar affine map (x, y) -> (a*x + b*y + c, d*x + e*y + f), with z scaled by scale_z.\n\nExists for testing: a rotation, scaling and translation has an exactly known inverse and exactly known axis unit vectors, so it pins the kernel's arithmetic without reference to PROJ or to the reference implementation. Not a substitute for a map projection — it cannot represent one.\n\n\n\n\n\n","title":"ImagePairGeometry.AffineTransform"},{"category":"type","location":"#ImagePairGeometry.IdentityTransform","page":"Home","text":"IdentityTransform()\n\nThe transform between a coordinate system and itself.\n\nReturns its argument unchanged, which is what PROJ's +proj=noop pipeline does for two identical CRSs — bit for bit, including the vertical coordinate. Dispatching on this type rather than calling PROJ makes the same-CRS case exact by construction and removes three transform calls per grid point.\n\n\n\n\n\n","title":"ImagePairGeometry.IdentityTransform"},{"category":"type","location":"#ImagePairGeometry.TransformPair","page":"Home","text":"TransformPair(forward, inverse)\n\nThe forward and inverse transforms between a grid CRS and an image CRS, held together.\n\nforward maps grid coordinates to image coordinates; inverse maps back. Construct from a single transform with transform_pair, which derives the inverse, or pass both explicitly when they come from a library that builds each separately.\n\n\n\n\n\n","title":"ImagePairGeometry.TransformPair"},{"category":"method","location":"#ImagePairGeometry.inverse-Tuple{IdentityTransform}","page":"Home","text":"inverse(t::AbstractCoordTransform)\n\nThe transform in the opposite direction.\n\nThe kernel needs both directions — forward to find a pixel index, inverse to find where a one-pixel step in the image lands on the grid — and takes them as one object so a caller cannot supply a mismatched pair.\n\n\n\n\n\n","title":"ImagePairGeometry.inverse"},{"category":"method","location":"#ImagePairGeometry.isidentity-Tuple{IdentityTransform}","page":"Home","text":"isidentity(t) -> Bool\n\nWhether t is the identity.\n\nA predicate for inspecting a transform, not the mechanism behind the fast path: that is chosen by dispatch on IdentityTransform, so nothing in the kernel branches on this at runtime. A Proj.Transformation between two identical CRSs is a no-op pipeline but reports false, since only the type is examined.\n\n\n\n\n\n","title":"ImagePairGeometry.isidentity"},{"category":"method","location":"#ImagePairGeometry.transform_pair-Tuple{ImagePairGeometry.AbstractCoordTransform}","page":"Home","text":"transform_pair(t) -> TransformPair\n\nA TransformPair from t and its inverse.\n\n\n\n\n\n","title":"ImagePairGeometry.transform_pair"},{"category":"type","location":"#ImagePairGeometry.FastTransform","page":"Home","text":"FastTransform(source, target; always_xy = true)\n\nA coordinate transform between two EPSG codes, evaluated by FastGeoProjections.\n\nCallable as (x, y, z) -> (x′, y′, z′), the interface AbstractCoordTransform defines. The third coordinate is carried through unchanged: both CRSs of a projected pair share a datum, so a height is not a quantity either projection transforms.\n\nalways_xy = true — easting/longitude first — is the default. See fast_transform on why, and on the one case where the choice is observable.\n\n\n\n\n\n","title":"ImagePairGeometry.FastTransform"},{"category":"method","location":"#ImagePairGeometry.fast_transform-Tuple{Any, Any}","page":"Home","text":"fast_transform(grid_crs, image_crs; always_xy = true) -> TransformPair\n\nA TransformPair between two EPSG codes, evaluated by FastGeoProjections.\n\ngrid_crs and image_crs are EPSG codes — integers, or FastGeoProjections.EPSG objects. Pass the result as pairgeometry's or pairgeometry_blocked's transform; it needs no factory even for a threaded run, because the transformation holds no per-thread state.\n\nEqual codes give an IdentityTransform rather than a projection composed with its own inverse. PROJ resolves that case to a +proj=noop pipeline and returns its input unchanged, and the same-CRS fixtures asserted against it are bitwise on every band; composing unproject with project would instead land tens of nanometres away and break them.\n\nAxis order\n\nalways_xy = true is the default: a coordinate pair is (easting, northing) or (longitude, latitude), whatever the CRS's authority order says.\n\nFor a projected-to-projected pair the flag is inert — every EPSG code this package is used with orders a projected pair easting-first anyway, so the fixtures are unaffected by the choice. Where it decides anything is a geographic CRS: EPSG:4326's authority order is (latitude, longitude), so always_xy = false returns that pair reversed relative to what the radar path reads.\n\nThat is why the default is true rather than matching the reference's osr.CoordinateTransformation on SRSs from ImportFromEPSG, which is authority order. The radar path transforms grid coordinates to geodetic degrees and reads the result as (lon, lat, h); an authority-order transform swaps the two and yields geometry that is wrong everywhere without raising — the solve converges against a target on the wrong side of the planet. Defaulting to the order the kernel actually wants removes a silent failure whose only symptom is bad output.\n\nPass always_xy = false to reproduce authority order deliberately.\n\ntf = fast_transform(3413, 32624)\nr = pairgeometry_blocked(grid, pair, source; transform = tf, ntasks = 8)\n\n# A radar pair: grid CRS to geodetic degrees, needing no flag now that xy-order is the default.\ntf = fast_transform(32632, 4326)\n\n\n\n\n\n","title":"ImagePairGeometry.fast_transform"},{"category":"type","location":"#ImagePairGeometry.CoregisteredPair","page":"Home","text":"CoregisteredPair\n\nA pair of acquisitions, the image coordinate system their geometry is computed against, and their time separation.\n\nFields\n\ncoordinate: the image coordinate system pixel indices are relative to — any AbstractImageCoordinate. For a projected pair whose overlap was computed this is that overlap, not either input image; for a radar pair it is the reference acquisition outright, since testGeogrid.py:427-470 takes every radar parameter from image 1.\nreference_offset, secondary_offset: the (column, row) index of the overlap's first pixel within each input image, zero-based as the reference reports them. A caller reads its image windows at these offsets. Both zero when coordinate is already the window — see the one-argument constructor.\ndt: time separation in seconds, secondary minus reference. Enters the geometry as the factor converting velocity to displacement.\n\nBuilt by coregister when the overlap has to be computed, or directly from a coordinate when it does not.\n\n\n\n\n\n","title":"ImagePairGeometry.CoregisteredPair"},{"category":"method","location":"#ImagePairGeometry.CoregisteredPair-Tuple{ImagePairGeometry.AbstractImageCoordinate}","page":"Home","text":"CoregisteredPair(coordinate; dt)\n\nA pair whose image coordinate system is already known, with both offsets zero.\n\nFor a RadarCoordinate, and for a ProjectedCoordinate the caller built directly from a view of an overlap it has already sliced. In both cases the coordinate is the window, so there is no offset into a larger image to record — which is what the zeros mean, rather than \"the overlap happens to start at the origin\".\n\ncoregister is the other way to obtain a pair, for the case that needs it: two footprints whose intersection has to be computed, which is also where nonzero offsets come from.\n\n\n\n\n\n","title":"ImagePairGeometry.CoregisteredPair"},{"category":"type","location":"#ImagePairGeometry.ImageFootprint","page":"Home","text":"ImageFootprint(; origin, spacing, size)\n\nWhere one image sits in projected coordinates: enough to intersect two images without reading pixels.\n\norigin is the projected coordinate of the first pixel's center and spacing is the signed pixel size, matching a GDAL geotransform's (gt[1], gt[4]) and (gt[2], gt[6]). See ProjectedCoordinate, whose fields have the same meaning.\n\n\n\n\n\n","title":"ImagePairGeometry.ImageFootprint"},{"category":"method","location":"#ImagePairGeometry.coregister-Tuple{ImageFootprint, ImageFootprint}","page":"Home","text":"coregister(reference::ImageFootprint, secondary::ImageFootprint; dt) -> CoregisteredPair\n\nIntersect two image footprints, giving the overlap's coordinate system and each image's offset into it.\n\nReproduces GeogridOptical.coregister (GeogridOptical.py:277-345) including its four failure conditions, which are errors rather than clamps: the reference refuses a pair it cannot align instead of silently processing a partial overlap.\n\nThe overlap is W = max(x₁, x₂), N = min(y₁, y₂), E = min(east₁, east₂), S = max(south₁, south₂), where each image's far edge is its last pixel center (origin + (size - 1) * spacing) rather than its outer boundary. Index arithmetic uses nround — half to even — because the reference computes it in NumPy, not in the C++ kernel where std::round applies.\n\ndt is seconds, secondary minus reference. The reference derives it from calendar dates only (testGeogrid.py:351-354), so it is always a whole number of days there; nothing here requires that.\n\nThrows ArgumentError if the two spacings differ, if the overlap is empty, or if any of the reference's four conditions fail.\n\nExample\n\njulia> using ImagePairGeometry\n\njulia> a = ImageFootprint(origin = (100.0, 900.0), spacing = (10.0, -10.0), size = (50, 50));\n\njulia> b = ImageFootprint(origin = (150.0, 950.0), spacing = (10.0, -10.0), size = (50, 50));\n\njulia> p = coregister(a, b; dt = 86400.0);\n\njulia> p.coordinate.origin, p.coordinate.size\n((150.0, 900.0), (45, 45))\n\njulia> p.reference_offset, p.secondary_offset\n((5, 0), (0, 5))\n\n\n\n\n\n","title":"ImagePairGeometry.coregister"},{"category":"type","location":"#ImagePairGeometry.MapGrid","page":"Home","text":"MapGrid(; geotransform, size, crs = nothing)\n\nThe grid the geometry is computed on, defined by a DEM.\n\nFields\n\ngeotransform: GDAL's six coefficients (x₀, dx, rx, y₀, ry, dy), where (x₀, y₀) is the outer corner of the first pixel — not its center. Rotation terms rx/ry must be zero; the reference assumes a north-up grid throughout and its index arithmetic is wrong otherwise.\nsize: (ncolumns, nrows) of the DEM.\ncrs: the projection as a GeoFormatTypes.GeoFormat, or nothing. An Integer is taken as an EPSG code and converted, so crs = 32624 and crs = GFT.EPSG(32624) are the same thing. Read it back with GeoInterface.crs. Carried for output metadata; the transform used in the computation is passed separately, so this is not consulted during the kernel.\n\nGrid point centers are at x₀ + (i + 0.5) * dx, matching the reference (geogridOptical.cpp:678-679).\n\n\n\n\n\n","title":"ImagePairGeometry.MapGrid"},{"category":"constant","location":"#ImagePairGeometry.DEFAULT_ZRANGE","page":"Home","text":"DEFAULT_ZRANGE\n\nElevation range, in meters, bounding an image footprint where the true terrain height is unknown: (-200.0, 4000.0), the reference's default (GeogridOptical.py:115).\n\nThe footprint is computed at both extremes and the union taken, so the bounding box is conservative with respect to terrain.\n\nElevation changes a horizontal coordinate only when the transform crosses datums, since only then does the pipeline apply a 3D Helmert shift. Between two CRSs on one datum the horizontal result is identical at any height, so for imagery and grids on WGS84 — UTM, EPSG:3413, EPSG:3031, hence every ITS_LIVE projection — this range widens nothing.\n\n\n\n\n\n","title":"ImagePairGeometry.DEFAULT_ZRANGE"},{"category":"method","location":"#GeoInterface.crs-Tuple{MapGrid}","page":"Home","text":"GeoInterface.crs(g::MapGrid)\nGeoInterface.crs(r::PairGeometry)\n\nThe projection as a GeoFormatTypes.GeoFormat, or nothing if none was given.\n\nThe GeoJulia accessor, so a consumer reads the CRS the same way here as from a Raster or a geometry rather than reaching for the field. A result inherits its grid's.\n\n\n\n\n\n","title":"GeoInterface.crs"},{"category":"method","location":"#ImagePairGeometry.footprint_bounds-Tuple{Any, ProjectedCoordinate}","page":"Home","text":"footprint_bounds(transform, c::ProjectedCoordinate; zrange = DEFAULT_ZRANGE) -> Extent\n\nBounding box, in grid coordinates, of the image described by c.\n\ntransform maps image coordinates to grid coordinates and is called as transform(x, y, z) -> (x′, y′, z′). The image's four corners are transformed at both elevations in zrange — eight points — and the box is their min/max, reproducing GeogridOptical.determineBbox (GeogridOptical.py:115-155).\n\nCorners are pixel centers of the first and last pixel (origin + (size - 1) * spacing), not the outer boundary, matching the reference.\n\nReturns an Extents.Extent{(:X, :Y)}. The elevation range is not part of the result: it only widens the horizontal box.\n\nGiven a TransformPair this takes the correct direction itself. Note that it is the pair's inverse — the kernel's forward direction is grid to image, and this needs image to grid — so passing tf.forward by hand computes a bounding box in the wrong space, which surfaces as a window far outside the grid.\n\n\n\n\n\n","title":"ImagePairGeometry.footprint_bounds"},{"category":"method","location":"#ImagePairGeometry.grid_window-Tuple{MapGrid, Extent}","page":"Home","text":"grid_window(g::MapGrid, bounds::Extent) -> CartesianIndices{2}\n\nThe window of grid points overlapping bounds, as one-based CartesianIndices.\n\nReproduces geogridOptical.cpp:239-244:\n\nlOff   = std::max(std::floor((ymax - geoTrans[3]) / geoTrans[5]), 0.);\nlCount = std::min(std::ceil((ymin - geoTrans[3]) / geoTrans[5]), demYSize - 1.) - lOff;\n\nincluding the demXSize - 1. / demYSize - 1. clamp, which excludes the grid's last column and row from ever being processed. That is reproduced rather than corrected — the reference's outputs are one pixel short of the DEM extent and matching it is the point.\n\nThe reference computes these as double and truncates to int, and can produce a negative count for a pair that does not overlap the grid, which it then hands to GDAL as a raster size. This throws instead; see REFERENCE.md.\n\nCartesianIndices rather than an offset/count pair so the result composes with array views, DiskArrays chunk iteration, and blocking without conversion. The reference's zero-based pOff/lOff are recoverable as first(window).I .- 1.\n\n\n\n\n\n","title":"ImagePairGeometry.grid_window"},{"category":"method","location":"#ImagePairGeometry.gridorigin-Tuple{MapGrid}","page":"Home","text":"gridorigin(g::MapGrid)\n\nOuter corner (x₀, y₀) of the grid's first pixel. Point centers are half a pixel inside this.\n\n\n\n\n\n","title":"ImagePairGeometry.gridorigin"},{"category":"method","location":"#ImagePairGeometry.gridpoint_center-Tuple{MapGrid, Integer, Integer}","page":"Home","text":"gridpoint_center(g::MapGrid, i::Integer, j::Integer) -> NTuple{2}\n\nProjected coordinate of the center of grid point (i, j), one-based.\n\nThe half-pixel offset matches the reference (geogridOptical.cpp:678-679): geometry is evaluated at pixel centers, not corners.\n\n\n\n\n\n","title":"ImagePairGeometry.gridpoint_center"},{"category":"method","location":"#ImagePairGeometry.gridspacing-Tuple{MapGrid}","page":"Home","text":"gridspacing(g::MapGrid)\n\nSigned grid spacing (dx, dy). Geogrid reports dx as gridSpacingX in its scalar output, where downstream code uses it to relate chip size to grid spacing.\n\n\n\n\n\n","title":"ImagePairGeometry.gridspacing"},{"category":"method","location":"#ImagePairGeometry.window_geotransform-Tuple{MapGrid, CartesianIndices{2, R} where R<:Tuple{OrdinalRange{Int64, Int64}, OrdinalRange{Int64, Int64}}}","page":"Home","text":"window_geotransform(g::MapGrid, window::CartesianIndices{2}) -> NTuple{6}\n\nGeotransform of the sub-grid window selects, for writing outputs that carry their own georeferencing.\n\nMatches the reference's output geotransform: the origin shifts by the window offset and the spacing is unchanged (geogridOptical.cpp:246-252).\n\n\n\n\n\n","title":"ImagePairGeometry.window_geotransform"},{"category":"type","location":"#ImagePairGeometry.SearchRangeScaling","page":"Home","text":"SearchRangeScaling(; dt_unity = 182.0, max_scale = 5.0, lower = 0.0, upper = 20000.0)\n\nParameters of the interval-dependent search-range inflation.\n\nFields\n\ndt_unity: interval in days at which the scale reaches 1, i.e. no inflation.\nmax_scale: scale approached as the interval goes to zero.\nlower, upper: bounds the scaled range is clamped to, in the same units as the input (m/yr).\n\nDefaults are the reference's (GeogridOptical.py:358-362: srs_dt_unity = 182, srs_max_scale = 5, srs_min_search = 0, srs_max_search = 20000).\n\n\n\n\n\n","title":"ImagePairGeometry.SearchRangeScaling"},{"category":"method","location":"#ImagePairGeometry.scaled_searchrange-Tuple{SearchRangeScaling, Real, Real}","page":"Home","text":"scaled_searchrange(p::SearchRangeScaling, sr, scale) -> Float64\n\nOne axis of a search range, inflated by scale then clamped to p.lower/p.upper.\n\nMatches geogridOptical.cpp:701-711, where the clamp is min(max(sr, lower), upper).\n\nThe clamp is the reason the reference's nodata handling for search range is partly ineffective. Applied before the nodata comparison, it maps a negative sentinel such as the DEM's -32767 to lower — normally 0.0, which the downstream == 0 test does catch — but maps the search-range rasters' own +32767 to upper, an apparently valid 20000 m/yr range. Reproduced; see REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.scaled_searchrange"},{"category":"method","location":"#ImagePairGeometry.searchrange_scale-Tuple{SearchRangeScaling, Real}","page":"Home","text":"searchrange_scale(p::SearchRangeScaling, dt) -> Float64\n\nInflation factor applied to a search range for a pair separated by dt seconds.\n\nReproduces geogridOptical.cpp:701-704 including its division order:\n\nstd::max(max_factor * ((dt_unity - 1) * max_factor + (max_factor - 1)\n         - (max_factor - 1) * dt / 24.0 / 3600.0) / ((dt_unity - 1) * max_factor), 1.0)\n\nFalls linearly from max_scale toward 1 as dt grows, floored at 1 — so a pair longer than dt_unity days is not inflated, and never shrunk.\n\njulia> using ImagePairGeometry: SearchRangeScaling, searchrange_scale\n\njulia> p = SearchRangeScaling();\n\njulia> round(searchrange_scale(p, 1 * 86400.0), digits = 4)\n5.0\n\njulia> round(searchrange_scale(p, 91 * 86400.0), digits = 4)\n3.011\n\njulia> searchrange_scale(p, 182 * 86400.0)\n1.0\n\njulia> searchrange_scale(p, 365 * 86400.0)\n1.0\n\n\n\n\n\n","title":"ImagePairGeometry.searchrange_scale"},{"category":"type","location":"#ImagePairGeometry.PointGeometry","page":"Home","text":"PointGeometry\n\nGeometry at one grid point that depends only on the grid, the image coordinate system and the projection — not on the image pair's time separation.\n\nFields\n\nimage_xy: where the point falls in the image, in whatever units that path's forward mapping produces. On the projected path these are image coordinates, and pixel_index converts them to a rounded index. On the radar path they are already the rounded (range, azimuth) index, because range and azimuth are computed from the solve rather than from an affine relation — there is no intermediate coordinate to hold. So pixel_index applies to the projected path only; a radar caller reads this field directly.\nxunit, yunit: unit vectors along the image's two axes, expressed in grid coordinates.\nxlen, ylen: length in grid coordinates of a one-pixel step along each image axis. The physical pixel size at this point, which differs from the nominal spacing wherever the projection distorts.\nnormal: upward surface normal from the slope rasters, or the zero vector where no slope is supplied.\n\n\n\n\n\n","title":"ImagePairGeometry.PointGeometry"},{"category":"constant","location":"#ImagePairGeometry.DEG2RAD","page":"Home","text":"DEG2RAD\n\nRadians per degree, as the reference computes it: M_PI / 180.0 (geogridOptical.cpp:39).\n\n\n\n\n\n","title":"ImagePairGeometry.DEG2RAD"},{"category":"constant","location":"#ImagePairGeometry.NO_NORMAL","page":"Home","text":"NO_NORMAL\n\nThe zero vector, standing for \"no slope raster supplied\".\n\nThe reference sets the normal to all zeros in that case (geogridOptical.cpp:761-764) rather than skipping the computations that use it, so the velocity closure divides by zero and the outputs that depend on it are Inf or NaN. Those outputs are not written at all without a slope raster, so the values never reach a file — but the arithmetic runs, and reproducing it keeps the code paths aligned.\n\n\n\n\n\n","title":"ImagePairGeometry.NO_NORMAL"},{"category":"method","location":"#ImagePairGeometry.cross_check-Tuple{ImagePairGeometry.PointGeometry}","page":"Home","text":"cross_check(g::PointGeometry) -> Float64\n\nDegrees by which the surface normal deviates from perpendicular to the plane of the image axes.\n\nMatches geogridOptical.cpp:812-814: the angle between the normal and the normalized cross product of the axis unit vectors, less 90°, in absolute value. The reference computes the displacement-to-velocity operator only where this exceeds 1°, since below that the two-by-two system is near singular and its inverse is dominated by rounding.\n\nFor realistic terrain this sits at 82–90°, far above the threshold; it approaches it only for near-vertical surfaces.\n\n\n\n\n\n","title":"ImagePairGeometry.cross_check"},{"category":"method","location":"#ImagePairGeometry.inbounds-Tuple{Float64, Float64, ImagePairGeometry.AbstractImageCoordinate}","page":"Home","text":"inbounds(xind, yind, c::AbstractImageCoordinate) -> Bool\n\nWhether a zero-based pixel index falls inside the image.\n\ngeogridOptical.cpp:775 and geogridRadar.cpp:1112 write the same expression against their own size fields, so one method serves both paths through nsamples and nlines.\n\nThe comparison is on the pre-conversion Float64. A NaN index — reachable when the transform is fed a NaN elevation — compares false in every direction and so is reported in bounds, as the reference reports it. See REFERENCE.md.\n\n\n\n\n\n","title":"ImagePairGeometry.inbounds"},{"category":"method","location":"#ImagePairGeometry.pixel_index-Tuple{ImagePairGeometry.PointGeometry, ProjectedCoordinate}","page":"Home","text":"pixel_index(g::PointGeometry, c::ProjectedCoordinate) -> NTuple{2,Float64}\n\nZero-based pixel index of the point in the image, rounded to the nearest whole pixel.\n\nProjected path only, by signature: it inverts the affine relation between image coordinates and pixel indices, which a radar acquisition has no analogue of. There, the index comes out of the solve — see PointGeometry on what image_xy holds on each path.\n\nMatches geogridOptical.cpp:723-724. Uses cround — halves away from zero — because the reference rounds here in C++, not in NumPy. The result is Float64 so the bounds test that follows sees exactly the value the reference tests, before any integer conversion.\n\n\n\n\n\n","title":"ImagePairGeometry.pixel_index"},{"category":"method","location":"#ImagePairGeometry.pointgeometry-Tuple{TransformPair, Real, Real, Real, ProjectedCoordinate, StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"pointgeometry(tf::TransformPair, gx, gy, gz, c::ProjectedCoordinate, normal) -> PointGeometry\n\nGeometry at the grid point (gx, gy, gz) for an image with coordinate system c.\n\ngx, gy are the point's center in grid coordinates and gz its elevation. normal comes from surface_normal, or is NO_NORMAL.\n\nReproduces geogridOptical.cpp:717-752. The axis unit vectors are obtained by stepping one pixel along each image axis, transforming back to grid coordinates, and normalizing the difference — so they carry the projection's local rotation and scale, which is what makes the velocity conversion valid off the projection's center.\n\nUnder IdentityTransform a specialized method computes the same values in closed form.\n\n\n\n\n\n","title":"ImagePairGeometry.pointgeometry"},{"category":"method","location":"#ImagePairGeometry.surface_normal-Tuple{Real, Real}","page":"Home","text":"surface_normal(dhdx, dhdy) -> SVector{3,Float64}\n\nUpward unit normal to a surface with the given slopes.\n\nMatches geogridOptical.cpp:756-764: normalize (dhdx, dhdy, -1) then negate, giving a vector with positive z. The velocity closure divides by that z component, so the sign convention is load-bearing.\n\n\n\n\n\n","title":"ImagePairGeometry.surface_normal"},{"category":"constant","location":"#ImagePairGeometry.SECONDS_PER_YEAR_TERMS","page":"Home","text":"SECONDS_PER_YEAR_TERMS\n\nThe reference's year length as the three factors it divides by in sequence: (365.0, 24.0, 3600.0).\n\nTheir product, 31536000, is exact in Float64, but x / 365.0 / 24.0 / 3600.0 and x / 31_536_000.0 still differ in the last bit for most x because each division rounds. Kept as separate factors so the division order matches.\n\n\n\n\n\n","title":"ImagePairGeometry.SECONDS_PER_YEAR_TERMS"},{"category":"method","location":"#ImagePairGeometry.axis_velocity-Tuple{Float64, Float64}","page":"Home","text":"axis_velocity(spacing, dt) -> Float64\n\nVelocity per pixel of displacement along one image axis: spacing / dt scaled to meters per year.\n\nThe radar path's third off2vel band (geogridRadar.cpp:1185-1187), written dr / dt * 365.0 * 24.0 * 3600.0 for range and with the along-track step for azimuth. Where the two-by-two operator resolves a displacement into map axes, this converts it along the image's own axis, which is the quantity a range- or azimuth-only measurement needs.\n\nThe projected path writes no such band — its off2vel files have two bands, not three — so this is unused there.\n\n\n\n\n\n","title":"ImagePairGeometry.axis_velocity"},{"category":"method","location":"#ImagePairGeometry.chip_pixels-Tuple{Float64, Float64, Float64, Int64, Int64}","page":"Home","text":"chip_pixels(csx, csy, chip_size_0, pix_x, pix_y) -> NTuple{2,Float64}\n\nA chip size in meters converted to pixels along each axis.\n\nMatches geogridOptical.cpp:874-875: scaled by the ratio of the reference chip size to its pixel equivalent. The caller converts with ctrunc32 — the reference truncates here rather than rounding, unlike every other integer output.\n\npix_x/pix_y come from chip_size_pixels.\n\n\n\n\n\n","title":"ImagePairGeometry.chip_pixels"},{"category":"method","location":"#ImagePairGeometry.chip_size_pixels-Tuple{Real, Real}","page":"Home","text":"chip_size_pixels(chip_size_0, res) -> Int\n\nThe reference chip size in pixels for a pixel size of res, rounded up to a multiple of four.\n\nMatches geogridOptical.cpp:513-514: ceil(chip_size_0 / res / 4) * 4. The multiple of four is what the correlator's chip pyramid requires.\n\njulia> using ImagePairGeometry: chip_size_pixels\n\njulia> chip_size_pixels(240.0, 30.0)\n8\n\njulia> chip_size_pixels(240.0, 10.0)\n24\n\njulia> chip_size_pixels(240.0, 15.0)\n16\n\n\n\n\n\n","title":"ImagePairGeometry.chip_size_pixels"},{"category":"method","location":"#ImagePairGeometry.close_slope_parallel-Tuple{Float64, Float64, StaticArraysCore.SVector{3, Float64}}","page":"Home","text":"close_slope_parallel(vx, vy, normal) -> SVector{3,Float64}\n\nA horizontal velocity completed to three components by requiring flow parallel to the surface.\n\nThe vertical component follows from v · n = 0:\n\nvz = -(vx * nx + vy * ny) / nz\n\nmatching geogridOptical.cpp:766-773. Applied to both the reference velocity and the search range, since a search extent is a velocity bound and must lie in the same plane.\n\nNo guard on nz: with a slope raster the normal's z is positive by construction, and without one the normal is the zero vector and the result is non-finite — which is the reference's behavior, and those outputs are not written in that configuration.\n\n\n\n\n\n","title":"ImagePairGeometry.close_slope_parallel"},{"category":"method","location":"#ImagePairGeometry.offset_to_velocity-Tuple{ImagePairGeometry.PointGeometry, Tuple{Float64, Float64}, Float64}","page":"Home","text":"offset_to_velocity(g::PointGeometry, spacing::NTuple{2,Float64}, dt)\n    -> NTuple{4,Float64}\n\nThe two-by-two operator converting a pixel displacement to a map velocity, as (vx_from_dx, vx_from_dy, vy_from_dx, vy_from_dy).\n\nspacing is the nominal pixel size along each image axis — signed on the projected path, where it is the coordinate's spacing; on the radar path it is (dr, |da|), the range sample spacing and the per-point along-track step. A value rather than a coordinate because the two paths supply it differently and the radar one varies per point (geogridRadar.cpp:1180-1188).\n\nGiven a displacement (dx, dy) in pixels, the map velocity is\n\nvx = vx_from_dx * dx + vx_from_dy * dy\nvy = vy_from_dx * dx + vy_from_dy * dy\n\nwhich is how the downstream correlator reconstructs velocity, after scaling the displacement by scale_factors.\n\nMatches geogridOptical.cpp:817-832. The operator inverts the two-by-two system relating map displacement to image displacement, projected into the plane perpendicular to the surface normal — so it embeds both the projection's local geometry and the slope-parallel-flow constraint. The shared determinant is evaluated once rather than the reference's four times, which is bit-identical.\n\nValid only where cross_check exceeds one degree; the caller checks, as the reference does, and writes nodata otherwise.\n\n\n\n\n\n","title":"ImagePairGeometry.offset_to_velocity"},{"category":"method","location":"#ImagePairGeometry.per_year-Tuple{Float64}","page":"Home","text":"per_year(x) -> Float64\n\nx divided by one year in seconds, in the reference's division order: x / 365.0 / 24.0 / 3600.0.\n\n\n\n\n\n","title":"ImagePairGeometry.per_year"},{"category":"method","location":"#ImagePairGeometry.pixel_offset-Tuple{StaticArraysCore.SVector{3, Float64}, ImagePairGeometry.PointGeometry, Float64}","page":"Home","text":"pixel_offset(vel, g::PointGeometry, dt) -> NTuple{2,Float64}\n\nDisplacement in pixels, along each image axis, that vel produces over dt.\n\nMatches geogridOptical.cpp:807-808: the velocity projected onto the axis unit vector, times the interval, divided by the physical length of one pixel along that axis. Rounded with cround to a whole pixel.\n\nvel is in meters per year and three-component — use close_slope_parallel to build it — and the axis lengths come from g, so projection distortion is accounted for.\n\n\n\n\n\n","title":"ImagePairGeometry.pixel_offset"},{"category":"method","location":"#ImagePairGeometry.scale_factors-Tuple{ImagePairGeometry.PointGeometry, Tuple{Float64, Float64}}","page":"Home","text":"scale_factors(g::PointGeometry, spacing::NTuple{2,Float64}) -> NTuple{2,Float64}\n\nRatio of true ground distance to nominal pixel spacing, per axis.\n\nMatches geogridOptical.cpp:841-842: the physical length of a one-pixel step in grid coordinates divided by the nominal spacing, in absolute value. Exactly 1 where the grid and image share a CRS; away from that, the map projection's local scale distortion — around 0.9996 at a UTM zone's central meridian.\n\nThe radar equivalent is geogridRadar.cpp:1190-1191, the same ratio against dr and the along-track step.\n\nThe correlator multiplies its measured displacement by these before applying offset_to_velocity, which is why that operator uses nominal spacing.\n\n\n\n\n\n","title":"ImagePairGeometry.scale_factors"},{"category":"method","location":"#ImagePairGeometry.search_pixels-Tuple{StaticArraysCore.SVector{3, Float64}, StaticArraysCore.SVector{3, Float64}, ImagePairGeometry.PointGeometry, Tuple{Float64, Float64}, Float64}","page":"Home","text":"search_pixels(sr1, sr2, g::PointGeometry, spacing::NTuple{2,Float64}, dt)\n    -> NTuple{2,Float64}\n\nSearch half-extent in pixels along each image axis, from a search range already scaled and slope-closed.\n\nsr1 and sr2 are the two search vectors the reference forms: sr2 is sr1 with the x component negated (geogridOptical.cpp:713-714). Both are projected onto each axis and the larger magnitude taken, so the extent covers motion in either direction, then floored at one pixel — a zero extent would mean no search at all.\n\nMatches geogridOptical.cpp:849-863.\n\n\n\n\n\n","title":"ImagePairGeometry.search_pixels"},{"category":"method","location":"#ImagePairGeometry.times_year-Tuple{Float64}","page":"Home","text":"times_year(x) -> Float64\n\nx multiplied by one year in seconds, in the reference's multiplication order: x * 365.0 * 24.0 * 3600.0.\n\nThe inverse direction from per_year, and a separate function rather than a reciprocal because the reference writes it as a chain of multiplications (geogridRadar.cpp:1185-1187). Multiplying by 31536000 in one step, or dividing by the reciprocal, gives a different last bit.\n\nUsed only by the radar path's third off2vel band, which converts a pixel displacement straight to a velocity along its own axis rather than into map coordinates.\n\n\n\n\n\n","title":"ImagePairGeometry.times_year"},{"category":"type","location":"#ImagePairGeometry.NoDataPolicy","page":"Home","text":"NoDataPolicy(; input, output = -32767.0)\n\nHow missing values are recognized on input and written on output.\n\nFields\n\ninput: the value treated as missing in every input raster. The reference takes this from the DEM's first band and applies it to all of them, so it is one value rather than one per raster. A raster whose own sentinel differs is not fully masked; see REFERENCE.md.\noutput: written wherever an output cannot be computed. -32767.0 in production (testGeogrid.py:357), stored as Float64 and converted per output band.\n\nThe reference reads its input sentinel with GetNoDataValue(NULL), discarding the success flag, so a raster with no nodata set yields 0.0 and every genuine zero is then treated as missing. Constructing this type with input = 0.0 reproduces that; nodata_from makes the choice visible at the call site.\n\n\n\n\n\n","title":"ImagePairGeometry.NoDataPolicy"},{"category":"constant","location":"#ImagePairGeometry.NODATA_INT32","page":"Home","text":"NODATA_INT32\n\nThe output sentinel as it appears in the integer bands: Int32(-32767).\n\n\n\n\n\n","title":"ImagePairGeometry.NODATA_INT32"},{"category":"method","location":"#ImagePairGeometry.ismissingval-Tuple{NoDataPolicy, Real}","page":"Home","text":"ismissingval(p::NoDataPolicy, x) -> Bool\n\nWhether x is missing: equal to the sentinel, or NaN.\n\nEquality is what the reference tests, and NaN is equal to nothing — but the ITS_LIVE velocity rasters store NaN while declaring a nodata of -32767, so on real data the reference's test never fires there and NaN reaches its arithmetic. std::round(NaN) converted to GInt32 is undefined behavior, which arm64 clang resolves to 0 — the same value the reference writes from its own missing-velocity branch (geogridOptical.cpp:800-805). Treating NaN as missing therefore agrees with the reference bit for bit on the outputs while reaching it by a defined route.\n\n\n\n\n\n","title":"ImagePairGeometry.ismissingval"},{"category":"method","location":"#ImagePairGeometry.nodata_from-Tuple{Real}","page":"Home","text":"nodata_from(missingval; output = -32767.0) -> NoDataPolicy\n\nA NoDataPolicy from a raster's declared missing value, falling back to 0.0 when there is none.\n\nThe fallback reproduces the reference: it reads the DEM's nodata without checking whether one is set, so an absent sentinel arrives as 0.0 and every zero-valued input pixel is read as missing. Passing nothing here makes that consequence explicit rather than incidental.\n\n\n\n\n\n","title":"ImagePairGeometry.nodata_from"},{"category":"type","location":"#ImagePairGeometry.PairGeometry","page":"Home","text":"PairGeometry\n\nPer-grid-point geometry of an image pair, one array per output quantity.\n\nAll arrays share the axes of the grid window the result was computed over.\n\nInteger fields, sentinel -32767\n\nlocation_x, location_y: zero-based pixel index of the grid point in the image.\noffset_x, offset_y: expected displacement in pixels, from the reference velocity field.\nsearch_x, search_y: search half-extent in pixels.\nchip_min_x, chip_min_y, chip_max_x, chip_max_y: chip size bounds in pixels.\nstable_surface: stable-surface mask.\n\nFloat fields, sentinel -32767.0\n\noff2vx_dx, off2vx_dy, off2vy_dx, off2vy_dy: the operator converting a pixel displacement to a map velocity. Displacement (dx, dy) gives vx = off2vx_dx * dx + off2vx_dy * dy and likewise for vy, after scaling the displacement by the scale factors.\noff2vx_dr, off2vy_dr: velocity per pixel of displacement along the image's own axes, the radar path's third off2vel band (geogridRadar.cpp:1185-1187). Left at their sentinel on the projected path, which writes two-band off2vel files.\nscale_x, scale_y: ratio of true ground distance to nominal pixel spacing.\n\nMetadata\n\ngeotransform, crs: georeferencing of the window. The CRS is a GeoFormatTypes.GeoFormat or nothing, inherited from the grid; read it with GeoInterface.crs.\nwindow: the grid indices covered.\nnodata: the policy the sentinels come from.\ncoordinate: the AbstractImageCoordinate the geometry was computed for. Carried as a type parameter so consumers that differ between the two paths — the output band layout, the sign of the correlator's y prior — dispatch on it rather than inferring or being told.\n\nField names map to the reference's files as location → window_location.tif, offset → window_offset.tif, search → window_search_range.tif, chip_min/chip_max → window_chip_size_min.tif/_max.tif, stable_surface → window_stable_surface_mask.tif, off2vx/off2vy → window_rdr_off2vel_x_vec.tif/_y_vec.tif, scale → window_scale_factor.tif.\n\n\n\n\n\n","title":"ImagePairGeometry.PairGeometry"},{"category":"constant","location":"#ImagePairGeometry.FLOAT_BANDS","page":"Home","text":"FLOAT_BANDS\n\nNames of the Float64 fields of PairGeometry, in the reference's band order.\n\n\n\n\n\n","title":"ImagePairGeometry.FLOAT_BANDS"},{"category":"constant","location":"#ImagePairGeometry.INT_BANDS","page":"Home","text":"INT_BANDS\n\nNames of the Int32 fields of PairGeometry, in the reference's band order.\n\n\n\n\n\n","title":"ImagePairGeometry.INT_BANDS"},{"category":"constant","location":"#ImagePairGeometry.RADAR_REFERENCE_FILES","page":"Home","text":"RADAR_REFERENCE_FILES\n\nREFERENCE_FILES with the radar path's three-band off2vel layout.\n\nBand 3 of each is the velocity per pixel of displacement along that image axis (geogridRadar.cpp:1185-1187), which the projected path has no equivalent of.\n\n\n\n\n\n","title":"ImagePairGeometry.RADAR_REFERENCE_FILES"},{"category":"constant","location":"#ImagePairGeometry.REFERENCE_FILES","page":"Home","text":"REFERENCE_FILES\n\nEach of the reference's nine output files, with the PairGeometry fields holding its bands in order — the projected path's band layout.\n\nThe basis for writing GeoTIFFs that a downstream reader consuming the reference's output can read unchanged, and for comparing band by band against it.\n\nThe two off2vel files have two bands here and three on the radar path (geogridOptical.cpp:461 versus geogridRadar.cpp:634,652). That difference is not cosmetic: a reader written against the projected output indexes bands positionally, so writing three where the reference writes two would break it. Use reference_files to get the layout for a given coordinate system rather than assuming this one.\n\n\n\n\n\n","title":"ImagePairGeometry.REFERENCE_FILES"},{"category":"method","location":"#ImagePairGeometry.allocate_geometry-Tuple{CartesianIndices{2, R} where R<:Tuple{OrdinalRange{Int64, Int64}, OrdinalRange{Int64, Int64}}, NTuple{6, Float64}, Any, NoDataPolicy, ImagePairGeometry.AbstractImageCoordinate}","page":"Home","text":"allocate_geometry(window, geotransform, crs, nodata, coordinate) -> PairGeometry\n\nA PairGeometry sized to window, with every band filled with its sentinel.\n\nPrefilling means a band the inputs do not support — the reference writes no file at all in that case — is uniformly nodata rather than uninitialized, and a point skipped for being out of bounds needs no explicit write.\n\n\n\n\n\n","title":"ImagePairGeometry.allocate_geometry"},{"category":"method","location":"#ImagePairGeometry.npoints-Tuple{PairGeometry}","page":"Home","text":"npoints(r::PairGeometry) -> Int\n\nNumber of grid points in the result, valid or not.\n\n\n\n\n\n","title":"ImagePairGeometry.npoints"},{"category":"method","location":"#ImagePairGeometry.nvalid-Tuple{PairGeometry}","page":"Home","text":"nvalid(r::PairGeometry) -> Int\n\nNumber of grid points that mapped inside the image, i.e. whose location is not the sentinel.\n\n\n\n\n\n","title":"ImagePairGeometry.nvalid"},{"category":"method","location":"#ImagePairGeometry.reference_files-Tuple{PairGeometry}","page":"Home","text":"reference_files(g::PairGeometry) -> Tuple\n\nThe layout for a result, from the coordinate system it was computed for.\n\nThis is how write_geotiffs picks a band count. Getting it wrong is silent — a reader built against the reference's projected output indexes bands positionally, so an extra band shifts everything after it — which is why the result carries its coordinate rather than the layout being inferred from the bands. Inferring it from whether off2vx_dr is all-sentinel misclassifies a radar result computed without a slope raster, since the whole slope branch that fills that band is skipped.\n\n\n\n\n\n","title":"ImagePairGeometry.reference_files"},{"category":"method","location":"#ImagePairGeometry.reference_files-Tuple{ProjectedCoordinate}","page":"Home","text":"reference_files(c::AbstractImageCoordinate) -> Tuple\n\nThe output file and band layout for a coordinate system: REFERENCE_FILES for a projected image, RADAR_REFERENCE_FILES for a radar one.\n\nDispatching rather than branching on a flag, so a caller cannot write the wrong band count for the path it is on.\n\n\n\n\n\n","title":"ImagePairGeometry.reference_files"},{"category":"method","location":"#ImagePairGeometry.velocity_conversion-Tuple{PairGeometry}","page":"Home","text":"velocity_conversion(g::PairGeometry; coordinate = g.coordinate) -> NamedTuple\n\nThe parts of g that turn a measured pixel displacement into a map velocity.\n\nA correlator returns a displacement in pixels; these are the per-point quantities that convert it, gathered in one place so a caller need not know which fields of a PairGeometry participate.\n\nFields\n\noff2vx, off2vy: the two-by-two operator, each as (dx, dy) coefficient arrays.\nscale: the scale factors, as (x, y).\nstable_surface: the stable-surface mask, true where stable. Points that are missing or outside the image are false.\nchip_scale_y: median ratio of the y to the x chip-size bound, the aspect ratio the reference derives as ScaleChipSizeY (testautoRIFT.py:376). NaN where no chip-size band is present.\ndy_sign: the factor a measured y displacement needs before use. See below.\nnodata: the sentinel marking a missing entry in the float arrays.\n\nGiven a displacement (dx, dy) in pixels, in the convention where +y points north:\n\nc = velocity_conversion(g)\nvx = c.off2vx.dx .* (dx .* c.scale.x) .+ c.off2vx.dy .* (dy .* c.scale.y)\nvy = c.off2vy.dx .* (dx .* c.scale.x) .+ c.off2vy.dy .* (dy .* c.scale.y)\n\ndy_sign is y_displacement_sign of the coordinate system: the factor a measured y displacement needs before the expressions above, -1.0 on the radar path and +1.0 on the projected one. Applying it is the caller's step, since the caller is what holds the measurement, but the value is supplied here so it need not be re-derived.\n\n\n\n\n\n","title":"ImagePairGeometry.velocity_conversion"},{"category":"type","location":"#ImagePairGeometry.GeometryInputs","page":"Home","text":"GeometryInputs(; dem, dhdx = nothing, dhdy = nothing, vx = nothing, vy = nothing,\n                 srx = nothing, sry = nothing, csminx = nothing, csminy = nothing,\n                 csmaxx = nothing, csmaxy = nothing, ssm = nothing)\n\nThe per-grid-point input rasters, covering the window being computed.\n\nOnly dem is required. Each other quantity is nothing when not supplied, and the outputs depending on it are then left as nodata — matching the reference, which writes no file at all for those bands.\n\nDependencies among them, as the reference enforces them:\n\nSlope (dhdx, dhdy) gates the displacement-to-velocity operator, the scale factors, the expected offset and the search extent. Without it the reference writes none of them, since all four need the surface normal.\nVelocity (vx, vy) gates the expected offset; search range (srx, sry) gates the search extent. Both additionally require slope.\nChip size and the stable-surface mask are independent of slope.\n\nAll arrays must share axes with each other and with the window.\n\n\n\n\n\n","title":"ImagePairGeometry.GeometryInputs"},{"category":"type","location":"#ImagePairGeometry.GeometryParams","page":"Home","text":"GeometryParams(; chip_size_0 = 240.0, scaling = SearchRangeScaling(),\n                 zero_doppler_start = SceneCenterStart())\n\nScalar parameters of the computation.\n\nchip_size_0: reference chip size in meters, 240.0 in production (testGeogrid.py:358).\nscaling: the search-range inflation, see SearchRangeScaling.\nzero_doppler_start: where the radar path starts each zero-Doppler solve. SceneCenterStart() is the reference's behavior and the default; WarmStart is faster but makes the result depend on the order points are visited in. Ignored on the projected path, which has no such solve.\n\n\n\n\n\n","title":"ImagePairGeometry.GeometryParams"},{"category":"method","location":"#ImagePairGeometry.pairgeometry-Tuple{MapGrid, CoregisteredPair, GeometryInputs}","page":"Home","text":"pairgeometry(grid, pair, inputs; transform = IdentityTransform(), window = nothing,\n             params = GeometryParams(), nodata = nodata_from(nothing)) -> PairGeometry\n\nGeometry of pair at every point of grid covered by window.\n\ngrid is the target MapGrid, pair a CoregisteredPair, and inputs a GeometryInputs whose arrays cover window. transform maps grid coordinates to image coordinates — an IdentityTransform, a TransformPair, a FastTransform, any callable following AbstractCoordTransform, or a factory returning one, such as InterpolatedTransform, which is resolved once here.\n\nwindow defaults to the whole grid intersected with the pair's footprint, via footprint_bounds and grid_window.\n\nEvery point is independent, so computing a window in pieces gives the same answer as computing it whole, so blocking and threading cost nothing in fidelity.\n\nExample\n\njulia> using ImagePairGeometry\n\njulia> grid = MapGrid(geotransform = (0.0, 10.0, 0.0, 100.0, 0.0, -10.0), size = (10, 10));\n\njulia> a = ImageFootprint(origin = (5.0, 95.0), spacing = (5.0, -5.0), size = (16, 16));\n\njulia> pair = coregister(a, a; dt = 86400.0);\n\njulia> win = grid_window(grid, footprint_bounds(IdentityTransform(), pair.coordinate))\nCartesianIndices((1:8, 1:8))\n\njulia> r = pairgeometry(grid, pair, GeometryInputs(dem = zeros(size(win))); window = win);\n\njulia> size(r), nvalid(r)\n((8, 8), 64)\n\n\n\n\n\n","title":"ImagePairGeometry.pairgeometry"},{"category":"type","location":"#ImagePairGeometry.AbstractInputSource","page":"Home","text":"AbstractInputSource\n\nA source of the per-block input rasters.\n\nImplement readblock for a new source. InMemoryInputs views arrays already held; the Rasters extension adds a source that reads windows from disk, so a grid larger than memory never has to be materialized.\n\n\n\n\n\n","title":"ImagePairGeometry.AbstractInputSource"},{"category":"type","location":"#ImagePairGeometry.InMemoryInputs","page":"Home","text":"InMemoryInputs(inputs::GeometryInputs)\n\nAn input source viewing arrays already in memory, indexed relative to the full window.\n\nBlocking such a source bounds the working set rather than total memory — the arrays are already resident — which is what makes it the right thing to compare a blocked run against.\n\n\n\n\n\n","title":"ImagePairGeometry.InMemoryInputs"},{"category":"constant","location":"#ImagePairGeometry.DEFAULT_BLOCKSIZE","page":"Home","text":"DEFAULT_BLOCKSIZE\n\nBlock size in grid points when none is given: (512, 512).\n\n262144 points is a few megabytes of input per raster — small enough to bound memory on a continental grid, large enough that per-block overhead (a block read, a task claim) is negligible against the per-point work.\n\n\n\n\n\n","title":"ImagePairGeometry.DEFAULT_BLOCKSIZE"},{"category":"method","location":"#ImagePairGeometry.block_ranges-Tuple{CartesianIndices{2, R} where R<:Tuple{OrdinalRange{Int64, Int64}, OrdinalRange{Int64, Int64}}, Tuple{Int64, Int64}}","page":"Home","text":"block_ranges(window, blocksize) -> Vector{CartesianIndices{2}}\n\nwindow partitioned into blocks of at most blocksize, in column-major order.\n\nBlocks tile the window exactly: no overlap, no halo, and the union is the window. The final block in each direction is short where the window does not divide evenly.\n\n\n\n\n\n","title":"ImagePairGeometry.block_ranges"},{"category":"method","location":"#ImagePairGeometry.pairgeometry_blocked-Tuple{MapGrid, CoregisteredPair, AbstractInputSource}","page":"Home","text":"pairgeometry_blocked(grid, pair, source; transform, window = nothing,\n                     blocksize = DEFAULT_BLOCKSIZE, ntasks = nothing,\n                     params = GeometryParams(), nodata = nodata_from(nothing))\n    -> PairGeometry\n\npairgeometry computed block by block, optionally across threads.\n\nsource is an AbstractInputSource — InMemoryInputs, or the Rasters extension's disk-backed source. blocksize is in grid points. ntasks defaults to min(nblocks, Threads.nthreads()); pass 1 to run serially.\n\nThe result is bit-identical to the unblocked computation at any block size and any task count, because each point depends only on its own inputs.\n\ntransform may be a TransformPair, an AbstractCoordTransform, or — for a threaded run over a PROJ transform — a zero-argument function returning a fresh TransformPair. That last form exists for a transform holding state that is not safe to share across threads — a PROJ pipeline wraps a context that is not: each task calls the factory once and owns the result for its lifetime. fast_transform needs no factory, being immutable and stateless.\n\n\n\n\n\n","title":"ImagePairGeometry.pairgeometry_blocked"},{"category":"method","location":"#ImagePairGeometry.readblock-Tuple{InMemoryInputs, CartesianIndices{2, R} where R<:Tuple{OrdinalRange{Int64, Int64}, OrdinalRange{Int64, Int64}}}","page":"Home","text":"readblock(src::AbstractInputSource, block::CartesianIndices{2}) -> GeometryInputs\n\nThe inputs covering block.\n\nFor InMemoryInputs this is a view, so no copy is made and a blocked run over resident arrays allocates nothing per block.\n\n\n\n\n\n","title":"ImagePairGeometry.readblock"},{"category":"type","location":"#ImagePairGeometry.Bicubic","page":"Home","text":"Bicubic()\n\nCubic convolution from the sixteen nodes surrounding the point, with the Catmull-Rom parameter.\n\nReproduces an affine transform exactly, as Bilinear does, and a quadratic one besides, so it holds a given accuracy at a coarser lattice — fewer nodes to build, which is where the projection calls are. The per-point arithmetic is roughly twice Bilinear's, and it needs a two-node margin around the queried region rather than one.\n\n\n\n\n\n","title":"ImagePairGeometry.Bicubic"},{"category":"type","location":"#ImagePairGeometry.Bilinear","page":"Home","text":"Bilinear()\n\nLinear interpolation in each direction from the four nodes surrounding the point.\n\nExact for a transform that is affine over a lattice cell, which is what makes the error fall as the square of the lattice spacing. What gdalwarp uses, and what the measurements in docs/interpolated-transform.md were taken with.\n\n\n\n\n\n","title":"ImagePairGeometry.Bilinear"},{"category":"type","location":"#ImagePairGeometry.CoordLattice","page":"Home","text":"CoordLattice\n\nA coordinate transform tabulated on a regular lattice and interpolated between its nodes.\n\nCallable as (x, y, z) -> (x′, y′, z′), so it stands in for a transform anywhere one is taken — including inside a TransformPair, which is how InterpolatedTransform uses it.\n\nFields\n\nx, y: the transformed coordinates at each node, (nx, ny, nlevels). The trailing axis is the elevations in zrange: two where the transform's horizontal result depends on elevation, one where it does not.\norigin: the (x, y) input coordinate of node (1, 1).\nspacing: node spacing in input coordinates, positive in both directions.\nzrange: the elevations bounding what the lattice was tabulated over.\ninterpolation: the LatticeInterpolation used between nodes.\nflat: whether a query can read one level and ignore elevation.\n\nInterpolating linearly between two elevations is exact rather than approximate: elevation enters a horizontal coordinate only through a datum shift, which is linear in it. For two CRSs on one datum — every ITSLIVE projection — there is no such shift, so [`buildlattice`](@ref) tabulates one level instead of two and a query reads it directly.\n\nA query outside the lattice throws. Clamping instead would turn a bounds error into a plausible coordinate, which is exactly the failure this package's bitwise tests exist to catch. A NaN coordinate is not a bounds error and passes through as NaN, matching the exact path: the reference reaches a NaN pixel index from a NaN elevation and reports it in bounds (see REFERENCE.md).\n\n\n\n\n\n","title":"ImagePairGeometry.CoordLattice"},{"category":"type","location":"#ImagePairGeometry.InterpolatedTransform","page":"Home","text":"InterpolatedTransform(transform, grid, pair; lattice = 4, mode = :hybrid,\n                      interpolation = Bilinear(), window = nothing,\n                      zrange = DEFAULT_ZRANGE)\n\nA transform factory that approximates transform on a coarse lattice.\n\nPass it as pairgeometry's or pairgeometry_blocked's transform. It is an AbstractTransformFactory, so a blocked run calls it once per task and each task owns its own lattice, and whatever state the underlying transform wraps, for that task's lifetime — the same ownership the exact path needs, for the same reason.\n\ntransform is anything the exact path accepts: an AbstractCoordTransform, a TransformPair, or a zero-argument factory returning one. grid and pair give the region the lattice must cover; window defaults to the whole grid intersected with the pair's footprint, and must be the same window the result is computed over.\n\nModes\n\n:hybrid — the forward transform stays exact and only the inverse is interpolated. Every integer band is bitwise identical to the exact path, because location_x/location_y come from the forward transform alone. Drops two of the three calls per point.\n:full — both directions are interpolated, so no call is made per point at all. Faster, but location_x/location_y can differ from the exact path by one pixel: the positional error is far below a pixel, and what shifts is which side of a rounding boundary a point falls on. Sound where a one-pixel difference in a search center is acceptable, which for a correlator searching tens of pixels it may be.\n\nlattice is the node spacing as a multiple of the grid spacing; 1 puts a node at every grid point. Coarser is faster to build and less accurate — docs/interpolated-transform.md tabulates the measured error against spacing. interpolation selects the kernel: Bilinear, Bicubic or NearestNode.\n\nBoth lattices are spaced by the same ground distance. The inverse lattice is tabulated over image coordinates and extended by one image pixel beyond the footprint, because the kernel queries it at the point stepped one pixel along each image axis.\n\ntf = InterpolatedTransform(fast_transform(3413, 32624), grid, pair;\n                           lattice = 4, mode = :hybrid, window = win)\nr = pairgeometry_blocked(grid, pair, source; transform = tf, window = win, ntasks = 8)\n\n\n\n\n\n","title":"ImagePairGeometry.InterpolatedTransform"},{"category":"type","location":"#ImagePairGeometry.LatticeInterpolation","page":"Home","text":"LatticeInterpolation\n\nHow a CoordLattice interpolates between its nodes.\n\nSubtypes: NearestNode, Bilinear, Bicubic. Chosen by dispatch rather than a stored flag, so the per-point arithmetic specializes with no branch.\n\n\n\n\n\n","title":"ImagePairGeometry.LatticeInterpolation"},{"category":"type","location":"#ImagePairGeometry.NearestNode","page":"Home","text":"NearestNode()\n\nTake the value at the nearest lattice node, with no interpolation.\n\nReproduces no transform beyond the nodes themselves, so it is not a candidate for production. It exists to bound an error budget from below: comparing it against Bilinear at the same spacing separates the error the lattice's coarseness carries from the error the interpolation adds.\n\nNamed for the node rather than as Nearest to leave that name free: AutoRIFT.Nearest is a distinct resampling method for displacement fields, and a caller using both packages has both in scope.\n\n\n\n\n\n","title":"ImagePairGeometry.NearestNode"},{"category":"constant","location":"#ImagePairGeometry.LATTICE_MODES","page":"Home","text":"LATTICE_MODES\n\nThe modes InterpolatedTransform accepts: (:hybrid, :full).\n\n\n\n\n\n","title":"ImagePairGeometry.LATTICE_MODES"},{"category":"method","location":"#ImagePairGeometry.build_lattice-Tuple{Any, Extent, Tuple{Real, Real}, LatticeInterpolation}","page":"Home","text":"build_lattice(t, bounds::Extent, spacing, interpolation;\n              zrange = DEFAULT_ZRANGE) -> CoordLattice\n\nTabulate the transform t on a lattice of the given spacing covering bounds.\n\nt is called as t(x, y, z) -> (x′, y′, z′). bounds is the region the lattice must be able to answer queries in, as an Extents.Extent{(:X, :Y)} in t's input coordinates; the lattice extends a kernel-dependent margin beyond it so the interpolation stencil is complete at the boundary. spacing is a positive (dx, dy) in input coordinates.\n\nThis is the only place the underlying transform is called. A non-finite node means the transform failed over the region and is thrown rather than interpolated, since it would otherwise spread NaN across every cell touching that node.\n\nOne elevation level is tabulated where the transform's horizontal result does not depend on elevation, two where it does, so the cost is nx * ny calls rather than 2 * nx * ny for a same-datum pair. Which case holds is established by probing the two zrange extremes at the lattice's own corners and center: elevation enters a horizontal coordinate only through a datum shift, and a pipeline either applies one everywhere over a region this size or nowhere.\n\n\n\n\n\n","title":"ImagePairGeometry.build_lattice"},{"category":"method","location":"#ImagePairGeometry.latticesize-Tuple{CoordLattice}","page":"Home","text":"latticesize(L::CoordLattice) -> NTuple{2,Int}\n\nNumber of nodes in each direction.\n\n\n\n\n\n","title":"ImagePairGeometry.latticesize"},{"category":"module","location":"#ImagePairGeometry.ImagePairGeometry","page":"Home","text":"ImagePairGeometry\n\nGeometry of a co-registered image pair on a map-projected grid.\n\nFor each point of a grid defined in projected coordinates (northing/easting), the geometry of a pair of images relates map coordinates to image coordinates in both directions:\n\nforward: grid point (x, y, z) to pixel index in each image of the pair,\ninverse: a pixel displacement between the two images to a velocity in map coordinates.\n\nDerived per grid point alongside that mapping: the expected displacement implied by a reference velocity field, the search extent in pixels implied by a search-range field, chip size converted from meters to pixels, and the scale factors relating image-space distance to ground distance.\n\nRadar and Cartesian (e.g. optical) imagery differ in how the forward mapping is obtained — orbit state vectors and a DEM for the former, map projection information for the latter — and in the unit vectors that define the inverse operator.\n\nThe pixel displacement itself is estimated elsewhere, by a feature-tracking or correlation routine; this package supplies the geometry that routine searches within and the operator that converts its result to velocity.\n\nREFERENCE.md records the reference implementation and every deliberate divergence from it.\n\n\n\n\n\n","title":"ImagePairGeometry.ImagePairGeometry"},{"category":"function","location":"#ImagePairGeometry.blocksize_from_chunks","page":"Home","text":"blocksize_from_chunks(source; floor = 256) -> NTuple{2,Int}\n\nA block size aligned to a raster source's own chunking. Defined when Rasters is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.blocksize_from_chunks"},{"category":"function","location":"#ImagePairGeometry.image_footprint","page":"Home","text":"image_footprint(image) -> ImageFootprint\n\nAn ImageFootprint describing where a raster sits. Defined when Rasters is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.image_footprint"},{"category":"function","location":"#ImagePairGeometry.mapgrid","page":"Home","text":"mapgrid(dem) -> MapGrid\n\nA MapGrid describing a raster's grid. Defined when Rasters is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.mapgrid"},{"category":"function","location":"#ImagePairGeometry.write_geotiffs","page":"Home","text":"write_geotiffs(dir, g::PairGeometry) -> Vector{String}\n\nWrite a result as the reference's nine GeoTIFFs. Defined when Rasters is loaded.\n\n\n\n\n\n","title":"ImagePairGeometry.write_geotiffs"},{"category":"type","location":"#ImagePairGeometryRastersExt.RasterInputs","page":"Home","text":"RasterInputs(; dem, dhdx = nothing, ..., ssm = nothing)\n\nAn input source reading each block's window from rasters.\n\nAccepts the same eleven optional rasters as GeometryInputs, and each may be lazy: a disk-backed Raster is read one window at a time, so a grid larger than memory is never materialized. All must share the grid the geometry is computed on.\n\nOpen each with missingval = nothing, so its stored values arrive unmasked:\n\nparam(name) = Raster(name; lazy = true, missingval = nothing)\nsrc = RasterInputs(dem = param(\"h.tif\"), vx = param(\"vx.tif\"), ...)\n\nA raster whose element type admits missing is refused. Which value counts as missing is decided by NoDataPolicy from the DEM's sentinel — the reference applies that one sentinel to every raster — so a raster's own declared nodata is not it. The ITS_LIVE parameter rasters declare nodata 0 for chip size and stable surface and 32767 for search range, all of which are ordinary data here.\n\nWindows are indexed in grid coordinates, so the rasters must cover the whole grid, not just the window — which is the usual case, since the DEM is what defines the grid.\n\n\n\n\n\n","title":"ImagePairGeometryRastersExt.RasterInputs"},{"category":"method","location":"#ImagePairGeometry.blocksize_from_chunks-Tuple{ImagePairGeometryRastersExt.RasterInputs}","page":"Home","text":"ImagePairGeometry.blocksize_from_chunks(src::RasterInputs; floor = 256) -> NTuple{2,Int}\n\nA block size aligned to the inputs' own chunking.\n\nReading a window that straddles chunk boundaries reads whole chunks and discards the rest, so a block that is a whole number of chunks reads each byte once. floor guards the pathological case: a striped GeoTIFF — what Rasters.write produces by default — reports chunks one row tall, and a block one row tall would issue a read per row.\n\nReturns ImagePairGeometry.DEFAULT_BLOCKSIZE when no input is disk-backed, since then there is no chunking to align to.\n\n\n\n\n\n","title":"ImagePairGeometry.blocksize_from_chunks"},{"category":"method","location":"#ImagePairGeometry.image_footprint-Tuple{AbstractRaster}","page":"Home","text":"ImagePairGeometry.image_footprint(image::AbstractRaster) -> ImageFootprint\n\nAn ImageFootprint describing where image sits, for coregister.\n\nOrigin is the first pixel's center, which is what ImageFootprint documents and what the reference's startingX/startingY are — so the pixel edge the geotransform names is shifted half a pixel inward.\n\nOnly the geometry is read, never the pixels, so this is cheap on a disk-backed scene and needs no data at all.\n\n\n\n\n\n","title":"ImagePairGeometry.image_footprint"},{"category":"method","location":"#ImagePairGeometry.mapgrid-Tuple{AbstractRaster}","page":"Home","text":"ImagePairGeometry.mapgrid(dem::AbstractRaster) -> MapGrid\n\nA MapGrid describing dem's grid.\n\nThe grid's origin is the outer edge of the first pixel, the convention MapGrid and GDAL share. For a raster backed by a file that is GDAL's geotransform, read from the file. For one built in memory the convention is inferred from the lookup's sampling, and a Center or point lookup is adjusted by half a pixel, since taking its coordinates for edges would offset every output by that much.\n\nA raster whose lookups are not regularly spaced is refused: the index arithmetic this package reproduces assumes a constant step.\n\n\n\n\n\n","title":"ImagePairGeometry.mapgrid"},{"category":"method","location":"#ImagePairGeometry.write_geotiffs-Tuple{AbstractString, PairGeometry}","page":"Home","text":"ImagePairGeometry.write_geotiffs(dir, g::PairGeometry) -> Vector{String}\n\nWrite g as the reference's nine GeoTIFFs in dir, returning the paths written.\n\nNames, band order, data types and nodata values match the reference, so a consumer built against its output reads these unchanged. Int32 bands are written as Int32 and Float64 as Float64, as the reference writes GDT_Int32 and GDT_Float64.\n\nThe two off2vel files get two bands for a projected result and three for a radar one, matching geogridOptical.cpp:461 and geogridRadar.cpp:634,652. The count comes from the result itself, via reference_files — a reader indexes bands positionally, so an extra band would shift every band after it without any error.\n\nA file whose bands are entirely nodata is skipped, matching the reference: it writes no file at all for an output its inputs did not support, and a consumer distinguishes \"unsupported\" from \"computed and empty\" by the file's absence.\n\n\n\n\n\n","title":"ImagePairGeometry.write_geotiffs"},{"category":"method","location":"#ImagePairGeometryRastersExt._file_geotransform-Tuple{Any}","page":"Home","text":"_file_geotransform(r) -> NTuple{6,Float64} or nothing\n\nr's geotransform as GDAL reports it, or nothing for a raster not backed by a file.\n\nPreferred over deriving one from the lookups because it is the file's own answer. Rasters sets a lookup's sampling from the AREA_OR_POINT tag — Points for Point, Intervals{Start} for Area — while reporting the same coordinates either way, namely the geotransform's. So a raster tagged AREA_OR_POINT=Point, which both the ITS_LIVE parameter rasters and the Landsat scenes are, gets Points sampling with coordinates that are already pixel edges. Inferring the convention from the sampling would take those edges for centers and displace the grid half a pixel.\n\n\n\n\n\n","title":"ImagePairGeometryRastersExt._file_geotransform"}]
}
